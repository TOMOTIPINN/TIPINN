# 引き継ぎメモ（自動生成）

> `/handoff` が生成。**手で編集しない**（次回の生成で消える）。
> 生成日時: 2026-09-17 11:35 JST
> HEAD: `e76bc67`
> 対象範囲: `5af3e54..HEAD`（8 commits）

---

## この期間に完了したこと

**§16（店長 Inbox の可視性トグル廃止）を決定・実装・本番確認まで完了。**

| commit | 内容 | 根拠(docs) |
|---|---|---|
| `e42a089` | HANDOFF.md を再生成（支払いボタン押下の確認結果を反映） | `HANDOFF.md` |
| `603134f` `f2f72a2` `be5a681` | **Inbox のトグルを廃止し `share_scope` を表示。** 件数は**サロン全体**を count（`limit(50)` を母集団にすると 53 件でも「50」と出続ける）。文言は `SHARE_SCOPES` が正・既知の2値以外はバッジを出さない。`visibility` のフォールバックと注意ブロックを削除。`POST /api/manager/visibility`（`visibility` を書く唯一の経路）を削除。`.tag-quiet` を新設 | §16 決定1〜3 |
| `87c8c63` `488d1bb` | 本番確認で見つかった見た目2件を修正: バッジを**名前の下の段に固定**（上段が `flex-wrap: wrap` で行ごとにずれていた）／バッジを**両方 `.tag-quiet`** に（お客様の選択に優劣をつけない。ミントは好調/上昇の差し色） | §16 |
| `8aa73a5` `e76bc67` | §16 を新設し、見た目の修正方針と**本番実機確認（2026-09-17）**を追記。`10_domain.md` に「`visibility` 列は DB にあるがコード未使用」、`20_product.md` の Inbox 役割と API 一覧を更新 | §16 |

**`reviews.visibility` 列は DB に残したまま**（§16 決定4・migration を増やさない）。
**スタッフ側のファイルは1行も触っていない**（§16 は Inbox 内に閉じた変更）。

---

## 残タスク

### 実装済み（commit hash 必須）

| 項目 | commit |
|---|---|
| 公開同意の記録（本人同意）／既存スタッフ22名の同意記録（2026-09-15 に本番22件を確認） | `6205fa9` ／ §10.1 決定5 |
| 未受諾3日経過の店長画面表示（`UNBOUND_ALERT_DAYS=3`） | `a525f35` |
| 年齢確認（Stripe metadata に記録・echo の DB には保存しない） | `8af3c98` |
| 感想受付3日・1来店1感想 ／ 二重決済の検知と運営者通知 | `abc2bb0` ／ `66c9063`+`e692dd0` |
| 購入可能な感想のみ購入可（`isPurchasableReview` 単一化） | `84234d2` |
| 購入条件から rating 撤廃・低評価＋購入ありを名前＋ティアで本人に届ける（本番確認済み） | `617726c`〜`167da5c` |
| **Inbox の可視性トグル廃止・`share_scope` 表示（本番確認済み）** | `603134f`〜`488d1bb` |
| 引き継ぎメモの自動生成 | `0761cd8` |

### 決定済み・未実装（docs の節番号 必須）

| 項目 | 節 |
|---|---|
| **`/mypage` の補助導線2ボタン**（着手条件だった §14 の本番実機確認は完了。**着手可**） | §15 |
| **退会・削除機能**（窓口への申出を手作業。**手順書未作成**）／**保存期間の削除処理**（`login_attempts` 以外なし・最短は通知履歴6か月）／**`audit_log.note` に個人情報を書かない運用ルール** | `70_legal.md` §5-1〜3 |
| organizations（会社概念）の導入・`/owner` 新設。2026年10月末目標 | §8.1 |
| 複数サロンでの Stripe アカウント共有（着手は要望2〜3件目から） | §7.1 |
| 購入済みの感想で「¥100 を支払う」ボタンが押せる見た目のまま残る（**課金経路が無いことは本番確認済み＝見た目だけ**。無効化か非表示に） | §14 未対応2 |
| **Niii の VIP 特典で同じ名前の特典が2つ表示された**（設定の重複登録か表示の問題か切り分けから） | §14 未対応3 |
| Inbox の `limit(50)` のページング／行から詳細画面への導線（**どちらも §16 で範囲外と明記**） | §16 範囲外 |
| **`/manager/salon/new` の通知遅延の下限・刻みを `/manager/visit` に揃えるか** | `60_incidents.md` 2026-09-16 |
| **店内QRの持ち出し対策の要否判断**（ローテート／掲示の工夫／検知。案であり決定ではない） | `60_incidents.md` 2026-09-16 |
| `reviewId` 必須化で常に true になった `route.ts` 分岐の整理 | §13 未対応3 |
| 既存 eslint 指摘9件の整理 | §13 未対応4 ／ §14 未対応5 |
| `failed` の理由が DB に残らない／100% 到達後は通知も送れない ／ 運営者画面の感想率が来店月と感想月をまたぐとずれる | §9 穴1・4 ／ §12.1 未対応1 |
| `staff-invite.ts` の `line_taken` ガードが `archived_at` で絞っていない ／ LINE 公式アカウント認証の再申請要否（外部サロン展開前に判断） | `60_incidents.md` 2026-08-30・2026-09-11 |

### 法務（依頼中・回答待ち）

| 項目 | 節 |
|---|---|
| プライバシーポリシー最終チェック（2026-09-15 送付）＋告知文の法的十分性（第5条4項とあわせて依頼） | `70_legal.md` §3・§13 未対応1 |
| **未依頼**: サロン向け規約／購入者向け利用条件／carta↔echo Labs 業務委託契約／PayPay の前提条件 | `70_legal.md` §4 |
| **範囲外**: 労基法（賞与の賃金性）は山口先生に確認できていない | `70_legal.md` §4 |

※ §14 決定4 により、購入画面の告知文とポリシー第5条4項は変更不要。§16 は画面内の表示のみで**法務に影響しない**。

### 未確認

| 項目 | 出典 |
|---|---|
| **本番に `reviews.visibility` 列があるか**（§16 で読み書きしないので実装の前提にはならないが、0006 の適用記録が docs に無い） | §16 未対応2 |
| **Niii の §16 確認と、CARTA 以外に `share_scope` が `null` / `either` の行があるか**（CARTA は全行にバッジが出た＝無い。優先度は低い） | §16 未対応1・3 |
| **VIP 特典が2つ表示された原因**（設定の重複登録か表示側か） | §14 未対応3 |
| LINE Official Account Manager の手動配信が `consumption` に含まれるか ／ 当月集計のリセット時刻・タイムゾーン | §9 穴2・3 |
| webhook の実到着時間・再送頻度 | §13 未対応5 |
| **`rating` が NULL の感想の扱い**（低評価と同じに倒している。RPC が 1..4 を強制しているため実データには無い想定） | §14 未対応4 |
| 2時間以上経過後も LINE 通知が届いていないケースの有無 | `60_incidents.md` 2026-09-03 |
| **顧客削除時の FK 挙動**（手順書が書けない原因） | `70_legal.md` §5-1 |
| `audit_log` のトリガー定義（prosrc）／RLS の有効状態・ポリシー数 | `70_legal.md` §5 補足 |
| 特商法の販売業者の**回答日**（内容は確定）／2026-07-22・07-30 の相談分の**回答有無**／最終チェックが 5万円の範囲に含まれるか（**送付文に書いていない**） | `70_legal.md` §1・§4・§3 |
| 実在しない staff 行が**他サロンにもあるか** | `60_incidents.md` 2026-09-15 |
| 店内QRの持ち出しによる**来店記録の実害**（発生の記録なし） | `60_incidents.md` 2026-09-16 |
| `/manager/salon/new` に 45 等を入力して**実際に作成が失敗するか**（該当入力の記録なし） | `60_incidents.md` 2026-09-16 |

---

## 本番で未確認

コードはあるが、**docs に本番実機確認の記録が無い**もの。

| 項目 | commit | 状況 |
|---|---|---|
| §14 の対照群5（`manager_only`） | `617726c`〜`167da5c` | §13 で確認済み・§14 で購入条件も表示条件も触っていないため未実施。優先度は低い |
| 年齢確認（18歳以上チェック） | `8af3c98` | §11 に実機確認の節が無い。§13・§14 の購入確認で間接的に通っているが §11 として未記録 |
| 未受諾3日経過の「未参加・N日経過」表示 | `a525f35` | §10.1 決定7 に実装の記載はあるが実機確認の記録が無い |
| スタッフ本人の受諾フロー | `6205fa9` | §10.1 未確認1 に「**実機確認は未実施**」と明記 |

**確認済みの記録があるもの**: §9（`891972e`）／§12（16:16〜16:27）／§13（18:11〜18:46）／
§14（2026-09-17・対照群1〜4＋二重購入時のボタン押下）／**§16（2026-09-17・件数と SQL の一致・全行バッジ・API 404）**。

---

## docs と実装の食い違い

| # | docs の記述 | 実装 | 状態 |
|---|---|---|---|
| 1 | `10_domain.md:56`「share_scope: `manager_only` / `everyone`（`either` は**廃止済み**）」 | migration 0046 の RPC 本文は `('manager_only','everyone','either')` を受理（`:58`） | **食い違い**。ただし**本番 prosrc は未確認**のため、どちらにも寄せない。§16 で Inbox が既知2値のみ扱う＝`either` が実データにあれば**バッジ無し・件数の合計不足**として表に出る（CARTA では出なかった） |
| 2 | 本番の CHECK は **`10〜360 かつ 10の倍数`**（`0042:11-13`・2026-08-22 全文確認済み） | `/manager/salon/new` は下限 **30**・`step={1}` で刻み未検証（`page.tsx:57-58,216-224` / `route.ts:52-53,111`）。`/manager/visit` は 10分刻みの select で一致（`page.tsx:20-27`） | **食い違い**。コード側が DB の第3項を見ていない。実害は未確認 |

**この期間に解消**: Inbox の「「店長控え」にした感想はスタッフ本人の画面には表示されません」が
実装と違っていた件 → §16 でトグルごと廃止し、説明文を実態に合わせた（`f2f72a2`）。

---

## 運用メモ

docs に書かれているものだけ。

- **migration は SQL エディタで手動適用。`supabase db push` は使わない**（§6.1）
- **DB 書き込みは全て共有 `supabaseAdmin`。独自に `createClient` しない**（§6.2）
- **SQL は Claude Code に書かせず、チャットで書いて SQL エディタで実行する**（§6.3）
- **一度に一つの変更を実機で確認してから次へ進む**（§6.4）
- 「作成」≠「適用」≠「記録」。関数を再定義する migration は**本番 `prosrc` を先に読む**（§1.1・§1.3・§1.4）
- 優先順位: `00` > `40` > `10`/`20`/`30`/`50` > `60` > `70` > `CLAUDE.md`
- **法務の回答は `70_legal.md` を正とする。** 状態が「依頼中」のものを根拠に確定させない
- **保存期間（当社判断）**: アカウント2年／購入7年／来店3年／特典・調整3年／通知6か月／監査3年／IP 30日（`70_legal.md` §2）
- **「nun」行の対処は順序が重要**: `/kiosk` 登録に切り替えてからアーカイブする。
  逆にすると店長画面に入れなくなる（`60_incidents.md` 2026-09-15）
- **購入の条件（`@/lib/review-purchase`）と表示の条件（`@/lib/review-visibility`）は別モジュール。
  互いに import しない**（§14）。閾値を共有すると片方の都合でもう片方が黙って動く
- **感想の公開範囲はお客様の選択（`share_scope`）が唯一の軸。** 店長のキュレーション
  （`reviews.visibility`）は §16 で廃止＝**表示条件に3つ目の軸を足さない。** 文言の正は
  `@/lib/review` の `SHARE_SCOPES`（`everyone` を「全体に公開」と言い換えない＝外部公開ではない）
- §13 実機確認で**本番に残したデータ**: visit `1f7fcb76…` / review `40ad8de7…` /
  purchase `35999b35…` / VIP 特典発動1件 / 返金済み `pi_3UFsX27…`
- **§14 実機確認で本番に残したデータ（返金しない・削除しない）**:
  CARTA review `b64be72e…` / purchase `f1b2d015…`（`pi_3UGTwS7w7uzzQqfF01UFwfZ…`）、
  Niii review `20ff7475…` / purchase `7780a979…`（`pi_3UGU9R97r938WLIb1t6qhtVr`）、
  Niii の VIP 特典発動1件。テスト由来の `audit_log` 行も**削除しない方針**
- **`notify_after_minutes` の正は `10〜360 かつ 10の倍数`**（`0042`）。`0014` の `30〜360` は
  破棄済みの古い値。**第3項「10の倍数」を落とさないこと**（範囲だけ合わせても等価にならない）
- **店内QR（`salons.visit_token`）は期限なし・ローテート不可**。店外からでも `/visit` は通る
  （上限は JST 1日1回）。QR の扱いはこの前提で運用する（`60_incidents.md` 2026-09-16）

---
未確認: 17 件 ／ 食い違い: 2 件
