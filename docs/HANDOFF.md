# 引き継ぎメモ（自動生成）

> `/handoff` が生成。**手で編集しない**（次回の生成で消える）。
> 生成日時: 2026-09-20 12:48 JST
> HEAD: `f3380de`
> 対象範囲: `2d0c31d..HEAD`（6 commits）

---

## この期間に完了したこと

**§19 を4本の commit で実装し、本番実機確認の大半（確認1〜5・7・8・10・12）まで通した。**

| commit | 内容 | 根拠(docs) |
|---|---|---|
| `e17ca9a` | HANDOFF.md を再生成（§15 項目5 合格・§19 着手条件の解消） | `HANDOFF.md` |
| `b6d8086` | **§19 commit 1/4**: `staffViewMode` の `full` を **allow-list 化**。`everyone` のときだけ rating 判定に進み、それ以外（`manager_only` / `null` / `either` / 未知）は rating を見ずに「購入あり→`stamp_only`／なし→`hidden`」。`return "full"` は1つだけ | §19 決定3 |
| `726ccbe` | **§19 commit 2/4**: 詳細画面で **`stamp_only` にも気分（rating）を出す**。本文とタグは `!isStampOnly` の内側だけ。`stamp_only` かつ気分なしはセクションごと畳む（罫線の二重化なし）。`!tierDef` の 404 ガードは維持 | §19 決定2 |
| `633357d` | **§19 commit 3/4**: `myLowQuery` を `myHighQuery` の**補集合**に。`.or("share_scope.neq.everyone,share_scope.is.null,rating.lt.3,rating.is.null")`。select は `"id, created_at"` のまま | §19 実装の範囲 |
| `c371da0` | **§19 commit 4/4（唯一お客様から見える変化）**: 購入条件から `share_scope` を外す。`PURCHASE_SHARE_SCOPE` 削除・`PurchasableReviewRow` から列ごと削除・`loadReviewForPurchase` は4列に | §19 決定1 |
| `f3380de` | **§19 の本番実機確認（2026-09-20・Niii）を記録**＋**§15 に決定6 を追加**（「評価スタンプを送る」も `.btn-mint` に。ただし「感想を送る」と同時に出るときは後者を優先＝1カードに `.btn-mint` は最大1つ。未対応6 も同時に直す） | §19・§15 決定6 |

**4本とも `npm run lint` は 9 errors / 3 warnings（変更前と同数）・`npm run build` 成功。migration 無し。**

---

## 残タスク

### 実装済み（commit hash 必須）

| 項目 | commit |
|---|---|
| 公開同意の記録（本人同意）／既存スタッフ22名の同意記録（2026-09-15 に本番22件を確認）／未受諾3日経過の店長画面表示 | `6205fa9` ／ §10.1 決定5 ／ `a525f35` |
| 年齢確認（Stripe metadata に記録・DB には保存しない）／感想受付3日・1来店1感想／二重決済の検知と通知 | `8af3c98` ／ `abc2bb0` ／ `66c9063`+`e692dd0` |
| 購入可能な感想のみ購入可（`isPurchasableReview` 単一化）／購入条件から rating 撤廃・低評価＋購入ありを名前＋ティアで本人に届ける（本番確認済み） | `84234d2` ／ `617726c`〜`167da5c` |
| Inbox の可視性トグル廃止・`share_scope` 表示（本番確認済み）／「もっと見る」と公開範囲フィルタ（同） | `603134f`〜`488d1bb` ／ `cba2dcf`〜`e0b4a2c` |
| ダッシュボードの内訳表示（本番確認は一部）／`/mypage` の補助導線2ボタン（本番確認は項目1〜5・残るは項目6）／引き継ぎメモの自動生成 | `af7fc86`〜`2206bf0` ／ `84c101a`〜`4db4694` ／ `0761cd8` |
| **§19: `manager_only` の感想にも評価スタンプを贈れる**（表示の allow-list 化／詳細に rating／一覧を補集合に／購入条件から `share_scope` 削除）。**本番確認は一部**（下記） | `b6d8086` ／ `726ccbe` ／ `633357d` ／ `c371da0` |

### 決定済み・未実装（docs の節番号 必須）

| 項目 | 節 |
|---|---|
| **§20: 店長画面で「誰がどのティアを送ったか」を見られるようにする**（Inbox の行にティアバッジ＝`rating_purchases` を `.in("review_id", …)` で1本／Inbox の行から `/staff/received/[reviewId]` へリンク＝**§16・§17 が範囲外としていた導線を解消**／「最近の評価」に顧客名／5件ずつの「もっと見る」）。**migration 不要・DB 変更なし。★§19 の確認9（店長画面が変わらない）を見てから実装する★。ガードレール（恒久）**: スタッフ別のティア集計を Inbox に足さない／ティア順に並べ替えない／高ティアを強調色にしない／金額を出さない | §20・§18 A・§19 確認9 |
| **⚠️ §20 を実装しても過去の購入にはティアが出ない。** §13 決定6 で**過去21件の `review_id` は全件 null のまま遡及しない**と決めているため。**「新しいものにしか出ない」ことを店長に伝える** | §20 注意・§13 決定6 |
| **§21: `/owner`（オーナーが自社5店舗を見る）**。店舗を切り替えて既存の店長画面と同じものを見る（**既存ファイルは壊さない**）／**5店舗横断の全スタッフ一覧は作らない**／店舗単位の数字の比較は出す／**閲覧記録は作らない**。**2026年10月末目標。依存: §20 を先に実装する。** migration が要る（番号は **0048**） | §21・§8.1 |
| **§15 決定6（2026-09-20 追加）: `/mypage` の「評価スタンプを送る」を `.btn-mint` に。ただし「感想を送る」が同じカードに同時に出るときは「感想を送る」を優先**（感想が先にあるべき行動）＝**1カードに `.btn-mint` は最大1つ**。**§15 未対応6（補助文字「感想を送ったあとに、評価スタンプを贈れます。」が感想送信済みでしか出ない）も同時に直す**（`mypage/page.tsx:406-421` の同じブロック）。**差し替え文言は未決** | §15 決定6・未対応6 |
| **⚠️ §19 未対応4: `LOW_RATING_SCAN = 20`（`staff/page.tsx`）の母集団が「`full` 以外すべて」に広がった**（commit 3 で補集合化済み）。`manager_only` を多く受けているスタッフでは**20件の窓から古い購入済み行がこぼれうる**（消えるのは一覧だけ・詳細は URL があれば開ける）。定数名も実態と合わない。**CARTA は全53件のため当面は問題にならない** | §19 未対応4 |
| **退会・削除機能**（窓口への申出を手作業・**手順書未作成**）／**保存期間の削除処理**（`login_attempts` 以外なし・最短は通知履歴6か月）／**`audit_log.note` に個人情報を書かない運用ルール** | `70_legal.md` §5-1〜3 |
| 複数サロンでの Stripe アカウント共有（着手は要望2〜3件目から。organizations 導入後は「同一 `organization_id` から継承」と書ける） | §7.1 |
| **受付期間の二重管理**（`src/lib/review.ts:17` と RPC 0046 の `v_window_days`）／`notify_at` 前にもボタンを出す挙動の再判断（実運用の声待ち）／**「購入可能な最新1件にだけ導線を出す」ことの妥当性**（実機で挙動は確認済み・判断は保留） | §15 未対応2・3・4 |
| 購入済みの感想で「¥100 を支払う」ボタンが押せる見た目のまま残る（**課金経路が無いことは本番確認済み＝見た目だけ**）／**Niii の VIP 特典で同じ名前の特典が2つ表示された**（切り分けから） | §14 未対応2・3 |
| **1サロン 500 件超でのキーセット移行**（現在の最大は CARTA の 53 件。`reviews(salon_id, created_at desc, id desc)` の索引が要る＝migration） | §17 未対応2 |
| **`/admin/staff` が全サロンを対象にしている** ／ **`line_taken` ガードが `archived_at` で絞っていない**（どちらも §21 で回収する） | `60_incidents.md` 2026-09-02 ／ §21 未対応3 |
| **`/manager/salon/new` の通知遅延の下限・刻みを `/manager/visit` に揃えるか** ／ **店内QRの持ち出し対策の要否判断**（案であり決定ではない） | `60_incidents.md` 2026-09-16 |
| `route.ts` の常に true な分岐の整理 ／ eslint 指摘9件 ／ `failed` の理由が DB に残らない・100%到達後は通知も送れない ／ 感想率が来店月と感想月をまたぐとずれる ／ LINE 公式アカウント認証の再申請要否 | §13 未対応3・4 ／ §9 穴1・4 ／ §12.1 未対応1 ／ `60_incidents.md` 2026-08-30 |

### 法務（依頼中・回答待ち）

| 項目 | 節 |
|---|---|
| プライバシーポリシー最終チェック（2026-09-15 送付）＋告知文の法的十分性（第5条4項とあわせて依頼） | `70_legal.md` §3・§13 未対応1 |
| **サロンをまたいだ閲覧（同一運営会社の複数店舗・`/owner`）**: ポリシー確定版に「運営会社が複数店舗を運営する場合、その運営会社が閲覧する」と読める記述があるかを**回答後に自己確認**し、無ければ1文を足す。**`/owner` の実装は法務待ちにしない** | `70_legal.md` §4.1・§21 法務 |
| **未依頼**: サロン向け規約／購入者向け利用条件／carta↔echo Labs 業務委託契約／PayPay の前提条件。**フランチャイズ（事業者が別会社）一般の扱いも未依頼** | `70_legal.md` §4・§4.1 |
| **範囲外**: 労基法（賞与の賃金性）は山口先生に確認できていない ／ **店長の閲覧記録は依頼しない**（§21 決定4 で機能自体を作らないため） | `70_legal.md` §4 |

※ §15〜§18・§20 は画面内の表示・ナビゲーションのみで**法務に影響しない**。**§19 も変更不要**（§19 法務・告知文「お名前とスタンプの種類が、担当スタッフとサロンに表示されます。」は `manager_only` でも成立）。§21 は影響する（上記）。

### 未確認

| 項目 | 出典 |
|---|---|
| **本番に `reviews.visibility` 列があるか**（§16 で読み書きしないので実装の前提にはならないが、0006 の適用記録が docs に無い） | §16 未対応2 |
| **CARTA 以外に `share_scope` が `null` / `either` の行があるか**（CARTA は 17+36=53 で無いことを確認済み）。**§19 確認13 で Niii の分布を SQL で1回見る**（未実施）。あれば §19 決定3 の allow-list どおり「購入あり→`stamp_only`」になるかを1件見る | §16 未対応1・3 ／ §17 未対応4 ／ §19 確認13 |
| **`rating` が NULL の感想が実在するか**（§14 は低評価と同じ扱いに倒し、§18 は4段階のどこにも数えない。RPC が 1..4 を強制しているため無い想定） | §14 未対応4 ／ §18 未対応2 |
| **VIP 特典が2つ表示された原因**（設定の重複登録か表示側か） | §14 未対応3 |
| **§20 決定1 の `.in("review_id", …)` が PostgREST の URL 長上限に当たらないか** ／ **過去21件（`review_id` null）の扱いを画面で説明するか** ／ **「もっと見る」を URL に持つかクライアント state にするか** | §20 未対応3・4・2 |
| **§8.1 の未決定3点**（オーナーの認証方法／登録方法／既存データ移行＝`salons` 7行への `organization_id` 割り当て・テスト/デモサロンの扱い） | §8.1 未決定・§21 未対応1 |
| LINE Official Account Manager の手動配信が `consumption` に含まれるか ／ 当月集計のリセット時刻・タイムゾーン | §9 穴2・3 |
| webhook の実到着時間・再送頻度 ／ 2時間以上経過後も LINE 通知が届いていないケースの有無 | §13 未対応5 ／ `60_incidents.md` 2026-09-03 |
| **顧客削除時の FK 挙動**（手順書が書けない原因） ／ `audit_log` のトリガー定義（prosrc）・RLS の有効状態とポリシー数 | `70_legal.md` §5-1 ／ §5 補足 |
| 特商法の販売業者の**回答日**（内容は確定）／2026-07-22・07-30 の相談分の**回答有無**／最終チェックが 5万円の範囲に含まれるか（**送付文に書いていない**） | `70_legal.md` §1・§4・§3 |
| **「nun」行（実在しない staff 行）が他サロンにもあるか** ／ 共用端末で店長画面を使っているかの確認中。**`/owner` の店舗一覧に出るため §21 実装前に確定させる** | `60_incidents.md` 2026-09-15 ／ `70_legal.md` §6 ／ §21 未対応4 |
| 店内QRの持ち出しによる**来店記録の実害** ／ `/manager/salon/new` に 45 等を入力して**実際に作成が失敗するか**（どちらも発生の記録なし） | `60_incidents.md` 2026-09-16 |

---

## 本番で未確認

コードはあるが、**docs に本番実機確認の記録が無い**もの。

| 項目 | commit | 状況 |
|---|---|---|
| **§19 の確認6・9・11・13** | `b6d8086`〜`c371da0` | **確認1〜5・7（2026-09-20・Niii・藤原和音）と 8・12（commit 1〜3 時点）・10 は合格。** 残り: **6**（`manager_only`＋rating 1 で気分まで出て本文は出ない・**CARTA で 2026-09-21 予定**）／**9**（店長画面が変わらない・**§20 の前に見る**）／**11**（CARTA `b64be72e…` に気分が新たに出る）／**13**（Niii の `share_scope` 分布）。**9・11・13 は既存データで足りる** |
| **§15 の項目6**（受付期間の経過で「感想を送る」が消える） | `84c101a`〜`4db4694` | **項目1〜5 は合格**（1・3＝9/19 CARTA／2＝9/17 SELNI／4＝9/17 CARTA・Niii／5＝9/20 CARTA）。**残りは項目6 のみ＝2026-09-23 予定** |
| **§18 の残り3点**: SQL との件数突き合わせ／**ティア別売上の表示**と総和の一致／**HR タブに売上カードが無いこと** | `af7fc86`〜`2206bf0` | A・B・E は 2026-09-17 に目視で合格。残りは数字の裏取りと D の見落とし確認 |
| §17「お店のスタッフに」（36件）の読み切り | `1aa85fe` | 全部・店長のみで読み切りを確認済み。同じ経路なので優先度は低い |
| 年齢確認（18歳以上）／未受諾3日経過の表示／スタッフ本人の受諾フロー | `8af3c98` ／ `a525f35` ／ `6205fa9` | §11 に実機確認の節が無い（§13・§14・§19 の購入で間接的に通過）。§10.1 決定7 は実装の記載のみ、同 未確認1 は「**実機確認は未実施**」と明記 |

**§14 の対照群5（`manager_only`）は確認対象として消滅した**: §14 当時の「`manager_only` は購入できない」は §19 決定1 で撤廃され、2026-09-20 の §19 確認1〜5・7 が新しい挙動を確認している。

**確認済みの記録があるもの**: §9（`891972e`）／§12／§13／§14・§16・§17（2026-09-17）／§18（同・A/B/E のみ）／**§15（項目1〜5）**／**§19（確認1〜5・7・8・10・12）**。

---

## docs と実装の食い違い

| # | docs の記述 | 実装 | 状態 |
|---|---|---|---|
| 1 | `10_domain.md:56`「share_scope: `manager_only` / `everyone`（`either` は**廃止済み**）」 | migration 0046 の RPC 本文は `('manager_only','everyone','either')` を受理（`:58`） | **食い違い**。**本番 prosrc は未確認**のためどちらにも寄せない。CARTA の実データに `either` は**無い**（17+36=53）。他サロンは未確認。**§19 決定3 の allow-list がこの不確実さを吸収している**（未知の値は本文を出さない） |
| 2 | 本番の CHECK は **`10〜360 かつ 10の倍数`**（`0042:11-13`・2026-08-22 全文確認済み） | `/manager/salon/new` は下限 **30**・`step={1}` で刻み未検証（`page.tsx:57-58,216-224` / `route.ts:52-53,111`）。`/manager/visit` は 10分刻みの select で一致 | **食い違い**。コード側が DB の第3項を見ていない。実害は未確認 |
| 3 | §15 決定3「補助文字は『感想を送ったあとに、評価スタンプを贈れます』程度」 | その文言が入っているが、**感想送信済みでしか描画されない**（`mypage/page.tsx:406,419-421`）ため状態と合わない | **食い違い**（2026-09-19 発見）。**文言だけ・判定は正しい。** → §15 決定6 で `.btn-mint` の変更と**同時に直す**（未実装） |
| 4 | §19 決定1「購入条件から `share_scope` を外した」（実装は `c371da0` で完了） | **コード内の JSDoc 3か所が旧条件のまま**: `review/complete/page.tsx:20`（「`share_scope='everyone'`」）／`lib/review.ts:107`（「everyone・未購入」）／`lib/review-server.ts:191` は `getMypageActionsMap` の select に**使われない `share_scope` 列**が残る | **食い違い**（文言と余剰列のみ・**挙動は §19 どおり**）。§19 の実装範囲表に載っていないため未修正 |

---

## 運用メモ

docs に書かれているものだけ。

- **migration は SQL エディタで手動適用・`supabase db push` は使わない**（§6.1）／**DB 書き込みは共有 `supabaseAdmin` のみ**（§6.2）／
  **SQL は Claude Code に書かせずチャットで書く**（§6.3）／**一度に一つの変更を実機で確認してから次へ**（§6.4。§19 の commit 4本もこれ）
- 「作成」≠「適用」≠「記録」。関数を再定義する migration は**本番 `prosrc` を先に読む**（§1.1・§1.3・§1.4）。優先順位: `00` > `40` > `10`/`20`/`30`/`50` > `60` > `70` > `CLAUDE.md`
- **法務の回答は `70_legal.md` を正とする**（「依頼中」を根拠に確定させない）。**保存期間（当社判断）**: アカウント2年／購入7年／
  来店3年／特典3年／通知6か月／監査3年／IP 30日（§2）。**「nun」行は `/kiosk` 登録に切り替えてからアーカイブ**（`60_incidents.md` 2026-09-15）
- **購入の条件（`review-purchase`）と表示の条件（`review-visibility`）は別モジュール・互いに import しない**（§14）／**公開範囲は
  お客様の選択（`share_scope`）が唯一の軸**＝表示条件に3つ目の軸を足さない（§16）。文言の正は `SHARE_SCOPES`
- **購入条件は「本人・当該サロン/スタッフ宛て」だけ**（§19 決定1。`rating` も `share_scope` も見ない。「未購入」は 0047 の部分一意
  インデックスと呼び出し側が担保）。**本文を出す条件は `full` の allow-list＝`everyone` かつ `rating>=3` のみ**（§19 決定3）
- **未知の `share_scope` は「危険な方向（本文）には通さず、安全な方向（名前・ティア・rating）には通す」**（§19 決定3）。一覧側も同じで
  **`myLowQuery` は `myHighQuery` の補集合**。**`is.null` の2項を落とすと3値論理でその行がどちらの一覧からも消える**（§19・commit 3）
- **お客様向けの文言では `REVIEW_WINDOW_CALENDAR_DAYS`（暦日・4）を使う。`REVIEW_WINDOW_DAYS`（差分・3）は判定にだけ。** 言い回しは「**ご来店日を含めて**」（§15）
- **`/mypage` の「評価スタンプを送る」は購入可能な最新1件にだけ出る**（§15 決定4・2026-09-20 実機確認）。**Inbox は 50件ずつ・
  件数はサロン全体**（§17）。**offset 追記は使わない**。並びは必ず `created_at, id`（`fetch-all-rows.ts:20-21`）。§20 決定4 も同じ
- **感想と評価スタンプを合算しない**（§18 B）／**HR タブに金額を出さない**（§18 D）／**スタッフをスコア化しない**＝平均点・件数順の
  並べ替え・低評価の強調色を足さない（§18 A・§4.1・4.5）。**§20・§21 でもこれを崩さない**
- **ティア別売上は `rating_purchases.amount` の実値合計。件数×単価にしない**（§18 C・価格改定 Wonderful ¥500→¥1,000・2026-06-18）
- **`/owner` でも `salon_id` はセッション由来**＝切り替え先がそのオーナーの `organization_id` 配下かをサーバー側で検証
  （`50_security.md` §1.1）。**`getDashboardData` を複数サロンに広げない**（`staffNames` が名前文字列キーのため同名で壊れる・§21）
- **スタッフ本人の経路の本番確認は `staff.role='staff'` のアカウント**（CARTA 歩夢 / Niii 藤原和音）。運営者は店長を兼ねるため
  manager 経路に落ちる（§14・§13 未対応2）。**SELNI にはこのアカウントが無い**
- **本番に残したテストデータ（返金しない・削除しない）**: §13＝visit `1f7fcb76…` / review `40ad8de7…` / purchase `35999b35…` /
  VIP 発動1件 / 返金済み `pi_3UFsX27…`。§14＝CARTA review `b64be72e…`+purchase `f1b2d015…`、Niii review `20ff7475…`+purchase
  `7780a979…`、Niii の VIP 発動1件。**§19（9/20）＝Niii の `manager_only`＋rating 4 の感想と Thank you ¥100 の購入1件**。
  §15 項目5＝CARTA の 9/19 分（未購入）／9/20 分 `7c6064f8…`。テスト由来の `audit_log` 行も**削除しない**
- **`notify_after_minutes` の正は `10〜360 かつ 10の倍数`**（`0042`。`0014` の `30〜360` は破棄済み）。**第3項「10の倍数」を落とさない**
- **店内QR（`salons.visit_token`）は期限なし・ローテート不可**。店外からでも `/visit` は通る（上限は JST 1日1回）

---
未確認: 12 件（表の行数）／ 食い違い: 4 件
