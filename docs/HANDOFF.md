# 引き継ぎメモ（自動生成）

> `/handoff` が生成。**手で編集しない**（次回の生成で消える）。
> 生成日時: 2026-09-19 18:35 JST
> HEAD: `79689fd`
> 対象範囲: `fb20767..HEAD`（2 commits）

---

## この期間に完了したこと

**§20（店長画面のティア表示）・§21（`/owner`）を決定。どちらも実装は未着手。**

| commit | 内容 | 根拠(docs) |
|---|---|---|
| `00b0b34` | HANDOFF.md を再生成（§19 の決定を反映） | `HANDOFF.md` |
| `79689fd` | **§20・§21 を新設**（docs のみ・コード未変更）。§20＝`/manager/inbox` の行にティアバッジ＋`/staff/received/[reviewId]` への導線、`/dashboard`「最近の評価」に顧客名＋5件ずつの「もっと見る」。§21＝`/owner` は**店舗を切り替えて**既存の店長画面と同じものを見る（§8.1 の具体化）。**上書き対象に注記を1行ずつ**（§13 決定2／§16 範囲外／§17 範囲外／§19 本番確認 9／§8.1）。`20_product` に `/owner` 行を追加、`70_legal` に §4.1 を新設 | §20・§21 |

**コード・migration は変更していない**（この期間の変更は `docs/` の3ファイルのみ）。
§20 の調査で確かめた事実: 顧客名＋ティアが**同時に出るのは `/staff/received/[reviewId]` だけ**／`/manager/inbox` は
`rating_purchases` を**参照0件**／店長がその詳細に至る導線は `/staff` の Team voices 経由のみで
`manager_only` 非表示・`limit(5)`・manager ナビにリンク無しの3重。

---

## 残タスク

### 実装済み（commit hash 必須）

| 項目 | commit |
|---|---|
| 公開同意の記録（本人同意）／既存スタッフ22名の同意記録（2026-09-15 に本番22件を確認）／未受諾3日経過の店長画面表示 | `6205fa9` ／ §10.1 決定5 ／ `a525f35` |
| 年齢確認（Stripe metadata に記録・DB には保存しない）／感想受付3日・1来店1感想／二重決済の検知と通知 | `8af3c98` ／ `abc2bb0` ／ `66c9063`+`e692dd0` |
| 購入可能な感想のみ購入可（`isPurchasableReview` 単一化）／購入条件から rating 撤廃・低評価＋購入ありを名前＋ティアで本人に届ける（本番確認済み） | `84234d2` ／ `617726c`〜`167da5c` |
| Inbox の可視性トグル廃止・`share_scope` 表示（本番確認済み）／「もっと見る」と公開範囲フィルタ（同） | `603134f`〜`488d1bb` ／ `cba2dcf`〜`e0b4a2c` |
| ダッシュボードの内訳表示（本番確認は一部）／**`/mypage` の補助導線2ボタン（本番確認は未実施）**／引き継ぎメモの自動生成 | `af7fc86`〜`2206bf0` ／ `84c101a`〜`4db4694` ／ `0761cd8` |

### 決定済み・未実装（docs の節番号 必須）

| 項目 | 節 |
|---|---|
| **§19: `manager_only` の感想にも評価スタンプを贈れるようにする**（購入条件から `share_scope` を外す／購入があれば `rating` まで届ける／`review-visibility` は `manager_only` を rating 判定より前に打ち切る）。**着手条件: §15 の本番実機確認（2026-09-22）完了後。** migration 不要 | §19 |
| **§20: 店長画面で「誰がどのティアを送ったか」を見られるようにする**（決定1 Inbox の行にティアバッジ＝`rating_purchases` を `.in("review_id", …)` で1本／決定2 Inbox の行から `/staff/received/[reviewId]` へリンク＝**§16・§17 が範囲外としていた導線をここで解消**／決定3「最近の評価」に顧客名＝`dashboard-data.ts:327` のコメントを撤回に書き換える／決定4 5件ずつの「もっと見る」＝`created_at, id` の複合キー・offset 不可）。**migration 不要・DB 変更なし。着手順序は §19 →（本番確認）→ §20**（§19 本番確認 9「店長画面は変化なし」と衝突するため）。**ガードレール（恒久）**: スタッフ別のティア集計を Inbox に足さない／ティア順に並べ替えない／高ティアを強調色にしない（`.tag-quiet` の褪せグレー）／金額を出さない | §20・§18 A |
| **⚠️ §20 を実装しても過去の購入にはティアが出ない。** §13 決定6 で**過去21件の `review_id` は全件 null のまま遡及しない**と決めているため。**「新しいものにしか出ない」ことを店長に伝える**（伝えないと「壊れている」と誤解される） | §20 注意・§13 決定6 |
| **§21: `/owner`（オーナーが自社5店舗を見る）**。決定1 店舗を切り替えて既存の店長画面と同じものを見る（`getDashboardData` は既に `salonId` 引数・**既存ファイルは壊さない**）／決定2 **5店舗横断の全スタッフ一覧は作らない**／決定3 店舗単位の数字の比較は出す／決定4 **閲覧記録は作らない**。**2026年10月末目標。依存: §20 を先に実装する。** migration が要る（番号は **0048**） | §21・§8.1 |
| **退会・削除機能**（窓口への申出を手作業・**手順書未作成**）／**保存期間の削除処理**（`login_attempts` 以外なし・最短は通知履歴6か月）／**`audit_log.note` に個人情報を書かない運用ルール** | `70_legal.md` §5-1〜3 |
| 複数サロンでの Stripe アカウント共有（着手は要望2〜3件目から。organizations 導入後は「同一 `organization_id` から継承」と書ける） | §7.1 |
| **受付期間の二重管理**（`src/lib/review.ts:17` と RPC 0046 の `v_window_days`。§14 から持ち越し）／`notify_at` 前にもボタンを出す挙動の再判断（実運用の声待ち） | §15 未対応2・3 |
| 購入済みの感想で「¥100 を支払う」ボタンが押せる見た目のまま残る（**課金経路が無いことは本番確認済み＝見た目だけ**）／**Niii の VIP 特典で同じ名前の特典が2つ表示された**（切り分けから） | §14 未対応2・3 |
| **1サロン 500 件超でのキーセット移行**（現在の最大は CARTA の 53 件。`reviews(salon_id, created_at desc, id desc)` の索引が要る＝migration） | §17 未対応2 |
| **`/admin/staff` が全サロンを対象にしている** ／ **`line_taken` ガードが `archived_at` で絞っていない**（どちらも「organizations 導入時に合わせて扱う」＝§21 で回収する） | `60_incidents.md` 2026-09-02 ／ §21 未対応3 |
| **`/manager/salon/new` の通知遅延の下限・刻みを `/manager/visit` に揃えるか** ／ **店内QRの持ち出し対策の要否判断**（案であり決定ではない） | `60_incidents.md` 2026-09-16 |
| `route.ts` の常に true な分岐の整理 ／ eslint 指摘9件 ／ `failed` の理由が DB に残らない・100%到達後は通知も送れない ／ 感想率が来店月と感想月をまたぐとずれる ／ LINE 公式アカウント認証の再申請要否 | §13 未対応3・4 ／ §9 穴1・4 ／ §12.1 未対応1 ／ `60_incidents.md` 2026-08-30 |

### 法務（依頼中・回答待ち）

| 項目 | 節 |
|---|---|
| プライバシーポリシー最終チェック（2026-09-15 送付）＋告知文の法的十分性（第5条4項とあわせて依頼） | `70_legal.md` §3・§13 未対応1 |
| **サロンをまたいだ閲覧（同一運営会社の複数店舗・`/owner`）**: ポリシー確定版に「運営会社が複数店舗を運営する場合、その運営会社が閲覧する」と読める記述があるかを**回答後に自己確認**し、無ければ1文を足す。**`/owner` の実装は法務待ちにしない** | `70_legal.md` §4.1・§21 法務 |
| **未依頼**: サロン向け規約／購入者向け利用条件／carta↔echo Labs 業務委託契約／PayPay の前提条件。**フランチャイズ（事業者が別会社）一般の扱いも未依頼**（サロン向け規約と同じ便で出せる） | `70_legal.md` §4・§4.1 |
| **範囲外**: 労基法（賞与の賃金性）は山口先生に確認できていない ／ **店長の閲覧記録は依頼しない**（§21 決定4 で機能自体を作らないため） | `70_legal.md` §4 |

※ §15〜§18・§20 はいずれも画面内の表示・ナビゲーションのみで、**法務に影響しない**。§21 は影響する（上記）。

### 未確認

| 項目 | 出典 |
|---|---|
| **本番に `reviews.visibility` 列があるか**（§16 で読み書きしないので実装の前提にはならないが、0006 の適用記録が docs に無い） | §16 未対応2 |
| **Niii の §16 確認と、CARTA 以外に `share_scope` が `null` / `either` の行があるか**（CARTA は 17+36=53 で無いことを確認済み） | §16 未対応1・3 ／ §17 未対応4 |
| **`rating` が NULL の感想が実在するか**（§14 は低評価と同じ扱いに倒し、§18 は4段階のどこにも数えない。RPC が 1..4 を強制しているため無い想定） | §14 未対応4 ／ §18 未対応2 |
| **VIP 特典が2つ表示された原因**（設定の重複登録か表示側か） | §14 未対応3 |
| **§20 決定1 の `.in("review_id", …)` が PostgREST の URL 長上限に当たらないか**（`take` を増やしたとき。既定50件・最大1000件） | §20 未対応3 |
| **§20: 過去21件（`review_id` null）の扱いを画面で説明するか**は未決 ／ **決定4 の「もっと見る」を URL に持つかクライアント state にするか**も未決 | §20 未対応4・2 |
| **§8.1 の未決定3点**（オーナーの認証方法／登録方法／既存データ移行＝`salons` 7行への `organization_id` 割り当て・テスト/デモサロンの扱い） | §8.1 未決定・§21 未対応1 |
| LINE Official Account Manager の手動配信が `consumption` に含まれるか ／ 当月集計のリセット時刻・タイムゾーン | §9 穴2・3 |
| webhook の実到着時間・再送頻度 ／ 2時間以上経過後も LINE 通知が届いていないケースの有無 | §13 未対応5 ／ `60_incidents.md` 2026-09-03 |
| **顧客削除時の FK 挙動**（手順書が書けない原因） | `70_legal.md` §5-1 |
| `audit_log` のトリガー定義（prosrc）／RLS の有効状態・ポリシー数 | `70_legal.md` §5 補足 |
| 特商法の販売業者の**回答日**（内容は確定）／2026-07-22・07-30 の相談分の**回答有無**／最終チェックが 5万円の範囲に含まれるか（**送付文に書いていない**） | `70_legal.md` §1・§4・§3 |
| **「nun」行（実在しない staff 行）が他サロンにもあるか** ／ 共用端末で店長画面を使っているかの確認中。**`/owner` の店舗一覧に出るため §21 実装前に確定させる** | `60_incidents.md` 2026-09-15 ／ `70_legal.md` §6 ／ §21 未対応4 |
| 店内QRの持ち出しによる**来店記録の実害** ／ `/manager/salon/new` に 45 等を入力して**実際に作成が失敗するか**（どちらも発生の記録なし） | `60_incidents.md` 2026-09-16 |

---

## 本番で未確認

コードはあるが、**docs に本番実機確認の記録が無い**もの。

| 項目 | commit | 状況 |
|---|---|---|
| **§15 の補助導線（全体）** | `84c101a`〜`4db4694` | §15 未対応1 に「**未実施**」と明記。来店→通知時刻前後→感想送信→購入→期間経過の順に見る |
| **§18 の残り3点**: SQL との件数突き合わせ／**ティア別売上の表示**と総和の一致／**HR タブに売上カードが無いこと** | `af7fc86`〜`2206bf0` | A・B・E は 2026-09-17 に目視で合格。残りは数字の裏取りと D の見落とし確認 |
| §17「お店のスタッフに」（36件）の読み切り | `1aa85fe` | 全部・店長のみで読み切りを確認済み。同じ経路なので優先度は低い |
| §14 の対照群5（`manager_only`） | `617726c`〜`167da5c` | §13 で確認済み・§14 で購入条件も表示条件も触っていないため未実施 |
| 年齢確認（18歳以上）／未受諾3日経過の表示／スタッフ本人の受諾フロー | `8af3c98` ／ `a525f35` ／ `6205fa9` | §11 に実機確認の節が無い（§13・§14 の購入で間接的に通過）。§10.1 決定7 は実装の記載のみ、同 未確認1 は「**実機確認は未実施**」と明記 |

**確認済みの記録があるもの**: §9（`891972e`）／§12／§13／§14・§16・§17（いずれも 2026-09-17）／§18（同・A/B/E のみ）。

---

## docs と実装の食い違い

| # | docs の記述 | 実装 | 状態 |
|---|---|---|---|
| 1 | `10_domain.md:56`「share_scope: `manager_only` / `everyone`（`either` は**廃止済み**）」 | migration 0046 の RPC 本文は `('manager_only','everyone','either')` を受理（`:58`） | **食い違い**。**本番 prosrc は未確認**のためどちらにも寄せない。CARTA の実データに `either` は**無い**ことを §17 の本番確認で確かめた（17+36=53）。他サロンは未確認 |
| 2 | 本番の CHECK は **`10〜360 かつ 10の倍数`**（`0042:11-13`・2026-08-22 全文確認済み） | `/manager/salon/new` は下限 **30**・`step={1}` で刻み未検証（`page.tsx:57-58,216-224` / `route.ts:52-53,111`）。`/manager/visit` は 10分刻みの select で一致 | **食い違い**。コード側が DB の第3項を見ていない。実害は未確認 |

---

## 運用メモ

docs に書かれているものだけ。

- **migration は SQL エディタで手動適用。`supabase db push` は使わない**（§6.1）／
  **DB 書き込みは共有 `supabaseAdmin` のみ・独自に `createClient` しない**（§6.2）／
  **SQL は Claude Code に書かせず、チャットで書いて SQL エディタで実行する**（§6.3）／
  **一度に一つの変更を実機で確認してから次へ進む**（§6.4）
- 「作成」≠「適用」≠「記録」。関数を再定義する migration は**本番 `prosrc` を先に読む**（§1.1・§1.3・§1.4）／優先順位: `00` > `40` > `10`/`20`/`30`/`50` > `60` > `70` > `CLAUDE.md`
- **法務の回答は `70_legal.md` を正とする**（「依頼中」を根拠に確定させない）。**保存期間（当社判断）**:
  アカウント2年／購入7年／来店3年／特典・調整3年／通知6か月／監査3年／IP 30日（§2）。
  **「nun」行の対処は順序が重要**: `/kiosk` 登録に切り替えてからアーカイブする（`60_incidents.md` 2026-09-15）
- **購入の条件（`review-purchase`）と表示の条件（`review-visibility`）は別モジュール・互いに import しない**（§14）／
  **感想の公開範囲はお客様の選択（`share_scope`）が唯一の軸**＝表示条件に3つ目の軸を足さない（§16）。
  文言の正は `SHARE_SCOPES`（`everyone` を「全体に公開」と言い換えない）
- **受付期間の日数は、お客様向けの文言では `REVIEW_WINDOW_CALENDAR_DAYS`（暦日・4）を使う。**
  `REVIEW_WINDOW_DAYS`（差分・3）は**判定にだけ**。言い回しは「**ご来店日を含めて**」で揃える（§15）
- **`/mypage` の「評価スタンプを送る」は購入可能な最新1件にだけ出る**（`/review/complete` からは引き続き買える・§15 決定4）
- **Inbox は 50件ずつ・件数はサロン全体**（`?scope=` `?take=`・§17）。**offset 追記は使わない**。並びは必ず
  `created_at, id` の複合キー（`fetch-all-rows.ts:20-21`）。§20 決定4 の「もっと見る」も同じ規約
- **ダッシュボードは感想と評価スタンプを合算しない**（§18 B）／**HR タブに金額は出さない**（§18 D）／
  **スタッフをスコア化しない**＝平均点・件数順の並べ替え・低評価の強調色を足さない（§18 A・§4.1・4.5）。
  **§20 のティア表示・§21 の `/owner` でもこれを崩さない**（ティア順に並べない／5店舗横断の全スタッフ一覧を作らない）
- **ティア別売上は `rating_purchases.amount` の実値合計。件数×単価にしない**（§18 C・
  価格改定 Wonderful ¥500→¥1,000・2026-06-18 より前を含む期間で過大になる）
- **`/owner` でも `salon_id` はセッション由来**＝切り替え先がそのオーナーの `organization_id` 配下かを
  サーバー側で検証する（`50_security.md` §1.1）。**`getDashboardData` を複数サロンに広げない**（`staffNames` が
  名前文字列キーのため同名スタッフで集計が合流して壊れる・§21 設計上の要点）
- §13 実機確認で**本番に残したデータ**: visit `1f7fcb76…` / review `40ad8de7…` /
  purchase `35999b35…` / VIP 特典発動1件 / 返金済み `pi_3UFsX27…`。
  **§14 実機確認で残したデータ（返金しない・削除しない）**:
  CARTA review `b64be72e…` / purchase `f1b2d015…`、Niii review `20ff7475…` / purchase `7780a979…`、
  Niii の VIP 特典発動1件。テスト由来の `audit_log` 行も**削除しない方針**
- **`notify_after_minutes` の正は `10〜360 かつ 10の倍数`**（`0042`。`0014` の `30〜360` は破棄済み）。
  **第3項「10の倍数」を落とさないこと**
- **店内QR（`salons.visit_token`）は期限なし・ローテート不可**。店外からでも `/visit` は通る（上限は JST 1日1回）

---
未確認: 14 件（表の行数）／ 食い違い: 2 件
