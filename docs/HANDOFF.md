# 引き継ぎメモ（自動生成）

> `/handoff` が生成。**手で編集しない**（次回の生成で消える）。
> 生成日時: 2026-09-24 15:25 JST
> HEAD: `0240c69`
> 対象範囲: `d25ff38..HEAD`（29 commits）

---

## この期間に完了したこと

**§21 `/owner` の閲覧（3a〜3d-1）とサロン作成の組織必須化（4a〜4c）が本番に出た。** 組織は 0048、権限の穴は 0049 で塞いだ。
**プライバシーポリシーは山口先生の最終チェック済み**（2026-09-23）。退会は窓口＋手作業（手順書あり）で約束を守る。**コミット5 は方式 P で決定・未着手**。

| commit | 内容 | 根拠(docs) |
|---|---|---|
| `cd825c3` `de5a691` | **0048 適用**（`organizations` / `organization_members` / `salons.org_id` NOT NULL / `salon_invites.org_id`）。**実列名は `org_id`** | §21「2026-09-23 決定」 |
| `a8cdbc1` `aa4a6bd` | `/staff` の可視範囲を `ctx.role` に（2a）／オーナー表示を `organization_members` 由来に（2）。本番確認済み | §21 進捗 |
| `1a81c82` `cdfae2a` `4e3d8a0` `f533778` `507ee5f` `5eb99ad` | `/owner` の認可と店舗一覧（3a）／`[salonId]/dashboard`・顧客名の注記の出し分け（3b）／`[salonId]/inbox`（3c）／一覧に今月の数字と前月比（3d-1）。**すべて本番確認済み**（他組織・不存在・形式不正の ID は同じ 404） | §21 3a〜3d の決定 |
| `0c0cc38` `f920527` `1611308` | 入口でスタッフ行を持つ人を弾く（4a）／招待コードの発行で組織必須（4b）／サロン作成で招待の組織を必須（4c・**本番未検証**） | §21 コミット4 |
| `f0040ad` `d395b08` `2d2a263` | **0049 適用**（全17テーブルの anon/authenticated から TRUNCATE 等を剥がし既定値も修正）。月次診断の記録を `55_security-scan-log.md` へ移し、**月次は毎月25日**に | `50_security.md` §5-3・`55` §4 |
| `2130a42` `abe1502` `44e5932` `69de1e8` `a151d14` | §21 の各決定と本番確認の記録。**「nun」行をアーカイブ**（`/review` から消えたことを実機確認） | §21・`60_incidents.md` 2026-09-15 |
| `b44fde8` | `/mypage` 末尾に「個人情報の取り扱い・退会についてのお問い合わせ：info@echo-thanks.jp」 | `70_legal.md` §5-1 |
| `50c1b6b` `21a7aba` | **手作業の退会手順書**（行は消さず `line_user_id`・`display_name` を置き換える）／**最終利用の定義**（来店・感想・購入の最新・ログインは含めない）／最終チェック済み／§5-3 の訂正 | `70_legal.md` §7・§2・§1・§5 |
| `c8f39c7` `977f79f` | 山口先生へサロン向け規約・購入者向け利用条件と**確認3点**を依頼（2026-09-24）。未報告2点は振込で解消 | `70_legal.md` §3・§4.1 |
| `a32e447` `0240c69` | **コミット5 の決定**（方式 P・判定順・RPC・14日・audit_log に載せない・分割 5a→5b→5d→5e→5c）／§21 見出しを現状に | §21「2026-09-24 決定」 |

---

## 残タスク

### 実装済み（commit hash 必須）

| 項目 | commit |
|---|---|
| §10.1 公開同意・未受諾3日表示／§11 年齢確認／感想受付3日・1来店1感想／二重決済の検知 | `6205fa9` `a525f35` ／ `8af3c98` ／ `abc2bb0` ／ `66c9063`+`e692dd0` |
| §14 購入条件の単一化・低評価＋購入の本人通知／§16・§17 Inbox／§18 内訳（一部確認）／§15 補助導線 | `84234d2` `617726c`〜`167da5c` ／ `603134f`〜`e0b4a2c` ／ `af7fc86`〜`2206bf0` ／ `84c101a`〜`4db4694` |
| §19 `manager_only` にも評価スタンプ／§20 店長画面のティア表示（4本）／§22 `/staff` の読み切り／詳細「今週」ラベル | `b6d8086`〜`c371da0` ／ `57909bd`〜`b37c7f4` ／ `bec3b60` `12fc436` ／ `f329d52` |
| §21 実装順 1・2a・2・3a〜3d-1・4a〜4c（上表） | `cd825c3`〜`1611308` |

### 決定済み・未実装（docs の節番号 必須）

| 項目 | 節 |
|---|---|
| **§21 コミット5 オーナー招待**（方式 P）。**次は 5a＝0050 `owner_invites`＋RPC** → 5b `/admin/organizations` → 5d 発行・一覧 → 5e `/owner/join` と消費 → 5c 組織名の修正。判定順は レート制限→ログイン→コード→すでにオーナー→書き込み。成功側は 5e 直後に社内スタッフで通し当日片付け。**11月の L/MA 登録を 5 と 4c の最終確認にする** | §21「2026-09-24 決定」 |
| §21 3d-2 業態でまとめる（`/owner` 初の書き込み・時期と値の持ち方は未決）／4d サロン作成のレート制限 | §21 3d-2・進捗 ／ `50_security.md` §5-5 |
| §21 列の細部・レート制限のしきい値・テスト片付け SQL は**計画のみで未決**（5a〜5e で決める） | §21「計画で出したが、まだ決めていないもの」 |
| §15 決定6: `/mypage` の「評価スタンプを送る」を `.btn-mint`（感想が同時に出るときは感想優先）＋補助文字の修正（**差し替え文言は未決**） | §15 決定6・未対応6 |
| **退会**: 画面・API は作らず窓口＋手作業（§7 手順書）。**本人確認の方法は未決**（確認2 で依頼中）／**保存期間の削除処理**は IP 以外なし（最初の期限は通知履歴6か月）／アカウント2年の**自動処理は 2028-06 までに作る** | `70_legal.md` §5-1・§5-2・§2・§7 |
| `stamp_adjustments.note` に個人情報を書かない運用ルール／**退会後もセッションが最長30日残る**（当面は手順書でログアウトを案内） | `70_legal.md` §5-3・§5-4 |
| 3b で見つかった「アーカイブ済みで LINE を一度も紐づけなかった staff 行」がスタッフ別に並ぶ（扱い未定・§21 と別） | §21 3b の別課題 |
| アーカイブ済み staff が `line_user_id` を占有し続ける（時期未定）／`/admin/staff` が全サロン対象 | `60_incidents.md` 2026-09-02★訂正★・§21 未対応3 |
| `/api/manager/staff/archive` に last_manager ガードが無い（transfer/role にはある）／`service_role` の既定 DELETE／§1.2 に権限確認を足すか未決 | `50_security.md` §5-6・§5-4・§5-3 |
| `access-control-audit.md` が25ページのまま（現在33＝8枚未監査）／ガードを各ページに個別実装している構造的リスクの判断 | `55_security-scan-log.md` §4 |
| 「タグを一覧に出すか」（`/manager/inbox` と `/owner` の両方に効く）は未決 | §21 3c の決定 |
| Stripe アカウント共有（§7.1）／受付期間の二重管理・導線の再判断（§15 未対応2〜4）／¥100 ボタンの見た目・同名特典2つ（§14 未対応2・3）／500件超のキーセット（§17 未対応2） | 各節 |
| `/manager/salon/new` の通知遅延の下限・刻み／店内QR持ち出し対策の要否（案）／§13 未対応3・4／§9 穴1・4／§12.1 未対応1 | `60_incidents.md` 2026-09-16 ほか |

### 法務（`70_legal.md` を正とする）

| 項目 | 節 |
|---|---|
| **依頼中**: 告知文の法的十分性（2026-09-15）／**サロン向け規約**（月額・責任範囲・スタッフへの説明義務・反社条項）／**購入者向け利用条件**（PayPay の審査で URL 公開が要る）／**確認3点**（オーナー閲覧が第5条3項に当たるか・「当社所定の方法」と本人確認の程度・`/mypage` の1行で足りるか）。希望 10月中旬〜下旬、見積もりと日程も依頼（**金額・納期は未確定**） | §3 |
| §4.1 サロンをまたいだ閲覧: 第2条の定義と第5条3項は確定案で不変と自己確認済み。**最終判断は確認1 の回答待ち** | §4.1 |
| **未依頼**: carta↔echo Labs 業務委託契約／PayPay の反社・URL 以外の前提条件／フランチャイズ一般。**範囲外**: 労基法（賞与の賃金性） | §4 |

### 未確認

| 項目 | 出典 |
|---|---|
| 本番に `reviews.visibility` 列があるか | §16 未対応2 |
| CARTA・Niii 以外に `share_scope` が `null`/`either` の行があるか | §16 未対応1・3 ／ §19 確認13 |
| `rating` が NULL の感想が実在するか ／ VIP 特典が2つ表示された原因 | §14 未対応3・4 ／ §18 未対応2 |
| PostgREST の URL 長上限の実測 ／ 過去21件（`review_id` null）を Inbox で説明するか | §20 未対応3・4 |
| LINE の手動配信が `consumption` に含まれるか ／ 当月集計のリセット時刻 | §9 穴2・3 |
| webhook の実到着時間・再送頻度 ／ 2時間以上届かないケースの有無 | §13 未対応5 ／ `60_incidents.md` 2026-09-03 |
| **customers を参照する FK の本番定義**（手順書は migration の定義で書いた・`pg_catalog` 未照合）／`audit_log` のトリガー定義・PUBLIC への grant | `70_legal.md` §7.1・§5 補足 ／ `50_security.md` §5-3 |
| 退会処理後の再ログインで新しい行ができるか（コード上の根拠のみ・本番未検証）／Stripe 自体が持つ決済情報の扱い | `70_legal.md` §7.6 |
| 特商法の販売業者の回答日 ／ 2026-07-22・07-30 の相談分の回答有無 | `70_legal.md` §1・§4 |
| 「実在しない staff 行」が他サロンにもあるか | `60_incidents.md` 2026-09-15 |
| 店内QR持ち出しの実害 ／ `/manager/salon/new` に 45 等を入れて作成が失敗するか | `60_incidents.md` 2026-09-16 |
| 9/2 に「staff の UNIQUE 制約が無かった」と記録された原因（本番には2本実在） | `60_incidents.md` 2026-09-02★訂正★ |
| 0043 の CHECK と `on delete set null` の組み合わせで、消費済み招待が指す行の削除が失敗するか（推測として記録） | §21「計画で出したが…」 |
| 2026-09-06 予定の月次診断を実施したか（記録なし） | `55_security-scan-log.md` §3 |

---

## 本番で未確認

| 項目 | commit | 状況 |
|---|---|---|
| **4c サロン作成で招待の組織を必須化** | `1611308` | 原のアカウントは 4a で止まり到達できない。**11月の L/MA 登録で確認**（確認 A〜D は §21 に記載） |
| `/mypage` 末尾の問い合わせ窓口の1行 | `b44fde8` | docs に実機確認の記録なし |
| §18 の残り3点（SQL 突き合わせ・ティア別売上の総和・HR タブに売上カードが無い） | `af7fc86`〜`2206bf0` | A・B・E のみ確認済み |
| §17「お店のスタッフに」36件の読み切り ／ §11 年齢確認・§10.1 決定7・本人受諾フロー | `1aa85fe` ／ `8af3c98` `a525f35` `6205fa9` | §10.1 未確認1 は「実機確認は未実施」 |

**確認済みの記録があるもの**: §21 1・2a・2・3a〜3d-1・4a・4b（2026-09-23）／0049（同日・プロフィール保存）／§20／§15／§19／§22／§9／§12〜§14・§16・§17。

---

## docs と実装の食い違い

| # | docs の記述 | 実装 | 状態 |
|---|---|---|---|
| 1 | `10_domain.md:56`「`either` は廃止済み」 | 0046 の RPC 本文は `either` を受理 | **食い違い**。本番 prosrc 未確認。§19 決定3 の allow-list が吸収 |
| 2 | 本番 CHECK は `10〜360 かつ 10の倍数`（`0042`） | `/manager/salon/new` は下限30・刻み未検証 | **食い違い**。実害未確認 |
| 3 | §15 決定3 の補助文字 | 感想送信済みでしか描画されない（`mypage/page.tsx`） | **食い違い**。§15 決定6 で同時に直す（未実装） |
| 4 | §19 決定1「購入条件から `share_scope` を外した」 | `review/complete/page.tsx`・`lib/review.ts` の文言と `review-server.ts` の余剰列が旧条件 | **食い違い**（挙動は §19 どおり） |
| 5 | §19・§22 の挙動 | `/staff` のお客様向け注記とコメントが旧条件 | **食い違い**（画面の文言）。§22 未対応3 |
| 6 | §8.1・§21 本文は `organization_id` | 0048 の実列名は `org_id` | **食い違い**。§21「列名の記録」に**本番の `org_id` が正**と注記あり |
| 7 | §6.3「SQL は Claude Code に書かせず、チャットで書く」 | `70_legal.md` §7.4 の退会 SQL は Claude Code が書いた（`50c1b6b`・ユーザー依頼） | **食い違い**。運用ルールと実際の作業が違う。どちらに寄せるかは未判断 |

---

## 運用メモ（docs に書かれているものだけ）

- migration は SQL エディタで手動・`db push` 禁止（§6.1）／書き込みは共有 `supabaseAdmin`（§6.2）／一度に一つ実機確認（§6.4）。「作成」≠「適用」≠「記録」・関数の再定義は本番 `prosrc` を先に読む（§1.1）
- **次の migration 番号は 0050＝オーナー招待**（最終利用は migration なし・§21）。0048・0049 は適用済み
- **法務の回答は `70_legal.md` を正**（依頼中を根拠に確定させない）。保存期間: アカウント2年（最終利用＝来店・感想・購入の最新・無ければ `created_at`）／購入7年／来店3年／特典3年／通知6か月／監査3年／IP 30日
- **退会申出は `70_legal.md` §7 の手順**（行は消さない・`rating_purchases` が RESTRICT・特典の履歴は CASCADE で消えるため）。行を1つに確定できない申出は処理しない
- **audit_log に LINE ID を入れない**（`70_legal.md` §5 補足・2026-09-24 再確認）。コミット5 でも `organization_members` にトリガーを付けない
- `/owner`: オーナー判定は `organization_members` のみ（`staff.role` を見ない）／`salonId` はサーバー側で組織配下を検証し、他組織・不存在・形式不正は**同じ 404**／非オーナーは `/staff` へ／`getDashboardData` を複数店舗に広げない（同名スタッフで壊れる）
- スタッフをスコア化しない（平均・順位・低評価の強調色を足さない）。店舗の並びは登録順・前月比は上昇だけミント・赤は使わない（§18・§21 3d）
- 購入条件（`review-purchase`）と表示条件（`review-visibility`）は別モジュール（§14）。本文を出すのは `everyone` かつ `rating>=3` のみ（§19 決定3）
- 「もっと見る」は成長する take（offset 追記しない・既定値は URL にクエリを付けない）（§17・§20・§22）
- スタッフ経路の本番確認は `role='staff'` のアカウント（CARTA 歩夢 / Niii 藤原和音）。**`/owner` の対照群は Niii の東佑美と Niii のスタッフ1名**（§21）
- 本番に残したテストデータは返金・削除しない（§13・§14・§19・§15 項目5）。テスト由来の `audit_log` 行も削除しない
- **月次セキュリティ診断は毎月25日**。次回 2026-10-25（`55_security-scan-log.md` §1・§5）。Google Workspace 管理者の2段階認証はオン済み・2つ目の手段は「Google からのメッセージ」のみ

---
未確認: 14 件（表の行数）／ 食い違い: 7 件
