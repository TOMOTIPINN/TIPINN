# echo — 決定ログ（Decisions）

> **「なぜそう決めたか」の唯一の正。追記のみ。削除しない。**
> ここに書かれた決定は、忘れたころに必ず効いてくる。特に §1 は全て**実際に事故が起きた**記録である。
> 新しい決定をしたら、日付・理由・実例を付けてここに足す。
>
> 最終更新: 2026-09-19

---

## 1. 事故から学んだ鉄則（DB / migration）

### 1.1 関数を再定義する migration は、必ず本番の `prosrc` を先に読む
`drop function` → `create function` で再定義するときは、本番の現行本文を `select` して確認し、
**過去の migration が追加した処理を全部持ってくる**こと。

> **実例**: 0019 が 0014 の `notification_outbox` enqueue を落とし、7/8〜7/10 の2日間、
> 全サロンで来店リマインド通知が停止した。0021 で再統合。

### 1.2 signature を変える migration は `create or replace` では置き換わらない
PostgreSQL は関数を「名前＋引数型の並び」で識別する。引数の型や数を1つでも変えると、
`create or replace` は**別関数として新規作成**し、古い定義が残って**同名関数が複数並存**する。
PostgREST の `PGRST203`（候補を一意に選べない）で**全呼び出しが失敗**する。

`drop function if exists ...(旧シグネチャ)` を書いても、DROP の引数型が本番の現行関数と
1つでもズレると **DROP は黙って空振り**し、同じ事故になる。

**対策**: signature を変えた後は必ず確認する。

```sql
select p.oid,
       pg_get_function_identity_arguments(p.oid) as args,
       pg_get_function_result(p.oid)             as result
from pg_proc p
join pg_namespace n on n.oid = p.pronamespace
where n.nspname = 'public' and p.proname = '<関数名>';
-- 2行以上返ったら並存＝事故。古い方を drop function public.<関数名>(<旧引数型>) で除去する。
```

> **実例**: `submit_review_and_earn_stamp` が3バージョン並存し、感想送信が全て 500 になっていた。
> 2026-07-12 発見・修正。

### 1.3 「作成」≠「適用」
ローカルで `supabase/migrations/` に SQL ファイルを作っただけでは**本番 DB に一切反映されない**。
必ず Supabase SQL エディタで手動適用し（列/index を変えたら `notify pgrst, 'reload schema';`）、
REST または `information_schema` で**実在を確認してからコードを push** する。

> **実例**: 0023 を「適用した」つもりがファイル作成だけで、本番に `staff.idempotency_key` が無く
> REST が `column ... does not exist` を返し続けた。2026-07-12。

### 1.4 「適用」≠「記録」
上の裏返し。SQL エディタで本番に適用しただけでは **repo に一切残らない**。
適用したら必ず `supabase/migrations/` に**一字一句同じ SQL** を置く（冒頭に「本番適用済み・再実行しない」ヘッダー）。

> **実例**: 消費型特典の 0025/0027/0028/0029 が repo に無く、0025 は下書きの上書きで本文も消えていた。
> 本番から吸い出して復元。2026-07-16〜17・44e2bb4 で解消。

### 1.5 SQL エディタのタブを作業場にしない
Untitled のまま放置したタブは次の作業で上書きされ、書いた SQL が消える（＝ 1.4 の主因）。

**対策**: ローカルで `.sql` を先に作る → コピペして Run → ファイルはそのまま repo に。
（これで 1.4 も同時に解決する）
エディタで直接書くときは毎回 `+` で新規タブを開き、Run が通ったらその場で名前を付ける。

### 1.6 `public` に `create table` すると RLS が自動で有効化される
イベントトリガー `ensure_rls`（関数 `public.rls_auto_enable`・owner=postgres）が `ddl_command_end` で動く。
**ポリシーを書かなければ完全deny＝REST から一切読めない**。
`supabaseAdmin`（service_role）はバイパスするので書き込み・RPC は通り、**気づきにくい**。
トリガーは `RAISE LOG` のみで画面には何も出ない。

**対策**: 新テーブルを作るときは、同じ migration 内で**ポリシーまで書き切るか、
「完全deny のままにする」を意図的に選ぶ**。
（`0025 reward_redemptions` は後者＝ポリシー0本の完全deny・読む導線は 0030 等の security definer RPC 経由）

> 2026-07-17 の監査で発見。migrations に記録が無かったため 0031 として事後記録。

### 1.7 表示系 bool フラグは DB のデフォルト値を疑う
顧客の表示に関わる bool（`visit_axis_enabled` 等）は、新規サロン登録時に**カラムのデフォルトで決まる**。
表示系フラグを追加・変更するときは、デフォルトが「顧客に見える側」になっているか必ず確認する。

> **実例**: `visit_axis_enabled` が `default false` で作られており、新店の来店カードが全顧客に非表示になっていた。
> 2026-07-11 発見・修正、0022 でデフォルト true 化。

### 1.8 migration 番号 `0007` は欠番（詰め直さない）
採番時に飛ばしただけ。2026-07-17 に本番 DB・repo・git 履歴のいずれにも存在しないことを確認済み。
番号を詰め直すと各所の番号参照が全部ズレて履歴を追えなくなるため、**欠番はそのまま残す**。

### 1.9 SQL エディタで「ちょっと緩めた」制約は、その場で追いつき migration を書く
1.4 の変種で、**より見つかりにくい**。テーブルや関数が丸ごと欠けていれば REST が即エラーを返すが、
制約を緩めただけの差分は**本番が正常に動き続ける**。壊れるのは新環境を作り直したときだけなので、
何ヶ月でも気づかないまま残る。しかも repo に残った古い migration は、単に不足しているのではなく
**現実と違う値を書いた「嘘の記録」**になっていて、読んだ人を積極的に誤らせる。

**対策**:
- 制約を SQL エディタで直接変えたら、**その場で**追いつき migration を書く。「あとで」は来ない。
- 適用済み migration の DDL は書き換えない（履歴が壊れる）。代わりに**その行の直前に
  「これは古い・正は 00NN」というコメントだけ**足して、単体で読んだ人が信じないようにする。

> **実例**: `salons.notify_after_minutes` の CHECK を、通知遅延UIを10分刻みにした際に
> SQL エディタで直接 `10〜360 かつ 10の倍数` へ張り替えたが、0014 は `between 30 and 360` のまま。
> 本番は正常に動き続けていたため発覚せず、2026-08-22 のスキーマ突き合わせでようやく発見。
> 0042 として事後記録し、0014 には注意コメントのみ追加。

### 1.10 「実DBを正とする」なら、制約は**全文**を写す。表示は黙って切れる
Supabase SQL エディタの結果セルは横に切れる。`pg_get_constraintdef` の途中までを見て
「一致している」と判断すると、**存在しない条件の欠けた制約を repo に記録してしまう**。

**対策**: 制約定義はセルを展開して全文を確認する。追いつき migration 側では、期待値との比較を
`regexp_replace(def, '\s+', ' ', 'g')` で**空白を潰してから**行う（括弧・空白の入り方は
PostgreSQL のバージョンで変わるため、生の文字列等価は誤検知する）。

> **実例**: 上の 0042 で、切れた表示から `>= 10 AND <= 360` の2条件と読み、第3項
> `(notify_after_minutes % 10) = 0` を落として記録しかけた。範囲だけ合わせても等価ではなく、
> `between 10 and 360` では 15分・37分が通り、10分刻みUIの前提が DB 側から外れる。
> 適用前に全文を取り直して発見。2026-08-22。

### 1.11 記録が正しいかの唯一の実効テストは「migrations だけで作り直せるか」
本番が動いていることは、migrations が揃っている証拠に**一切ならない**（1.9）。
差分は「新環境を migrations から再構築したときだけ壊れる」形で溜まる。

**対策**: スキーマに関わる作業をしたら、`information_schema.columns` と `pg_indexes` の全件を
migrations の DDL と突き合わせる。アプリコードが触るカラムだけを見る方法では、
**コードが使っていないカラム・インデックス・制約は原理的に検出できない**。

> **実例**: 認証方式B（[[auth-method-line-b]]）の根幹である `staff.line_user_id` /
> `invite_token` / `invite_expires_at` / `bound_at` の4カラムと、部分 unique index
> `uq_staff_line_user_id` / `uq_staff_invite_token` が、本番にはあるのに migration に無かった。
> 特に `uq_staff_line_user_id` は「1 LINE = 最大1 staff」の**唯一の強制点**で、
> `resolveStaffByLineUserId` が1行に解決できる前提そのもの。
> migrations から再構築した環境ではスタッフ招待とスタッフログインが丸ごと動かない状態だった。
> 2026-08-22・0041 として事後記録。

### 1.12 `raise notice` を検証手段に選ばない。Supabase の SQL エディタでは読めない
Supabase の SQL エディタには psql の Messages タブに相当する表示が無く、
`raise notice` / `raise warning` の出力が**どこにも出ない**。しかも `Success` とだけ返るため、
**検証が通ったのか、そもそも検証結果が見えていないだけなのか区別できない**のが最悪の性質。
「警告が出ていないから OK」と読んでしまう。

**対策**: migration に検証を埋めるなら、`do $$ … raise notice … $$` ではなく
**最後に `select` を置いて結果セットとして返す**。エディタは結果セットなら必ず表示する。

```sql
-- ✅ こう書く（結果セットとして必ず見える）
select
  (select count(*) from information_schema.columns
    where table_schema='public' and table_name='staff'
      and column_name in ('line_user_id','invite_token','invite_expires_at','bound_at')) as staff_cols,
  (select indexdef from pg_indexes
    where schemaname='public' and indexname='uq_staff_line_user_id') as uq_line;
```

`raise exception`（＝失敗させる）だけは有効。エラーは必ず画面に出るので、
「満たされなければ止めたい」条件はこちらで書く。

> **実例**: 0041 / 0042 の検証を全て `raise notice` で書いたため、適用時に何も表示されなかった。
> 同等の内容を `select` で取り直して確認する羽目になった。2026-08-22。
> 適用済みの 0041 / 0042 はそのまま残す（1.9 の原則どおり、適用済み migration の DDL は書き換えない）。

---

## 2. 導出ロジックの多重化に関する決定

### 2.1 cycle 導出の式は 0027 と 0029 で二重化している（統合不可）
感想軸 `floor(earned_stamps.count / 3)` と
来店軸 `floor((実来店 + 移行delta) / visit_cycle_size)` を、
`list_available_consumable_rewards`(0027) と `list_consumable_reward_states`(0029) が**各自に持つ**。

問いが違うため1本に統合できない。
- **0027** =「今この来店で何を消せるか」＝**本日来店ゲート有り**（スキャナ用）
- **0029** =「今 権利として残っているか」＝**ゲート無し**（mypage 用）

**片方だけ直すと mypage とスキャナの表示が食い違う。式を変えるときは必ず両方を同時に直す。**

### 2.2 0030 は cycle 導出を共有しないが、日境界だけは揃える
`get_todays_redemption` / `void_reward_redemption` は「何サイクル貯まったか」を数えないため cycle 導出は不要。
ただし**「本日の来店」の引き方＝ `v_today := (now() at time zone 'Asia/Tokyo')::date` と
`visits.visited_on = v_today` は 0027/0028 と一字一句そろえる**こと。
ズレると done 画面の排他（§3.3）が崩れる。

### 2.3 TS 側で排他を作り直さない
done 画面は**サーバから来た状態だけで分岐する**。
`availableRewards` と `todaysRedemption` の排他は **0027 が保証**している（本日消込済みなら候補0行）。
TS でこれを再実装すると**三重化**になる。

---

## 3. 消費型特典に関する設計判断

### 3.1 消費順は「感想軸 → 来店軸」の固定、各軸内は FIFO

### 3.2 軸はスタッフに選ばせない
軸は現実に対応物が無い**帳簿上の概念**。見えないものを選ばせると押下がランダムになる。
一方、**どの特典を使うかは UI が選ぶ**（顧客との実在の会話なので RPC では絞らない）。

### 3.3 取消は「当日のみ・再スキャンして done 画面から」
- **誰でも可**：`redeemed_by` が端末（kiosk）経路で null ＝ **そもそも操作者を権限で縛れない**ため、
  消込と同じく在籍staff も端末も可とする（`void_reward_redemption` にロール判定を持たせない）
- **当日のみ**：本日の来店に紐づく消込1件だけを対象にする。前日以前は SQL Editor で対応
- **確認ダイアログ無し**：現場で素で押せる。必要と判った時点で足す

### 3.4 「今日は使わない」は記録しない
declined を残すかは将来の判断。別テーブルで後から足せる。

### 3.5 `redeemed_by` は端末（kiosk）経路で null
個人特定不可。`stamp_adjustments.created_by` と同じ割り切り。

---

## 4. UI / フロントの鉄則

### 4.1 送信が必要な input に `disabled` を付けない
ネイティブ form POST では **`disabled` 要素は送信データから脱落する**（HTML 仕様）。
送信中に入力を止めたいなら `readonly` ＋ `pointer-events`、
または **fetch 送信にして body を state から明示構築**する（DOM シリアライズに依存しない）。

> **実例**: `AddStaffForm` で送信中に name input を disabled にし、name 欠落で 400・
> 「作成中…」のまま復帰不能。2026-07-12・fetch 化で解消。

### 4.2 `position: fixed` のモーダルは `createPortal(..., document.body)` で出す
祖先要素に `transform`（`translateY(0)` でも該当）があると、`position: fixed` の基準が
ビューポートでなくその祖先になり、中央配置のモーダルが画面外へ飛ぶ。

> **実例**: `.animate-in` の `transform` に閉じ込められ、削除モーダルがオーバーレイだけ見えて中身が画面外。
> 2026-07-12・portal 化で解消。

### 4.3 タッチ端末の `:hover` は「タップ後に貼り付く」
iPadOS には本物の hover が無く、タップした要素の `:hover` 状態が残る。
**同じ位置に別のボタンが出現すると、その新ボタンが直前の hover 塗りを引き継いで固定される。**

**対策**: hover 規則は必ず `@media (hover: hover)` で囲む。

> **実例**: `/staff/visit` の done 画面で、「来店を記録」の hover が同位置に現れる「◯を使う」ボタンに
> 貼り付いて黒塗り固定＝**慎重に押すべき消込ボタンが最も押しやすそうに見える視覚重み逆転**。
> 2026-07-17・全 `.btn` の hover を `@media (hover: hover)` で囲って解消・5f3d7ba。

### 4.4 取り返しのつかない操作は「構造的なガード」で守る
hard delete 等は「原因を1つ直す」のではなく、**確認を経た状態でしか実行に到達できない**設計にする。
実行関数の入口で、モーダルの open 状態と実行条件を検査し、満たさなければ即 `return`。
状態の完全初期化・毎回新しいモーダルDOM（`key`）等と合わせた**多層防御**にする。

> **実例**: スタッフ連続削除の2回目で確認がスキップされた件を、
> `handleDelete` 入口ガード＋全リセット＋portal key で不能化。2026-07-12。

### 4.5 「ランク」（件数由来の A/B/C/D）は意図的に無効。「ティア」とは別物

**この2つは繰り返し取り違えられる。名前が似ているだけで、出自も可否も違う。**

| | ランク | ティア |
|---|---|---|
| 実体 | echo が**通算件数から自動導出**する A/B/C/D | **お客様が選んだ**評価スタンプの種類 |
| 正 | `@/lib/staff-stats` の `rankForCount()` | `@/lib/rating-tiers` の `RATING_TIERS`（👍☕🍰💐👑） |
| 表示 | **どの画面でも出さない**（`RANK_ENABLED = false`） | `/staff/received/[reviewId]` で**無条件に表示**（hero 絵文字＋tier名） |
| スイッチ | `RANK_ENABLED`（恒久 false） | **無し**。フラグで包んでいない |

**ランクを恒久的に false にする理由**（2026-08-22 に再確認・据え置き）:

- **通算基準の累積指標**であり、「届いた瞬間の体験」ではない。個別レビュー詳細のランクも
  `rankForCount(totalCount)` ＝ 通算で、その1件とは関係がない。
- 累積の序列はスタッフ間の**比較・ランキング化**を招く。echo は ES 向上のためのアプリで、
  スタッフを順位付けする道具ではない（`docs/00_philosophy.md`）。
- 閾値（100/50/20）が**仮置き**のため、若手が構造的に D 固定になる。「育成の目安」にすらならない。

一方**ティアは見せる**。お客様が能動的に選んだ贈り物であり、届いた瞬間の体験そのものだから。
ただし **¥金額は出さない**（原則6・賞与非連動）。スタッフ画面は `rating_purchases.amount` を
**select すらしない**構造で担保している（`/staff/received/[reviewId]` は `.select("tier")` のみ、
`/staff` は `count: exact, head: true` で行を引かない）。この構造を壊さないこと。

**ティア別の集計・内訳を `/staff` に足さない。** 累積のティア別内訳は、ランクと同じく比較を招く。
ティア別集計が存在してよいのは `/dashboard`（`eval-data.ts` / `dashboard-data.ts`）だけ。

> **実例**: 「`RANK_ENABLED = false` でティア表示が全面 OFF になっている」という前提で
> フラグを場面別に分割する改修が提案された。実際にはティアは最初から表示されており、
> `RANK_ENABLED` を触っても**ティアの表示は1ピクセルも変わらない**。
> 2026-08-22・調査の結果コード変更は不要と判断し、取り違え防止のコメントとこの項目のみ追加。

---

## 5. PWA / iOS に関する知見

### 5.1 ホーム画面アプリ名は「どの追加経路か」で決まるソースが変わる
- **新経路（iOS 17.4+・有効な manifest がある）**: アプリ名も着地も **manifest の `name` / `start_url`** が優先
- **旧経路（manifest が読めない・standalone 無効）**: `apple-mobile-web-app-title` → 無ければ `<title>`
- **Android/Chrome**: 経路によらず常に manifest の `name`

**対策**: どちらの経路に落ちても同じ名前になるよう、
**`manifest.name` と `appleWebApp.title` を同一文字列にそろえる**。

> ⚠️ 2026-07-16 に「iOS は常に apple-mobile-web-app-title から取る」と記録したが、
> これは**旧経路のみ成立**で、新経路では manifest.name が勝つ。当時の断定は誤り。2026-07-17 に実機で訂正。

### 5.2 iOS はアプリ名/アイコンを追加時に確定・キャッシュする
名前やアイコンを変えても既存のホーム画面アイコンには反映されない。
反映には**一度削除→再追加**が必要（実機検証時の必須手順）。

### 5.3 standalone PWA は Safari と別 cookie ジャー
`/kiosk` は**アイコン起動のたびに `start_url=/kiosk/setup` を通り cookie を張り直す**設計。
これで ①standalone 別ジャー隔離 ②iOS ITP 失効 ③cookie 1年超え、を都度救う。

`scope` は**末尾スラッシュ無し必須**（within はパス前方一致のため）。

### 5.4 【設計判断・承知の上】per-salon manifest の start_url に device_token が載る
据え置き受付端末を standalone で自己プロビジョニングさせるための対価。
**httpOnly cookie ほどは隠れない**ことを承知の上で採用する。

- web clip 自体は iCloud 同期しない。漏れ口は **Safari 履歴/ブックマークの iCloud 同期**に限られる
- 据え置き端末は**専用 Apple ID・Safari 同期OFF が推奨**
- 店長 Apple ID（同期ON）なら伝播先は本人の端末＝実害は小さいが、**共用 Apple ID は不可**
- token は**サロン単位の bearer**。被害範囲は当該サロンの来店記録に限定（他店越境不可・PII 非開放）
- 紛失時は `/manager/kiosk` で再発行＝全コピー即時失効

### 5.5 device_token は `salons` に1つ＝iPad 3台で共有
1台紛失で再発行すると全端末が失効し全台再スキャンが必要。
3台規模では許容。端末別失効が要れば `device_tokens` テーブルへ分割する。

---

## 6. 運用ルール

### 6.1 migration は SQL エディタで手動適用。`supabase db push` は使わない
### 6.2 DB 書き込みは全て `@/lib/supabase-admin` の共有 `supabaseAdmin`。独自に `createClient` しない
### 6.3 SQL は Claude Code に書かせず、チャットで書いて SQL エディタで実行する
### 6.4 一度に一つの変更を実機で確認してから次へ進む

---

## 7. Stripe / 決済に関する判断

### 7.1 複数サロンでの Stripe アカウント共有は**保留**（2026-08-22 調査・未着手）

**要望**: cartaLLC の suco と nun は同一法人・同一口座で、Stripe アカウントを分ける必要がない。
`salons.stripe_account_id` の UNIQUE（`salons_stripe_account_id_key`）を外して共有したい。
他社の多店舗オーナーからも同じ要望が出ると想定される。

**結論: 9月パイロット前にはやらない。** 理由:

1. **パイロットは UNIQUE を外さなくても回る。** Stripe アカウントを2つ作れば今日のコードで動く
   （同一法人・同一口座でも Standard アカウントは複数持てる）。パイロットの目的は
   「評価が届く体験」の検証であって多店舗課金の検証ではない。
2. **本当のコストは UNIQUE ではなく導線の新規実装**（下表 #2）。制約を外すだけでは何も共有できない。
3. **決済経路の変更は最も戻しにくい。** パイロット中に踏むと、原因の切り分けが
   「echo の体験の問題」と混ざって検証そのものが濁る。

**パイロット前にやること**: cartaLLC と「suco / nun は当面 Stripe アカウント2つで運用する」を合意する。
**着手の判断材料**: 多店舗オーナーからの要望が実際に2件目・3件目と出た時点。
**着手時の順序**: #2 → #3 → #1 → #5。
organizations 導入後（§8）は「同一 `organization_id` のサロンから `stripe_account_id` を継承」と書ける。

#### 調査結果（2026-08-22 時点。着手時はまず現状と一致するか確認すること）

`stripe_account_id` を**絞り込みキー**に使っているのは webhook の2箇所だけ。
他は全て `.eq("id", …)`＝PK 引きで値を読むだけなので UNIQUE の有無に影響されない。
`salons` に対する `.single()`/`.maybeSingle()` は14箇所あるが、**全て PK 引き**で無関係。

| # | 箇所 | 共有時に何が起きるか | 改修案 |
|---|---|---|---|
| 1 | `webhook/connect` `resolveSalonIdByAccount` | `.maybeSingle()` が2行以上でエラー → 握り潰して null → **`stripe_events.salon_id` が常に NULL**。監査が劣化する。決済記録自体は `session.metadata.salon_id` 由来なので無事 | `checkout.session.completed` は metadata から取る。`account.*` は一意に決まらないので `stripe_events` に `account_id` 列を足し salon_id は NULL 許容のまま |
| 2 | `api/manager/stripe/onboard` | **本丸。** 2つ目のサロンは `stripe_account_id` が null なので「接続」を押すと**新しいアカウントを作ってしまう**。既存アカウントを共有する導線が存在しない | 同一オーナーの既存アカウントを選ぶ UI。オーナーの他サロンから `stripe_account_id` を継承する形が最小 |
| 3 | `api/manager/stripe/return` | 同期経路が2つあり**キーが違う**。webhook は `stripe_account_id`（＝共有する全サロンを更新・正しい）、return は `ctx.salon_id`（＝操作した本人だけ）。兄弟サロンのフラグが stale になり、その間 checkout が **409 `salon_not_onboarded`** で落ちる | 両経路を「`stripe_account_id` で全行更新」の1関数に統一する |
| 4 | UNIQUE 撤去そのもの | 「同じアカウントを二重接続した事故」を検出する唯一のガードが消える。`onboard` の競合ガード `.is("stripe_account_id", null)` は自分の行しか見ていない | 意図的な共有と誤接続は DB では区別できない。同一 account を持つ salon 数を監視するビュー／アラートで代替 |
| 5 | **インデックス** | `salons_stripe_account_id_key` を drop すると**裏のインデックスも消える**。webhook の2クエリが seq scan になる | drop と同時に `create index … on salons(stripe_account_id) where stripe_account_id is not null` を張る。**必ず同一トランザクション**で |
| 6 | Stripe 側（コード外） | 領収書・明細の事業者名は connected account 単位で1つ。suco / nun で分けられない | line_item 名には `salon.name` が入る（`api/checkout`）ので明細行では区別可。事業者名の扱いは §9 の税理士確認事項 |
| 7 | テナント分離 | **影響なし。** 集計・可視性は全て `salon_id` スコープで、アカウント共有でデータが混ざる経路は無い | — |

> ⚠️ `syncAccountFromStripe` が複数行更新に耐えるのは偶然ではなく、`.select("id")` の
> **配列長**で成否を判定しているため（`.single()` にすると壊れる）。ここを「1件のはずだから」と
> `.single()` に書き換えないこと。

---

## 8. 会社（organization）概念の導入

### 8.1 organizations 設計（2026-09-03 決定）

> **§21（2026-09-19）で `/owner` の画面設計・作らないもの・法務の宿題を具体化した。**
> テーブル・可視範囲・招待フロー・未決定3点はここが正のまま。画面の中身は §21 を見る。

#### テーブル

- `organizations`（会社）
- `salons.organization_id`
- `organization_members`（誰がどの会社のオーナーか）

#### 組織の実態（導入時点）

- carta LLC: CARTA / Niii / nun Fukushima / suco / SELNI
  SELNI は当面（4年程度の想定）carta と同一組織として扱う
- L/MA: 別組織
- 第三者サロン: 契約ごとに1組織

#### 請求

会社単位。店舗ごとにスタッフ数で料金を算出し、合算する。

§7.1 の Stripe アカウント共有は、organizations 導入により「同一 `organization_id` のサロンから継承」として実装できる。

#### 可視範囲

- オーナー: 自社の全店舗のみ。他社は一切見えない
- echo Labs: 全契約サロン（現行の `/admin/*` を維持）

#### 画面設計

`/owner` を新設する。

できること:
- 読み取り: 自社全店舗の稼働状況
- 書き込み: スタッフの店舗移動、権限変更（この2つのみ）

できないこと（各店長が `/manager` で行う。現行のまま）:
- 店舗の設定、スタッフ招待、特典設定、店頭QRの表示

理由: carta の実運用では、店舗の設定とスタッフ招待は各店長が
自分で行っている。オーナーが全店舗の `/manager` に入る必要はない。
また `getStaffContext()` が単一 `salon_id` を持つ前提を崩すと、
`/manager` 配下の10箇所以上のスコープ指定すべてに影響し、
認可の事故リスクが高い。`/owner` を別に作れば既存には触れない。

書き込みを2つだけ持たせるのは、店長が退職・異動で不在になったとき、
オーナーが自分で店長を任命できる必要があるため。

#### 招待フローの変更

現行は「1コード = 1サロン」で、組織の概念がない。
複数店舗を持つ会社と契約したとき、どのサロンが同じ会社かを
システムが判別できない。

変更後:
1. echo Labs が `/admin/organizations` で会社を作る（会社名・オーナーを登録）
2. その会社に対して `/admin/invites` から招待コードを必要本数発行する
3. オーナーがコードを使ってサロンを作ると、自動的にその会社に紐づく

`salon_invites` に `organization_id` を追加し、発行フォームに
会社の選択を追加する。既存フローの変更はこれだけ。

選び間違いを防ぐため、発行画面には会社名と既存サロン数を表示する。

採用理由: 契約は会社と結ぶものであり、「A社と契約して3店舗ぶん」という
順序が実態。サロンを先に作って後から会社を割り当てるのは順序が逆で、
請求先が決まらない期間が生まれる。またコードが最初から会社に紐づいていれば、
「このサロンはどの会社か」を人が判断する場面がゼロになり、
取り違えが構造的に起こらない。

#### サロンの組織移動は機能として作らない

作った後にサロンを別の会社へ移す画面は用意しない。
日常の誤操作で組織が混ざるのを防ぐため。

発生しうるケース（例: SELNI が独立して別会社になる）は、
サロンを削除して作り直すのではなく、`organization_id` を
SQL で書き換えることで対応する。

理由: 削除・再作成では顧客の来店履歴・スタンプ・特典、
スタッフの `line_user_id` 紐付けがすべて失われる。
契約上は「carta との契約から1店舗減、新会社と新規契約」でも、
システム上は `organization_id` の付け替えのみで足りる。
契約書の話とデータの話は分けて扱う。

#### 未決定（実装前に決める）

> **2026-09-23 更新**: 1（認証方法）と 3（既存データの移行）は **§21「2026-09-23 決定」で解決済み**。
> 2（登録方法）は**オーナー招待の方式 P / Q が未決**のまま。以下は決定前の記述として残す。

1. オーナーの認証方法
   `organization_members` に LINE user ID を持たせ、`admin-guard` と
   同じ型で判定するのが素直。ただし echo Labs 運営者は env、
   オーナーは DB という二重管理になる点をどう扱うか

2. オーナーの登録方法
   `/admin/organizations` から echo Labs が登録する想定だが、
   画面の詳細は未設計

3. 既存データの移行
   `salons` 7行への `organization_id` 割り当て。
   テストサロン・デモサロンの扱い（echo Labs 自身の組織にするか、
   NULL 許容にするか）

#### 時期

2026年10月末までに実装したい。第三者サロンとの契約が
その頃から動きうるため。

---

## 9. LINE 配信通数の監視

### 9.1 上限接近と送信失敗を運営者へ通知する（2026-09-15 決定）

#### 背景

2026-09-08、LINE公式アカウントのフリープラン（月200通）が上限に到達し、
以降4日間・95件の通知が全件送信失敗した。
cron は HTTP 200 を返し続け（送信対象0件と判定）、ダッシュボードの「未送信」は
`skip_reason` 付きのみを数えるため `failed` が見えず、気づけなかった。
9/12 にライトプラン（月5,000通・追加配信不可）へ変更したが、上限で止まる構造は変わらない。

#### 決定

1. **上限接近チェック**: purge cron（1日1回・JST 3:00）で
   `GET /v2/bot/message/quota` と `/v2/bot/message/quota/consumption` を取得し、
   `totalUsage >= value × 0.8` なら運営者（`SECURITY_ALERT_LINE_USER_ID`）へ LINE push。
   - **状態は持たない。** 超過中は毎日1通届く。
   - `type` が `"none"`（上限未設定）のときは通知せず warn ログのみ。API エラー時も通知しない。
2. **送信失敗通知**: line-push cron の1回の実行で `failed` が1件以上なら、
   実行の最後に運営者へ1通 push する。
3. **共通**: 通知の失敗で cron を失敗させない。本文に顧客名・サロン名・ID を含めない。
4. 実装 commit `891972e`。2026-09-15 にローカルから本番 LINE で両通知の到達を実機確認済み。

#### 採らなかった案と理由

| 案 | 採らなかった理由 |
|---|---|
| 監視通知をメールで送る | リポジトリにメール送信の実装がなく、依存・環境変数・DNS 設定が増える。80% 時点なら LINE でまだ送れるため不要と判断 |
| 10分ごとのチェック＋「今月通知済み」フラグ | DB に状態と月替わりのリセット処理が必要。80%→100% の1,000通には現状ペースで数日以上の猶予があり、日1回で足りると判断 |
| 店舗数が増えてから導入 | 導入タイミングに気づけるかが同じ穴になるため、パイロット5店舗の時点で先に入れた |

#### 残っている穴（未解決）

1. `failed` の理由が DB に残らない。
2. LINE Official Account Manager からの手動配信が `consumption` に含まれるかは**未確認**。
3. 当月集計のリセット時刻・タイムゾーンは**未確認**。
4. 100% 到達後は運営者通知も送れない（80% 通知を見逃した場合の保険はない）。

---

## 10. スタッフのプロフィール公開に対する同意

### 10.1 同意は本人から取る（2026-09-15 決定・同日改訂）

#### 背景

スタッフの氏名・肩書・紹介文・写真は、`staff` 行が作られた瞬間から
**本人のログインなしで**顧客に表示される。公開条件は `salon_id` 一致 ＋
`archived_at is null` のみで、本人の LINE 紐付けも、本人が echo の画面に
到達することも要らない。

山口先生の回答は「チェックボックスで許諾取得＋契約書に説明義務」。
その後、**「店長には本人に代わって許諾する権限がない」**との回答を得た。

#### 決定

1. **同意は本人から取る。** 取得地点は2か所:
   - `/staff/join` → `POST /api/staff/bind`（招待されたスタッフ）
   - `/manager/salon/new`（オーナー。自分で自分を登録する経路）
   どちらも未チェックなら UI でボタンを押せず、**サーバー側でも拒否する**
   （bind は 400 `consent_required`、salon/new は `error=consent`）。
   検証はいずれも**どの書き込みより前**に置く。
2. 保存するのは `publish_consent_confirmed_at` と `publish_consent_confirmed_by`
   （**本人の `staff.id`**）。`confirmed_by` はクライアントから受け取らない。
   **既に記録のある行は上書きしない**（`.is(...,null)` ガード）。
3. **店長によるスタッフ追加では同意を取らない。** 2列 null のまま作られ、
   本人が招待を受諾したときに記録される。
4. **公開の可否は制御しない。** 顧客側の表示条件は従来どおり `archived_at is null` のみ。
   ここを公開ゲートにすると、記録のない既存行が一斉に非表示になる。
5. 既存スタッフ22名（CARTA / Niii / nun Fukushima / SELNI / suco。店舗アカウント「nun」を除く）は、
   **2026-07-31 に原が構想説明と LINE で本人から同意を得ており**、SQL で記録した。
   この22行の `confirmed_by` は**原の `staff.id`**（本人から同意を受け取った人）で、
   今後の行（`confirmed_by` = 本人）とは**意味が異なる**。
   **2026-09-15 の本番集計で `publish_consent_confirmed_at` 記録済み22件を確認**（実施済み）。
6. 同意の撤回・拒否は、店長への相談とアーカイブ運用（`archived_at`）で受ける。
7. **同意前に表示される期間は許容する**（山口先生の回答・2026-09-15）:
   「承諾なしなら消せる体制と、確認までの時間が短いのであれば問題ない」。
   よって**非表示の実装はしない**。代わりに回答の2条件をそれぞれ担保する:
   - **「消せる体制」** … アーカイブ（`archived_at`）で担保する。
   - **「確認までの時間が短い」** … 招待を受諾しないまま3日経過したスタッフを、
     店長画面（`/manager/staff`）に「未参加・N日経過」と表示し、再発行か
     アーカイブを促す1文を添えて担保する（`UNBOUND_ALERT_DAYS = 3`・
     `src/lib/staff-invite.ts`）。**表示のみ**で、自動非表示も通知もしない。
     ★未受諾の判定は `line_user_id` と `bound_at` の**両方が null**★
     （オーナー自動登録は `bound_at` を入れないため、`bound_at` 単独で判定すると
     本番7行・うち6行は店長が「未参加」と誤表示される）。

実装 commit `6205fa9`。migration 0045（2列）は 2026-09-15 適用済み。

#### 経緯

一度、**店長がスタッフ追加時に申告する**方式を実装した（`d228224`）。
その後の弁護士見解（店長に許諾権限がない）を受けて、本人同意に変更した。

#### 採らなかった案と理由

| 案 | 採らなかった理由 |
|---|---|
| 店長の申告 | 店長に許諾権限がない（弁護士見解）。一度実装したが撤回した |
| 本人ログイン時の同意ゲート | 既存スタッフは LINE で同意済み。`/staff` `/manager` の layout はセッションを読まない pass-through で、認証は14ページが個別に持つため組み込みが重い |
| 同意するまで非表示 | 顧客側4画面と購入導線に手が入る。山口先生の回答（決定7）で不要になった |
| 一定日数後の自動非表示 | 顧客側4画面と購入導線に手が入り、**弁護士の回答の範囲を超える**（回答は「消せる体制と短い確認時間」を求めており、自動で消すことは求めていない） |
| 運用ルールのみ（画面に出さず店長の運用に任せる） | 放置に気づけない。気づける仕組みがなければ「確認までの時間が短い」を担保できない |

#### 未確認

1. **本人の受諾フローの実機確認は未実施。** 未紐付けの LINE アカウントが用意できないため。
   次に新しいスタッフが参加したときに SQL で記録を確認する。
2. サロン向け規約・契約書への説明義務の記載。

---

## 11. 有料スタンプ購入時の年齢確認

### 11.1 未成年には販売せず、購入のたびに確認する（2026-09-15 決定）

#### 背景

山口先生の回答: **未成年の購入は金額を問わずコンプライアンス上問題があり、取り消されうる。**
年齢確認は「18歳以上ですか」の**チェックボックス1つで足りる**。

#### 決定

1. **未成年には有料スタンプを販売しない。**
2. 確認ステップ（`/rating` の購入確認画面）にチェックボックス「私は18歳以上です」を置き、
   **`/api/checkout` でもサーバー検証する**。決済開始 API はこの1本のみで、
   ここが唯一の実効的な関門になる（UI のボタン無効化は補助にすぎない）。
   - 受けるのは **真偽値 `true` のみ**。欠落・`false`・文字列の `"true"`/`"1"`/`"0"`・空文字は
     すべて 400 `adult_confirmation_required`。緩めて「値があれば true」にすると、
     欠落だけを弾いて `false` を通してしまう。
   - 検証位置は **payload を読んだ直後**＝`salons`/`staff` の DB 参照よりも、
     `stripe.checkout.sessions.create` よりも前。確認のないリクエストで DB も Stripe も触らない。
3. 記録は **Stripe Checkout Session の metadata**（`adult_confirmed` / `adult_confirmed_at`）。
   時刻は**サーバー生成**（クライアントの申告時刻を信用しない）。
   **echo の DB には保存しない。**
4. **状態は保存せず、購入のたびに確認する。** tier を選ぶ／選び直すたびに未チェックへ戻し、
   決済キャンセルで戻る経路はフルページ遷移（`cancel_url`）なので必ず未チェックから始まる。

実装 commit `8af3c98`。無料の感想送信（`/api/reviews`）・webhook・`rating_purchases` は変更なし
（無料経路は `/api/checkout` と共通コードを持たないため、年齢確認が誤って掛かることはない）。

#### 採らなかった案と理由

| 案 | 採らなかった理由 |
|---|---|
| 記録を残さない | 購入単位で確認を取っていたことを示せない |
| DB（`rating_purchases`）に保存 | migration と webhook（`connect/route.ts` の `recordRatingPurchase`）の改修が要る。Stripe 側の metadata で示せる以上、決済まわりに手を入れる理由がない |

---

## 12. 感想の受付期間

### 12.1 来店日を含む4暦日（＝翌日から3日間）・1来店につき1感想（2026-09-15 決定）

#### 背景

スタッフから「**当日しか感想を送れないのは不便**」という現場の声。
当日中に送れなかったお客様が、そのまま送れずに終わっていた。

#### 決定

1. **受付期間は来店日の翌日から3日間**（15日に来店なら18日まで＝当日を含む4暦日）。
   RPC の `v_window_days`（migration 0046）とアプリの `REVIEW_WINDOW_DAYS`
   （`src/lib/review.ts`）を**必ず一致させる**。片方だけ変えると、画面は受け付けるのに
   RPC が弾く（またはその逆）状態になる。
2. **1来店につき1感想**。受付期間内の**最終来店日以降**に感想があれば送信済みとする。
   `reviews` に `visit_id` が無いため、来店と感想を日付で代用している。
   - 15日来店 → 16日に感想 → もう送れない
   - 15日来店 → 16日に感想 → 17日に再来店 → 送れる
3. **既知の例外**: 前日来店分を朝に送ったあと**同じ日に再来店**すると、
   その再来店分は送れない（感想の日付 = 最終来店日 で既送信と判定される）。
   日をまたげば送れる。頻度が低いため**許容する**。
4. **LINE リマインドは変更しない**（`STALE_HOURS = 24` を含む）。
   `notify_at` は来店から最大6時間後（`salons.notify_after_minutes` の CHECK は 30〜360分）、
   そこから24時間で打ち切るため、`pending` 行は遅くとも**来店から30時間以内**に閉じる。
   受付期間（3日）と重ならないので、`already_completed` スキップへの**実害はない**。
5. エラーコード `no_visit_today` は名前を変えない。`/api/reviews` が
   `error.message` の完全一致で 409 に変換しているため、識別子としての互換を優先する
   （意味は「受付期間内の来店が無い」に広がる）。

実装 commit `abc2bb0`。migration 0046 は 2026-09-15 に SQL エディタで手動適用済み
（適用前に `routine_privileges` が postgres / service_role のみであることを確認、
適用後に `pg_get_functiondef` で新判定の反映と当日限定判定の消失を確認）。

#### 本番実機確認（2026-09-15 16:16〜16:27 JST）

テストサロン `682336ef-…` × 運営者本人の顧客アカウントで実施。

**事前に確認したこと**:
来店行は SQL で手動作成した（`submit_visit_and_earn_stamp` を通さないため
**LINE 通知は enqueue されない**）。`reviews`・`visits` に**トリガーなし**、
`earned_stamps` は `trg_audit_earned_stamps` → `fn_audit_log`
（`audit_log` への insert のみ・**外部送信なし**）であることを事前確認。

**対照群**:

| # | 条件 | 結果 |
|---|---|---|
| A | 当日来店 | 1回目 `awarded=1`・`earned_stamps` **新規行 count=1**／2回目は「今回のご来店分の感想は送信済みです」 |
| B | 4日前の来店のみ | フォームは表示される。送信で「ご来店の確認ができませんでした。感想はご来店から3日以内にお送りください。」／`reviews`・`earned_stamps` とも**作成0件** |
| C | 2日前の来店 | `awarded=1`・`count=1` |

各対照群のあとに `reviews` / `visits` / `earned_stamps` を **id 指定で削除**し、
最終的に件数0を確認した。

**残っているもの（意図的）**:
`audit_log` にテスト由来の行が4件残っている
（`earned_stamps` の INSERT/DELETE × 2・`record_id` は `ea1f5016-…` と `2b010dec-…`）。
**監査ログは手で消さない方針**のため削除していない。

#### 採らなかった案と理由

| 案 | 採らなかった理由 |
|---|---|
| 来店判定だけ広げる（既送信判定は当日のまま） | 1来店で**スタンプを最大4個**取れてしまう（受付4暦日 × 1日1個） |
| `reviews` に `visit_id` を追加 | migration・画面・集計まで変更が広がる。日付での代用で要件を満たせる |

#### 未対応（記録のみ）

1. 運営者画面（`src/lib/admin-salon-stats.ts`）の**感想率**は、分母が `visits.visited_on`（date）、
   分子が `reviews.created_at`（timestamptz）のため、**来店月と感想月がまたぐとずれる**。
   月末来店・翌月感想で当月が過小・翌月が過大になる。

---

## 13. スタッフへの有料ティア表示と購入条件

### 13.1 顧客名＋ティアを本人宛て詳細に表示・1感想1スタンプ（2026-09-15 決定）

#### 背景

- `/staff/received/[reviewId]` に「○○様から」＋ティア表示は**実装済み**だが、
  `RatingPicker` と `review/complete` が `reviewId` を渡さず、
  本番 `rating_purchases` は**全件 `review_id` が null** で機能していない。
- `/rating` と `/api/checkout` は `reviews` を参照しておらず、
  **URL 直打ちで感想なしに購入できる**（アプリ内導線は `review/complete` のみ）。
- 本番21件の内訳（2026-09-15 確認）:
  デモ seed 10（`demo_pi_`）／運営者テスト（テストサロン・感想なし）3／
  運営者本人（CARTA）4／実際のお客様（CARTA）4。
  **実際のお客様4件はすべて同じスタッフへの感想の1〜5分後の購入**で、
  感想なし購入は運営者テストのみ。

#### 決定

1. **顧客名を出す。** ティアと価格は1対1で公開されているため、¥を出さなくても
   実質的に購入額が伝わる。**これを承知の上で**、承認感を優先して出す。
2. **表示場所はスタッフ本人宛ての感想詳細のみ。**
   一覧（あなたに届いた声）と Team voices には**顧客名・有料ティアを出さない**。
   ⚠️ **この「一覧に出さない」は §20（2026-09-19）で「スタッフ画面の一覧に限る」と限定した。**
   店長画面の一覧（`/manager/inbox`・`/dashboard` の「最近の評価」）には出す。以降の正は §20。
3. **購入は感想に紐付くものだけ許可する（A'）。**
   `/api/checkout` で `reviewId` を必須とし、その review が**本人**（`session.customer_id`）の
   もので、`salon_id`・`staff_id` が一致することを検証する。
   理由: 有料スタンプを常に「声＋評価」にし、**チップ（言葉のない送金）化を防ぐ**。
   また「課金されたのにスタッフに届かない」状態をなくし、
   名前表示の説明箇所を**1か所に固定**する。
4. **1感想につき有料スタンプは1回まで。**
   `rating_purchases` に `review_id` の**部分一意制約**（`review_id is not null`）。
   **migration 0047 を 2026-09-15 に本番適用済み**（`e692dd0`）。
   適用前に既存21件が全件 null で重複なしを本番で確認済み＝衝突しなかった。
5. **二重決済**（webhook 到着前の数秒間に2回支払うケース）は
   **自動返金もロックもしない（Y）。**
   webhook で一意制約違反を検知したら運営者に通知し、**手動で返金**する。
   Direct Charge のため返金元は**サロンの Stripe アカウント**。
   頻度はほぼゼロと見込む。発生したら自動返金を再検討。
6. **過去21件は遡及しない（P）。** `review_id` は null のまま。
   理由: 実際のお客様4件は**購入時に名前表示を説明していない**ため、
   推定紐付けで表示すると同意の後付けになる。
   運営者分は 8/29 の2件が**同一感想**で一意制約と衝突する。
   デモ seed の整備はデモ営業時に別タスク。

#### 採らなかった案と理由

| 案 | 採らなかった理由 |
|---|---|
| 名前を出さずティアのみ | 承認感が弱い |
| 感想なし購入も一覧表示（B）／本人詳細のみ表示（C） | `rating_purchases` に可視性設計が必要でスコープが大きい |
| 表示のみ直して購入は制限しない（A） | 直接URL購入が**表示されない課金**として残る |
| 複数回購入を許す／期限つき | 評価として不明快。期限という新たな判断が増える |
| 自動返金（X）／未完了セッションのロック（Z） | ほぼ起きない事象に対して重い |
| 過去4件の推定紐付け（Q）／デモのみ紐付け（R） | 同意の問題。今は不要 |

#### 追加決定（2026-09-15・実装中に確定）

- **購入できる感想は「スタッフ本人に届く感想」のみ**: `customer_id` が本人・`salon`/`staff` が一致・
  `staff_id` 非 null・`share_scope='everyone'`・`rating>=3`・未購入。
  ⚠️ **`rating>=3` は §14（2026-09-16）で撤廃した。**以降の正は §14
  （この行は当時の決定の記録として残す。下の「本番実機確認」にある
  「rating2 → `invalid_review`」も §14 以後は成立しない）。
  `manager_only` や `rating<=2` の感想に有料スタンプが付くと
  「課金されたのにスタッフに届かない」が残るため。
  判定は `src/lib/review-purchase.ts` の `isPurchasableReview` に**単一化**し、
  完了画面の導線（belt）と `/api/checkout`（砦）の2か所で使う。
  ⚠️ **`share_scope='everyone'` は §19（2026-09-17）で撤廃した。**以降の正は §19
  （`manager_only` の感想にも評価スタンプを贈れる。届かない問題は表示側で解いた）。
- **決定2の修正**: 名前＋ティアは「スタッフ本人宛て詳細」に加え **「同サロンの店長」にも表示される**
  （既存の `canView` 経路）。§5「オーナーは全数字を見る」と整合するため許容。
- **告知文**「お名前とスタンプの種類が、担当スタッフとサロンに表示されます。」を
  **ティア選択画面**に置く（確認ステップには置かない）。
  規約記載で足りるかは山口先生に確認中。
- 選択画面の「サロンへの評価スタンプ購入です…」は**削除**
  （`c77e25d` 以降、確認ステップと重複。原則5の説明と特商法・審査対応4項目は確認ステップ側）。
- **エラーコード**: `review_required`(400) / `invalid_review`(400) / `already_purchased`(409)。
  **存在しない review と他人・条件外の review は区別しない**
  （review id の実在判定オラクルを作らない）。
- `reviewId` は **完了画面→`/rating`**、**LINE ログイン往復（`returnTo`）**、
  **決済キャンセル（`cancel_url`）** の3経路で保持する。

#### 実装 commit

`66c9063`（webhook 23505 分岐＋運営者通知）/ `e692dd0`（0047 部分一意インデックス・適用済み）/
`2f44afc`（完了画面の導線ゲート）/ `3cb42f6`（reviewId 中継・告知文・cancel_url）/
`84234d2`（checkout 必須化・所有者検証・エラー文言）

#### 本番実機確認（2026-09-15 18:11〜18:46 JST）

CARTA × 運営者本人・スタッフ「原」で実施。

- 完了画面 URL に `review`、`/rating` へ引き継ぎ、決済キャンセル後も保持。
- **1回目購入**: `rating_purchases.review_id` が**本番で初めて埋まる**（purchase `35999b35…`）。
  `/staff/received` にティア Thank you、**¥ 非表示**。
- **同一感想の2回目（`84234d2` 以前）**: 決済は成立、webhook が 0047 違反を検知して
  **記録せず 200**、運営者 LINE 通知（`pi_3UFsX27…`）。
  Stripe で「重複」として**手動返金**（**手数料 ¥4 は戻らない**）。
- **`84234d2` 後**: 購入済み→`already_purchased`、review なし直打ち・存在しない uuid・
  `manager_only`/rating2 → `review_required`/`invalid_review` の文言、
  正常系は Stripe Checkout まで到達（支払わずキャンセル）。
- **事前検証**: PostgREST の 23505 は **message にのみ制約名が入る**ことを
  使い捨てテーブル `tmp_unique_probe` で実測（作成後削除）。
- **残したデータ**: visit `1f7fcb76…` / review `40ad8de7…` / purchase `35999b35…` /
  VIP 特典発動1件 / 返金済み `pi_3UFsX27…`。
  テスト由来の `audit_log` 行は**削除しない方針**。

#### 未対応・確認事項

> 2026-09-15 に旧リストと統合した（実装前に書いた項目が残っていたため）。
> **解消済み**: 「実装は未着手」→ `66c9063`〜`84234d2` で実装（上記「実装 commit」）／
> 「部分一意制約の衝突確認」→ 0047 適用前に本番で確認（決定4）／
> 「docs 上の『感想だけ送る』導線」→ `4aff2b7` で `20_product.md` を修正。

1. **告知文の法的十分性** — 2026-09-15 に**プライバシーポリシー確定案（第5条4項）とあわせて
   山口先生へ最終チェックを依頼済み・回答待ち**（→ `docs/70_legal.md`）。
2. **スタッフ本人の経路だけを切り分けた確認**（一般スタッフのアカウントで。
   今回は運営者が店長も兼ねるため区別不可）。
3. `route.ts` の `reviewId` 分岐（`cancel_url` / `metadata`）が
   必須化で**常に true** になった箇所の整理。
4. `RatingPicker.tsx` ほか既存 eslint 指摘（リポジトリ全体で9件）。
5. webhook の実到着時間・再送頻度は**未確認**。

---

## 14. 低評価の感想への評価スタンプ（2026-09-16 決定）

### 背景

- §13 は購入条件に `rating>=3` を入れた（`:761-762`）。理由は
  「`manager_only` や `rating<=2` の感想に有料スタンプが付くと
  『課金されたのにスタッフに届かない』が残るため」。
  つまり **購入条件が「本文をスタッフ本人に見せる条件」に相乗りしていた。**
- その土台である「rating<=2 の本文はスタッフ本人に見せない」は、
  コード内コメント3か所（`review-purchase.ts` / `staff/page.tsx` / `staff/received/[reviewId]/page.tsx`）が
  `docs/00_philosophy.md` を典拠として名指ししていたが、
  **2026-09-16 の調査で同ファイルに該当記述が無いことが判明した**（docs 00〜40 のどこにも無かった）。
  決定も理由も、コードコメントにしか存在しない状態だった。

### 決定

1. **rating 1〜2 の感想の本文は、これまでどおり店長のみ閲覧。** スタッフ本人には出さない。
   理由を **`docs/00_philosophy.md` §4.8 に初めて明文化した**（店長が自分のフィルターを通して
   口頭で伝える）。以後、この設計の正は §4.8。
2. **購入条件から rating を外す。** 条件は「本人・当該サロン/スタッフ宛て・
   `share_scope='everyone'`・未購入」。`manager_only` は引き続き購入不可
   （「店長にだけ伝えたい」という選択を課金で覆さない＝§13 の理由がそのまま残る）。
   ⚠️ **`share_scope='everyone'` は §19（2026-09-17）で撤廃した。**以降の正は §19。
3. **rating<=2 の感想に贈られたスタンプは、スタッフ本人に「お客様の表示名＋ティア」で届ける。**
   本文と rating は出さない。
   ⚠️ **このうち「rating を出さない」は §19（2026-09-17）で撤廃した。**以降は
   **購入があれば rating も届ける**（4段階の絵文字は「言葉」ではない）。本文を出さない点は不変。
4. **購入画面の告知文とプライバシーポリシー第5条4項は変更不要**
   （「お名前とスタンプの種類が、担当スタッフとサロンに表示されます。」は今回の変更後も過不足がない）。

### 設計上の要点

- **購入の条件と表示の条件を別モジュールに分けた。**
  `@/lib/review-purchase`（購入・rating を見ない）と
  `@/lib/review-visibility`（表示・`STAFF_BODY_MIN_RATING=3`）は**互いに import しない**。
  §13 では同じ `3` が実質1つの意味を持っていたが、§14 で**別の話**になったため、
  同じ定数を共有すると片方の都合でもう片方が黙って動く。
- スタッフ本人から見た表示は3値（`staffViewMode`）:
  `full`（everyone かつ rating>=3）/ `stamp_only`（everyone かつ rating<=2 かつ購入あり）/ `hidden`。
- **一覧には顧客名・ティアを出さない（§13 決定2 を維持）。**
  「あなたに届いた声」の `stamp_only` 行は「評価スタンプが届きました」＋日付のみ。
  名前とティアは**詳細1か所**に固定したまま（説明箇所を増やさない・§13 決定3 の理由）。
- **Team voices は `rating>=3` のまま。** 本文を出す一覧なので決定1に従う。
- **低評価側は本文を SQL で取ってこない。** 一覧は2クエリに分け、rating<=2 側の select に
  `body` を含めない。「画面で出さない」ではなく「取ってこない」で担保する。
- **404 に畳む性質は不変。** 「低評価で購入なし」は「存在しない reviewId」と同じ 404。
  購入の有無を外から当てられない（§13 と同じくオラクルを作らない）。
- **DB 変更なし**（0047 の部分一意インデックスに rating 条件は無い）。
  **webhook 変更なし**（`metadata.review_id` をそのまま記録しており rating を見ていない）。

### 採らなかった案と理由

| 案 | 採らなかった理由 |
|---|---|
| 一覧にも顧客名＋ティアを出す | §13 決定2（一覧に顧客名・有料ティアを出さない）を崩す。名前の説明箇所を1か所に保つ |
| 低評価の本文も本人に見せる（購入の有無によらず） | `00_philosophy.md` §4.8 に反する |
| 購入条件の `rating>=3` を残したまま表示だけ直す | 低評価の感想には誰も贈れないままで、決定3が成立しない |

### 副作用（意図どおり）

rating 1〜2（普通・改善）を選んだ直後の完了画面にも「評価スタンプを送る」リンクが出る。
購入の入口は完了画面のみなので、これが無いと低評価の感想には誰も贈れない。

### 実装 commit

`617726c`（表示側閾値を `review-visibility.ts` に集約・**挙動不変**）/
`e20178c`（購入条件から rating 撤廃・`PURCHASE_MIN_RATING` 削除）/
`c3a23a7`（詳細画面の3値分岐・購入取得を 404 判定の前へ）/
`167da5c`（「あなたに届いた声」を2クエリ化・`stamp_only` 行）

`npm run lint` の指摘は変更前（`3346f0b`）と**同一**（9 errors / 3 warnings、いずれも既存分）。

### 本番実機確認（**2026-09-17 実施・完了**）

スタッフ本人の経路は `staff.role='staff'` のアカウント（CARTA 歩夢 / Niii 藤原和音）で確認した。
**「運営者は店長を兼ねるため manager 経路に落ちる」という制約（§13 未対応事項2）は、
スタッフ用アカウントを用意して解消した。** お客様役は運営者本人。
チェックインは店内QRを店外から読む形で行った（→ `docs/60_incidents.md`）。

| # | 対照群 | 結果 |
|---|---|---|
| 1 | 高評価 everyone・購入なし（Niii 藤原和音） | **合格** 詳細に本文・その時の気分・タグ。ティア表示なし |
| 2 | 高評価 everyone・購入あり（同） | **合格** 詳細に名前＋Thank you＋本文。¥なし |
| 3 | **低評価 everyone・購入あり**（CARTA 歩夢） | **合格** 一覧は「評価スタンプが届きました」＋日付のみ。詳細は名前＋Thank you＋累計のみ（本文・その時の気分・タグなし）。¥なし |
| 4 | 低評価 everyone・購入なし（同） | **合格** 一覧に出ない。詳細は「この評価は見つかりませんでした」（404） |
| 5 | `manager_only` | **未実施**（§13 で確認済み・§14 で変更していないため） |

あわせて確認した点:

- **完了画面**: rating 1 の感想の直後にも「評価スタンプを送る」が表示された（§14 副作用のとおり）。
- **二重購入**: 購入済みの感想で購入画面を開くと
  「この感想には、すでに評価スタンプをお送りいただいています。」が表示され、決済に進まない。
- **店長 Inbox**: rating 1 の感想が本文つきで読める（`docs/00_philosophy.md` §4.8 のとおり）。
- **入金先**: 各サロンの Stripe アカウントへ Direct Charge で入金
  （PaymentIntent のアカウント部分が CARTA と Niii で異なることを確認）。

#### 残置データ（**削除しない**）

確認用の実決済（¥100 Thank you ×2）は**返金しない**（決定どおり）。

| サロン | review | purchase |
|---|---|---|
| CARTA | `b64be72e-31db-4cc2-870e-8845952ba589`（rating 1・歩夢宛て） | `f1b2d015-72ca-4dfe-b719-43c57ce40a43`（thank_you・`pi_3UGTwS7w7uzzQqfF01UFwfZ…`） |
| Niii | `20ff7475-8989-47b2-8cc7-abdb9ac9b66d`（rating 4・藤原和音宛て） | `7780a979-c463-4723-b416-2116db58c8d5`（thank_you・`pi_3UGU9R97r938WLIb1t6qhtVr`） |

Niii では、お客様役アカウントの感想スタンプが3個に達し **VIP 特典が発動**した
（ご褒美クレンジングSPA10分 ×2）。これも残置する。

### 未対応・確認事項

1. **`manager_only`（対照群5）の本番実機確認は未実施。**
   §13 で確認済み・§14 で購入条件も表示条件も触っていないため、優先度は低い。
2. **購入済みの感想で購入画面を開くと
   「この感想には、すでに評価スタンプをお送りいただいています。」の案内が出るが、
   「¥100 を支払う」ボタンは押せる見た目のまま残る。**
   **2026-09-17 に本番で実際に押して確認**: Stripe の決済画面には進まず、同じ画面にとどまる
   （**課金経路なし**・`rating_purchases` は1件のまま）。**見た目だけの問題。**
   UI の見直し候補（ボタンを無効化するか非表示にする）。
3. **Niii の VIP 特典で、同じ名前の特典が2つ表示された。**
   特典設定の重複登録か表示側の問題か未確認。
4. `rating` が NULL の感想の扱い — 一覧・詳細とも**低評価と同じ**（購入があれば `stamp_only`）に倒している。
   RPC が 1..4 を強制している（0004 / 0020）ため実データには存在しない想定。
5. `RatingPicker.tsx` ほか既存 eslint 指摘（リポジトリ全体で9件）。§13 未対応事項4 と同じ・今回増減なし。

---

## 15. マイページの補助導線（2026-09-16 決定・実装は §14 本番確認後・**2026-09-22 本番確認済み**）

### 背景

LINE の感想リマインドだけでは、**LINE を開かないお客様に届かない。**
LINE はそのまま残し、`/mypage` に補助の導線を置く。

現状 `/mypage` には**感想・評価スタンプへの導線が無い**（2026-09-16 調査）。
外向きリンクは `/onboarding/name`・`/`・`/help` の3つだけ
（`src/app/mypage/page.tsx:200-205`・`:416-424`）。

### 決定

1. **`/mypage` のサロンカードに2つのボタンを置く。**
   LINE リマインドの置き換えではなく**補助**（LINE 側は一切変えない）。
2. **「感想を送る」**: 通知遅延が経過（`notification_outbox.notify_at <= now()`）し、
   感想の受付期間内（`REVIEW_WINDOW_DAYS`）で**未送信**のときに目立たせる。
3. **「評価スタンプを送る」**: 購入可能な感想（`isPurchasableReview`・**§14 適用後**）があり、
   **その感想が受付期間内（`REVIEW_WINDOW_DAYS`）のときだけ**表示。最新の1件の購入画面へ。
   無ければ出さない。補助文字は「感想を送ったあとに、評価スタンプを贈れます」程度。
4. **ボタンで促す期間は感想の受付期間（3日）と揃える。**
   **購入そのものに期限は設けない**（期間外でも既存の導線から買える状態は変えない）。
5. **LINE 経由かマイページ経由かの区別はしない**（DB 変更しない）。
6. **（2026-09-20 追加・実装は未着手）「評価スタンプを送る」も `.btn-mint` にする。**
   **ただし同じサロンカードに「感想を送る」が同時に出るときは「感想を送る」を優先して
   `.btn-mint` にし、「評価スタンプを送る」は控えめな表示（`.btn-quiet`）のままにする。**
   ＝**1枚のカードに `.btn-mint` は最大1つ。**
   理由: **感想を送ることが先にあるべき行動**だから（決定2 の「目立たせる条件であって
   出す条件ではない」と同じ考え方を、2つのボタンの間にも適用する）。
   **未対応6（補助文字が状態と合っていない）も同時に直す**（差し替え文言は未決）。

### 理由

- **期間を3日に揃える**: echo は来店体験への感謝をすぐ届ける仕組みで、
  古い感想への購入の促しは**催促に近くなる**。
  評価スタンプはチップではなく評価の対価という位置づけ（原則5・§13 決定3）。
  **贈る自由は残し、促すのは3日だけにする。**
- **流入元を区別しない**: 定着・ROI 等は**結果であって目的ではない**
  （`00_philosophy.md` §3「サロン数・MRR は結果指標であって目標指標ではない」）。
  効果測定のための migration は単独開発の時間に見合わない。

### 採らなかった案と理由

| 案 | 採らなかった理由 |
|---|---|
| ボタンを期限なしで出す | 古い感想への購入の**催促**になる |
| 流入元の列を追加（`reviews` / `rating_purchases`） | 効果測定が目的化する。migration コスト |
| 「評価スタンプを送る」を感想から切り離す（スタッフを選んで直接購入） | `00_philosophy.md` と §13 決定3「**言葉のない送金を防ぐ**」に反する |

### 実装メモ（2026-09-16 調査より・**2026-09-17 実装済み**）

- **新規1**（例: `src/lib/` に `getMypageActionsMap`）＋ `mypage/page.tsx` の**第2波**
  （`:160-172`）への追加＋スタイル。**DB 変更なし。**
- `hasReviewedForLatestVisit`（`src/lib/review-server.ts:49-79`）は
  **最終来店日を返さない**（`:65` で取得 → `:79` で破棄）・**サロン単位**なので、
  `Map` で一括取得する版が必要（N+1 回避。既存の `getSalonRewardsMap` /
  `getConsumableRewardStatesMap` と同じ作法）。
- 同関数は「受付期間内に来店なし」でも `false` を返す（`:70`）ため、
  **「来店あり」の判定を別に持つ必要がある。**
- LINE 側の判定 `hasReviewAndPurchase`（`src/app/api/cron/line-push/route.ts:334-361`）とは
  **「送るべきか」の定義が違うため共有しない**
  （向こうは「感想**と**購入の**両方**がその来店日にあるとき送らない」）。
  **共有するのは `REVIEW_WINDOW_DAYS`（`src/lib/review.ts:17`）と `notify_at` だけ。**
- 「購入可能な感想の最新1件」を引く既存関数は**無い**
  （`loadReviewForPurchase` は review_id 前提）。新設する場合も条件をクエリに書き写すだけにせず、
  **最後に `isPurchasableReview` を通す**こと（`src/lib/review-purchase.ts:4-10` の単一ソース原則）。

### 実装（2026-09-17）

**判定は `getMypageActionsMap`（`src/lib/review-server.ts`）に閉じた。**

- **置き場所を新規ファイルにしなかった理由**: JST ヘルパー（`jstToday` /
  `jstDateMinusDays` / `jstDayStartISO`）は `review-server.ts` の非 export 関数で、
  別ファイルに置くと **export を増やすか書き写すか**になり、受付期間の境界が2実装に分かれる。
- **クエリは4本で固定**（サロン数によらない・`getSalonRewardsMap` と同じ `.in()` + `Map` 返し）:
  `visits` / `notification_outbox` / `reviews` / `rating_purchases`。
  **購入は必ず感想より後に起きる**（購入 `created_at` >= 対象の感想 `created_at` >= 窓の開始）ので、
  購入も感想と同じ窓で絞れば取りこぼさない＝**4本を並列にできる**。
- **`body` も `rating` も取らない**（`loadReviewForPurchase` と同じ方針）。
  取ってこなければ、この場で本文を出したり閾値を書き足したりする余地も生まれない。
- **購入可否は `isPurchasableReview` を最後に必ず通す。** クエリに条件を書き写すと、
  条件がクエリと純粋関数の2か所に分かれる（`review-purchase.ts:4-10` の単一ソース原則）。
- **`canReview` の2段判定は `hasReviewedForLatestVisit` と同じ手順**
  （受付期間内の最終来店日 → その日の JST 0 時以降に感想があるか）＝ RPC 0046 の
  `already_submitted` と揃う。

**`notify_at` 前と outbox 無しの扱い（重要）**

- **`outbox` は「通知の台帳」であって受付可否の正ではない。**
  行が無い来店（同日2回目・来店軸 OFF・`0014` 適用前の古い来店）はふつうにある。
  その場合は **`reviewDue=false`（目立たせない）に倒すだけ**で、
  **`canReview` は「受付期間内の来店」と「未送信」だけで決める。**
- **`notify_at` より前でもボタンは出す**（`.btn-quiet` で控えめ）。
  決定2 は「目立たせる」条件であって「出す」条件ではない。来店直後に書きたい人の入口を塞がない。
- **`status`（sent / skipped / failed）は見ない。** LINE が届かなかった人ほどこの導線が要る。

**遷移先**（§15 未対応2 をここで確定）

- 「感想を送る」→ **`/review?salon=<id>`**（LINE リマインドと同じ・`line-push/route.ts:203`）。
  **スタッフ指定は入らない。**
- 「評価スタンプを送る」→ **`ratingHref()`（`src/lib/review.ts`）**が組み立てる
  `/rating?salon=&staff=&reviewed=1&review=`。購入画面は **salon / staff / review の3点**を要求する
  （`isPurchasableReview` が staff 宛てを見るため）。`reviewed=1` は決済キャンセルで戻ったときに
  「感想だけ送る」を再表示しないためのフラグ。
  ※ `/review/complete` も同じ形の URL を組み立てているが、**あちらは購入導線の一部**なので
  §15 では触っていない（寄せ替えるなら別途）。

**目立たせ方**（2026-09-17 修正）

- **通知時刻を過ぎた「感想を送る」は `.btn-mint`**＝**`/review/complete` の
  「評価スタンプを送る」と同じ塗りつぶし**（`complete/page.tsx:148`）にそろえた。
  理由: お客様が**すでに学習している「次にやること」の形**を再利用する。
  **新しい色もクラスも作らない。**
  - 初版は「ミントは好調/上昇の差し色（`30_design.md` §2）なので促しには使わない」として
    `.btn-outline` にしていたが、**購入導線の主ボタンと見た目を分ける利点が無い**ため改めた。
- **通知時刻より前は `.btn-quiet`**（控えめに出すだけ。**出す/出さないは来店と未送信で決める**）。
- **「評価スタンプを送る」は常に `.btn-quiet`**（**金額を主役にしない**・`00_philosophy.md` §4.6）。
- **赤は使わない**（`30_design.md` §2）。**CSS は追加していない**
  （`.btn-mint` / `.btn-quiet` / `.btn-block` はいずれも既存）。

**受付日数の補助文字**（2026-09-17 追加）

- 「感想を送る」の下に **「ご来店日を含めて N 日間のみ感想をお送りいただけます」** を出す。
  初版は「ご来店**後** N 日間」だったが、**来店当日を含むかが読み取れない**ため
  `ReviewForm` と同じ「ご来店日を含めて」に揃えた（2026-09-17・`4db4694`）。
- **N は `REVIEW_WINDOW_CALENDAR_DAYS`（`src/lib/review.ts` ＝ `REVIEW_WINDOW_DAYS + 1`）
  から組み立てる。数字を書き写さない。**
  判定が `visited_on between today - N and today`（**両端を含む**）なので、
  来店当日を1日目と数えると **暦日では +1** になる。
  例: **9/17 に来店 → 9/17・18・19・20 の 4 日間（最終日 9/20）**。
- **お客様向けの文言は暦日に統一した**（2026-09-17・`42e7ebc`）。
  `ReviewForm` のエラーも「ご来店から **3** 日以内」→
  **「ご来店日を含めて `REVIEW_WINDOW_CALENDAR_DAYS` 日以内」**（4）に変えた。
  `REVIEW_WINDOW_DAYS`（差分）は**判定にだけ使い、文言には出さない**。
  - `src` 全体を grep した結果、**受付期間の日数を文言に使っているのは
    `/mypage` と `ReviewForm` の2か所だけ**で、どちらも
    `REVIEW_WINDOW_CALENDAR_DAYS` 由来になった。
  - **LINE リマインドの本文（`buildVisitReviewText`・`line-messaging.ts:138-144`）に
    日数表記は無い**（確認のみ・変更していない）。

**命名の注意**: `/mypage` の既存 `reviews` / `reviewRows` / `reviewMap` は
**`earned_stamps`（感想スタンプの累計）**で、`reviews` テーブルではない。
今回足したものは `mypageActionsMap` / `actions` と別名にした。

### 実装 commit

`84c101a`（`ratingHref`）/ `03cd43a`（`getMypageActionsMap`）/ `b39d9f1`（`/mypage` の導線）/
`0f5ad92`（塗りつぶしを `/review/complete` にそろえ・受付日数の補助文字）/
`42e7ebc`（`ReviewForm` のエラー文言を暦日に統一）/
`4db4694`（`/mypage` の補助文字も「ご来店日を含めて」に揃え・未対応5 を解消）

`npm run lint` の指摘は変更前と**同一**（9 errors / 3 warnings、いずれも既存分）。
`npm run build` は成功。**DB 変更なし**（§15 決定5・流入元を記録しない）。

### 触っていないもの

`src/app/staff/**`・`src/lib/review-purchase.ts`（**import して使うだけ**）・
`src/lib/review-visibility.ts`・`src/app/api/checkout/**`・`src/app/api/stripe/**`（決済経路）・
**`src/app/api/cron/line-push/**`（LINE リマインドの判定は範囲外）**・
`src/app/review/complete/page.tsx`・`src/app/manager/inbox/**`・`src/app/dashboard/**`。

### 範囲外（今回やらない）

- **LINE リマインドの判定の変更**（`hasReviewAndPurchase` も送信条件も触らない）
- **流入元の記録**（決定5・migration を足さない）
- 購入そのものへの期限追加（決定4・**期間外でも既存の導線から買える状態は変えない**）

### 本番実機確認（**2026-09-17・2026-09-19・2026-09-20・2026-09-22 実施・本番確認済み**）

確認の順序は「**来店→通知時刻前後→感想送信→購入→期間経過**」（未対応1 の手順）。
**日付とサロンは項目ごとに違う**（項目2・4 は実装当日の 2026-09-17、項目1・3 は 2026-09-19、項目5 は 2026-09-20、項目6 は 2026-09-22）。

| # | 確認項目 | 結果 |
|---|---|---|
| 1 | **`notify_at` 前**の「感想を送る」 | **合格**（2026-09-19・CARTA）。**`.btn-mint` にならず控えめな表示**（`.btn-quiet`）で、補助文字は「**ご来店日を含めて4日間のみ**」。決定2 の「目立たせる条件であって出す条件ではない」どおり |
| 2 | **`notify_at` 経過後**の「感想を送る」 | **合格**（2026-09-17・**SELNI**）。`notify_at` を過ぎた状態で `/mypage` を開くと**「感想を送る」が `.btn-mint`** で表示され、補助文字「**ご来店日を含めて4日間のみ感想をお送りいただけます**」が出ていた。項目1（`.btn-quiet`）との対照になっている |
| 3 | **感想送信後** | **合格**（2026-09-19・CARTA）。**`/review/complete` と `/mypage` の両方に「評価スタンプを送る」が出る** |
| 4 | **購入後** | **合格**（2026-09-17・**CARTA および Niii**）。購入済みの感想について、**`/mypage` にも `/review/complete` にも「評価スタンプを送る」が出ていなかった**＝`isPurchasableReview` の「未購入」条件（§13 決定4・1感想1スタンプ）が両経路で効いている |
| 5 | 受付期間内に購入可能な感想が**複数**あるとき、**最新1件にだけ**出る | **合格**（2026-09-20・CARTA）。**9/19 の感想（未購入のまま残置）と 9/20 の感想が両方ある**状態で `/mypage` を開き、「評価スタンプを送る」が**1つだけ**表示された。リンク先は `review=7c6064f8-a16f-4d16-8975-3ca63e0e21e0`（**9/20 分**）で、**9/19 分ではないことを URL で確認**した。あわせて**「感想を送る」が消えている**（1来店1感想・§12 決定）。→ 未対応4 |
| 6 | **受付期間の経過**で「感想を送る」が消える | **合格**（2026-09-22・**suco**・運営者本人の customer アカウント）。suco の **2026-09-12 の来店は感想を送っておらず**、受付期間（**ご来店日を含めて4日＝9/15 まで**）を過ぎている。`/mypage` の suco カードに**「感想を送る」が出ていない**ことを確認した。**対照**として、同じ画面の **CARTA カードには 2026-09-22 の来店分（感想未送信・1日目）の「感想を送る」が `.btn-mint` で出ていた** |

**この確認で見つかったもの**（2026-09-19・項目3）: 「評価スタンプを送る」の補助文字が
状態と合っていない（→ 未対応6）。**判定・購入可否そのものは正しく動いている。**

### 未対応・確認事項

1. ~~本番実機確認は一部のみ。残るは項目6（2026-09-23 予定）~~ → **2026-09-22 に項目6 も合格＝
   項目1〜6 すべて合格**（上表）。
   **訂正（2026-09-22）**: 以前の予定「項目6 は 2026-09-23・9/19 の CARTA 分で確認」は、
   **9/19 の CARTA はその日に感想を送っていた**（項目3・項目5 で使用）ため、**確認に使えない予定だった**。
   実際には感想未送信のまま受付期間を過ぎた suco の 9/12 来店で確認した。
2. 受付期間の二重管理は未解消（`src/lib/review.ts:17` と RPC 0046 の `v_window_days`）。
   §14 から持ち越し。
3. `notify_at` 前でもボタンを出す挙動を採った。**LINE と同時に出したい**のであれば
   「出さない」に倒せる（`reviewDue` で分岐している場所を1か所変えるだけ）。実運用の声を見て判断する。
4. 「評価スタンプを送る」は**購入可能な最新1件**にだけリンクする。受付期間内に
   購入可能な感想が複数ある（＝別々のスタッフ宛てに送った）場合、**古い方には導線が出ない**。
   既存の `/review/complete` からは引き続き買える（購入そのものに期限は無い・決定4）。
   **2026-09-20 の本番実機確認（項目5・CARTA）で、この挙動を実機で確認した**
   （9/19 分が残っている状態で 9/20 分にだけ導線が出る）。**仕様どおりだが、
   「古い方に出ない」ことの妥当性そのものは未対応のまま**（実運用の声を見て判断する）。
5. ~~受付期間の数字が画面上で2つある~~ → **解消**（2026-09-17・`42e7ebc` / `4db4694`）。
   お客様向けは**暦日に統一**し、数え方の言い回しも「**ご来店日を含めて**」に揃えた。
   `/mypage`「ご来店日を含めて4日間のみ感想をお送りいただけます」／
   `ReviewForm`「感想はご来店日を含めて4日以内にお送りください。」。
   **`REVIEW_WINDOW_DAYS`（差分）は判定にだけ使い、文言には出さない。**
6. **`/mypage` の「評価スタンプを送る」の補助文字が、今の状態と合っていない。**
   **2026-09-19 の本番実機確認（項目3・CARTA）で発見。**
   文言は「**感想を送ったあとに、評価スタンプを贈れます。**」のまま
   （`src/app/mypage/page.tsx:419-421`）。これは決定3 が「補助文字は…程度」と例示した文言が
   そのまま入ったもの。
   **この文は `actions.purchase` が真のときにしか描画されない**（同 `:406`）＝
   **感想を送り終えてボタンが押せる状態でしか出ない**ので、
   「感想を送ったあとに（贈れるようになります）」という**これから先の案内として読める文が、
   すでにその状態になっている画面に出ている**。
   **文言だけの問題で、判定・購入可否は正しく動いている**（項目3 は合格）。
   差し替え先の文言は未決。**コードは変更していない。**
   → **2026-09-20、決定6（`.btn-mint` の優先順位）と同時に直すことにした。**
   どちらも `/mypage` の同じブロック（`mypage/page.tsx:406-421`）なので1回で触る。

---

## 16. 店長 Inbox の可視性トグルの廃止（2026-09-17 決定・実装）

### 背景（2026-09-17 調査）

`/manager/inbox` は各行に **[全員に共有 / 店長控え]** のトグルを持ち、
`reviews.visibility`（`'all' | 'manager'`・migration 0006）を更新していた。
これは `share_scope`（お客様が選ぶ公開範囲）とは**別概念の「店長のキュレーション」**として
設計されたもの（`0006_review_visibility.sql:6-7`）。

調査で分かったこと:

- **`visibility` はスタッフ側のどこからも読まれていなかった。** `grep -rn "visibility" src` の
  ヒットは Inbox 2ファイルと API ルート1本だけ。`/staff`・`/staff/received`・
  `staffViewMode()`・`isPurchasableReview()` はすべて **`share_scope` のみ**で判定する。
- そのため画面の説明文「**「店長控え」にした感想はスタッフ本人の画面には表示されません**」は
  **事実と違っていた**。0006 の設計意図（`:8`「`visibility='manager'` の行は
  スタッフ本人画面には出さない」）が**未実装**のまま残っていた。
- Inbox は `share_scope` を select すらしておらず、**お客様が「店長のみ」を選んだ感想が
  「全員に共有」側に見えていた**（トグルが表していたのは別の列）。
- 上部の件数は `limit(50)` の母集団で `visibility` を数えており、53 件あるサロンで
  「全員に共有 50／店長控え 0」と表示されていた（SQL は everyone 36 / manager_only 17）。
- **本番に `visibility` 列があるかは未確認**（列なしでもコードが `"all"` に倒すため、
  観測値からは判別できない）。docs にも 0006 の適用記録が無い。

### 決定

1. **可視性トグル（店長のキュレーション）を廃止する。**
2. **Inbox には お客様が選んだ公開範囲（`share_scope`）を表示する。店長は変更できない。**
3. **上部の件数も `share_scope` で数える。母集団はサロン全体**（一覧の50件ではない）。
4. **`reviews.visibility` 列は DB から削除しない**（migration を増やさない）。
   コードから読み書きしなくなるだけ。

### 理由

- `visibility` は表示判定にも購入条件にも使われておらず、**画面の説明と実装が食い違っていた**。
- **店長が受け止める仕組みはすでに成り立っている。** 低評価の言葉は店長が受け止め
  （`00_philosophy.md` §4.8）、`manager_only` はお客様自身の選択。
  ここに「店長の判断」という**3つ目の軸**を足すと、§14 で
  `review-purchase` / `review-visibility` に整理した条件が再び散らばる。
- 決定4 の副産物として、**「本番に列があるか未確認」が実装の前提から外れた。**
  列が有っても無くても §16 は成立する。

### 採らなかった案と理由

| 案 | 採らなかった理由 |
|---|---|
| 店長控えを実装してスタッフ側の判定に `visibility` を加える | 判定の軸が増える。**スタッフが声を受け取る機会を店長が絞れる形**になり、感謝の可視化という思想と緊張する（`00_philosophy.md`） |
| `visibility` 列を `drop column` する | migration が増える。読み書きしなければ害が無く、0006 の記録も残したい（決定4） |
| API ルートを 410 Gone で残す | 外部クライアントが無い（呼び出しは Inbox のみだった）。残す利点が無い |
| 件数を `limit(50)` の母集団で数え続ける | 53 件あるサロンで永久に「50」と出る。**件数と実態のズレを設計として固定**してしまう |
| バッジ文言を「全体に公開」にする | **外部公開と誤読される。** `src/lib/review.ts` の `SHARE_SCOPES` 直上のコメントが「`everyone` は外部公開ではない／ラベルに『お店の』を付けて示す」と明記しており、正面から衝突する |

### 設計上の要点

- **文言の正は `@/lib/review` の `SHARE_SCOPES`**（`manager_only`→「店長のみ」/
  `everyone`→「お店のスタッフに」）。**お客様が感想フォームで見た文字列を店長にもそのまま見せる。**
  Inbox 側で言い換えない（語彙が増えると意味がずれる）。
- **既知の2値だけを明示的に扱い、それ以外（`null` / `either`）はバッジを出さない。**
  件数も `neq` で二分せず everyone / manager_only を個別に数える。
  `10_domain.md` は `either` を廃止済みとするが RPC 0046 は今も受理する（**食い違い#1**・
  本番 prosrc 未確認）ため、二分法にすると**未知の値が黙ってどちらかに寄り、画面が食い違いを隠す**。
  合計が全件に届かなければサインになる。
- **バッジは「お店のスタッフに」「店長のみ」の両方とも同じ褪せグレー（`.tag-quiet`）。**
  **位置は常に名前の下の段（`.inbox-scope`）に固定**（2026-09-17 の本番確認後に修正）。
  - 初版は `everyone` をミント・`manager_only` を褪せグレーにしていたが、**お客様の選択に
    優劣をつけない**ため同色に揃えた。ミントは「ブランド＋好調/上昇」の差し色
    （`30_design.md` §2）で、公開範囲はどちらが good でもない。**赤も使わない**（同 §2・§6）。
  - 初版はバッジを上段（`.inbox-meta`）に混ぜていたが、`flex-wrap: wrap` のため
    **名前の長さで横に並んだり下に落ちたりし、時刻の位置も行ごとにずれた**。
    上段を `nowrap` にして名前側を `min-width:0` + ellipsis で縮め、時刻を全行同じ右端に固定。
  - `.tag-quiet` は**新設**した。既存の `.tag-care` は「気づきの声」（低評価）専用で、
    Inbox には低評価の行も並ぶため**意味が混線する**（見た目は同じ・クラス名だけ分けた）。
- **フォールバックを削除した。** `share_scope` は 0001 からある中核列で、
  `/staff` も `@/lib/review-server` もそのまま select している。Inbox だけ
  二段構え（列なしで再取得＋注意ブロック）にする理由が無くなった。
- **スタッフ側のファイルは1行も触っていない**（`/staff`・`/staff/received`・
  `review-visibility.ts`・`review-purchase.ts`）。§16 は Inbox 内に閉じた変更。

### 実装 commit

`603134f`（`.tag-quiet` 追加）/ `f2f72a2`（Inbox の share_scope 表示・件数のサロン全体集計・
トグルとフォールバック削除）/ `be5a681`（`POST /api/manager/visibility` を削除）

本番確認後の見た目の修正:
`87c8c63`（バッジを名前の下の段に固定・時刻を全行で揃える）/
`488d1bb`（バッジを両方 `.tag-quiet` に）

`npm run lint` の指摘は変更前と**同一**（9 errors / 3 warnings、いずれも既存分）。
`npm run build` は成功。

### 本番実機確認（**2026-09-17 実施**）

CARTA・店長アカウントで確認した。

| 確認項目 | 結果 |
|---|---|
| 各行の公開範囲バッジ | **合格** `manager_only`→「店長のみ」/ `everyone`→「お店のスタッフに」。**全行にバッジあり**（＝`null` / `either` の行は無い） |
| トグル | **合格** [全員に共有][店長控え] が消えている |
| 上部の件数 | **合格** 「店長のみ 17」「お店のスタッフに 36」。本番 SQL（`group by share_scope`）と一致・合計 53＝全件 |
| `POST /api/manager/visibility` | **合格** HTTP **404**（curl）。同条件で生きている `/api/manager/visit` は 401 を返すので、404 はルート不在を示す |
| 注意ブロック（visibility 列が未適用です） | **合格** 出ない |
| スタッフ側の回帰 | `/staff`・`/staff/received` は**1行も触っていない**ため対象外（§16 は Inbox 内に閉じた変更） |

**見つかった見た目の問題**（この確認で修正・`87c8c63` / `488d1bb`）:

1. バッジが名前の長さで横に並んだり下に落ちたりし、**時刻の位置も行ごとにずれた**。
2. `everyone` のミントが「そちらが good」と読める。**お客様の選択に優劣をつけない**方針に反する。

`grep -rn "manager/visibility" docs src` の残りは **§16 の記録2行のみ**（`src` は 0 件）。

### 範囲外（今回やらない）

- `limit(50)` のページング・「もっと見る」
- Inbox の各行から `/staff/received/[reviewId]` への導線（現在も**無い**）
  ⚠️ **§20（2026-09-19）決定2 で解消する**（未着手）。以降の正は §20。
- `reviews.visibility` の `drop column`（決定4）
- `.claude/settings.local.json` に残る `/api/manager/visibility` 宛ての古い curl 許可（無害）

### 未対応・確認事項

1. **CARTA 以外のサロン（Niii）での確認は未実施。** CARTA で対象の分岐（`everyone` /
   `manager_only` の両方・件数・404）は出し切っているため、優先度は低い。
2. **本番に `reviews.visibility` 列があるかは未確認。** §16 では読み書きしないので実装の前提には
   ならないが、0006 の適用記録が docs に無い状態は残る。
3. `share_scope` が `null` / `either` の行があるか未確認。**あればバッジが出ず、
   上部の件数の合計が全件に届かない**形で表に出る（意図どおり）。→ 食い違い#1。
4. `docs/archive/phase5b_staff_screens.md:30,72,148` と `docs/archive/handoff.md:59` に
   「全員共有 / 店長控え」の記述が残るが、**archive は正ではない**ので書き換えない。

---

## 17. 店長 Inbox の「もっと見る」と公開範囲フィルタ（2026-09-17 決定・実装）

### 背景

§16 の後も `/manager/inbox` は**新着50件だけを取得し、ページ送りが無かった**。
CARTA は 53 件あり、**古い3件は画面から見られない**（データは DB に残っている）。
上部の件数（サロン全体）は正しい数を出すが、**押しても何も起きない**状態だった。

### 決定

1. **「もっと見る」で古い感想を続けて読み込める。** 1回につき 50 件。
2. **上部の件数ピルを公開範囲フィルタにする。** 「全部（初期表示）/ 店長のみ / お店のスタッフに」の3択。
3. **絞り込み中の「もっと見る」はその公開範囲の中で続きを読む。**
   最後まで読み込んだときの行数が上部の件数と一致する。
4. **お客様の公開範囲は変更できないまま**（§16 を維持）。

### 取得方式 — 「成長する take」

`?take=` を URL に持ち、**毎回先頭から take 件を取り直して全部描き直す**。
「もっと見る」は `take + 50` へのリンク。

| 案 | 挿入が起きたとき | 採否 |
|---|---|---|
| **成長する take（採用）** | 新着が先頭に入り全体が1つ下へずれるが、**毎回描き直すので重複も欠落も起きない**。末尾の1件が窓から溢れても次の「もっと見る」で到達できる | ○ |
| キーセット（`created_at, id` カーソル）で追記 | カーソルが値に固定されるので**構造的に安全**。ただし GET API ルートとクライアント状態が要る | **将来の乗り換え先** |
| offset 追記（`.range(50,99)`） | **壊れる。** 1件の挿入で境界の行が重複し、1件飛ぶ | × |

- **並びは `created_at desc, id desc`。** `created_at` は一意ではない（同一秒の挿入）。
  一意キーまで order に含めないと境界で重複/欠落が起きうる
  （`src/lib/fetch-all-rows.ts:20-21` と同じ規約）。§17 で `id` を足した。
- **乗り換え条件**: 1サロンが **500 件**を超えたらキーセットに切り替える。
  その際は `reviews(salon_id, created_at desc, id desc)` の複合索引が要る
  （現在の索引は `idx_reviews_salon`＝`salon_id` 単独・`0001:85`）＝**migration を1本足す**。
  **今回は追加しない**（53 件では不要）。
- **上限 1000 で丸める。** PostgREST は `max-rows`（既定 1000）を超えると
  **エラーにならず静かに切り捨てる**（`fetch-all-rows.ts:3-5`）。その手前で止める。

### 1回の読み込み件数 — 50 のまま

変える根拠が無い（現行 50 で遅いという観測が無い）。定数は `INBOX_PAGE_SIZE` 1つに閉じ、
クエリ・「もっと見る」の増分・表示文言がすべてこれを参照する。

### 状態の持ち方 — フィルタも深さも URL

| 状態 | 持ち方 |
|---|---|
| 公開範囲フィルタ | `?scope=`（`all` 既定 / `manager_only` / `everyone`） |
| 読み込み深さ | `?take=`（既定 50） |

- **リロード**: URL にあるのでそのまま同じ表示に戻る。
- **戻る**: 「もっと見る」1クリック＝1履歴。押した分だけ一段ずつ戻る（`replace` にしない）。
- **フィルタを切り替えたら `take` は 50 に戻す**（フィルタのリンクは take を引き継がない）。
  17 件しかない絞り込みに `take=200` が残らない。
- **不正値は既定に落とす**（`?scope=foo`→`all` / `?take=abc`・負数→50）。エラー画面は出さない。
  take はページサイズの倍数に切り上げる。
- `?salon=` は**引き続き受け取らない**（サロンは `ctx.salon_id` 固定＝越境不可）。
- **絞り込みはサーバー側の `eq`**。件数と行が同じ条件から出るので、決定3 の一致が構造的に保たれる。

### 分類できない値（`null` / `either`）

- **「全部」では表示する。バッジは出さない**（§16 の判定をそのまま使う）。
- **絞り込み中は出ない**（`eq` のどちらにも該当しない）。意図した挙動。
- **件数は3つを独立に数える**（`neq` で二分しない・引き算で出さない）。
  **「全部」が他の2つの合計より多ければ、未知の値が実データにある**サイン → 食い違い#1。

### 設計上の要点

- **クライアント JS を足さない。** フィルタも「もっと見る」も `<Link>` 遷移で、
  ページはサーバーコンポーネントのまま。§16 で減らした面を増やさない（新規 API ルートも作らない）。
- **続きの有無は「その scope の全件数 > 描いた行数」で判定**する。件数は同じ条件の
  exact count なので、`take+1` 件を引いて判定する必要がない。
- **アクティブなピルはミント。** `30_design.md` §2 がミントの許可用途に「アクティブタブ」を
  挙げている。バッジをグレーに統一した §16 とは役割が違う（状態表示 vs 選択中）。
- **スタッフ側のファイルは1行も触っていない**（`/staff`・`/staff/received`・
  `review-visibility.ts`・`review-purchase.ts`・`review-server.ts`）。§17 は Inbox の
  **読み取りとナビゲーションだけ**の変更で、`share_scope` の意味も書き込み経路も増やさない。
- **DB 変更なし**（migration を足さない）。

### 実装 commit

`cba2dcf`（`?scope=` フィルタ・件数3本・並びに `id` を追加）/
`1aa85fe`（`?take=` と「もっと見る」・上限の丸め・`InboxScope` の型を締める）/
`e0b4a2c`（件数ピルをリンクとして見せる CSS）

`npm run lint` の指摘は変更前と**同一**（9 errors / 3 warnings、いずれも既存分）。
`npm run build` は成功。

### 本番実機確認（**2026-09-17 実施**）

CARTA・店長アカウントで確認した。

| 確認項目 | 結果 |
|---|---|
| 初期表示のピル | **合格** 「全部 53 / 店長のみ 17 / お店のスタッフに 36」 |
| 合計の一致 | **合格** 17 + 36 = 53 ＝ 全件（**未知の `share_scope` は無い**） |
| 「全部」で最後まで | **合格** 「もっと見る」後に「**すべて表示しました（53件）**」。§17 の目的だった**古い3件が読めるようになった** |
| 「店長のみ」 | **合格** 最初から「**すべて表示しました（17件）**」＝17行ちょうど・ボタンが出ない |

**決定3（絞り込み中の行数が上部の件数と一致する）を満たしている。**

### 範囲外（今回やらない）

- Inbox の行から `/staff/received/[reviewId]` への導線
  ⚠️ **§20（2026-09-19）決定2 で解消する**（未着手）。以降の正は §20。
- 期間での絞り込み／担当スタッフ・rating での絞り込み
- `reviews(salon_id, created_at desc, id desc)` の複合索引（**500 件超が乗り換え条件**）
- `reviews.visibility` の `drop column`（§16 決定4 のまま）

### 未対応・確認事項

1. **「お店のスタッフに」（36件）の読み切りは未確認。** 全部・店長のみで読み切りを確認済みで、
   同じ経路（`?scope=` ＋ `?take=`）なので優先度は低い。
2. **1サロン 500 件超でのキーセット移行は未着手**（現在の最大は CARTA の 53 件）。
3. 「もっと見る」を押すたびに履歴が1つ積まれる。**戻るで一段ずつ戻る**挙動を採ったが、
   深く読み込んだ後に一気に戻りたい場合は不便。実運用の声を見てから判断する。
4. `share_scope` が `null` / `either` の行は**絞り込み中は出ない**。**CARTA には無いことを
   2026-09-17 の本番確認で確かめた**（17+36=53＝全件）。他サロンは未確認。→ 食い違い#1。

---

## 18. ダッシュボードの内訳表示（2026-09-17 決定・実装）

### 背景（2026-09-17 調査）

- **スタッフ別 × rating 別の感想件数が無かった。** `reviews` の select に `rating` 列が
  入っておらず（`dashboard-data.ts`）、**データの段階で取っていなかった**。
- **店舗の感想件数が単独で出ていなかった。** 「評価件数」は**感想＋評価スタンプの合算**で、
  1つの感想にスタンプが付くと **2 件**になる。店長は「評価件数 − ティア別内訳の合計」を
  暗算しないと感想の数が分からなかった。
- **ティア別の売上が無かった**（件数のみ）。
- **HR（月次）タブの中で期間の意味が混ざっていた。** 「※常に直近3ヶ月」と注記した直下で、
  売上カードだけが上部で選んだ集計期間に連動していた。
- **前期間比の「前期間」が画面から読み取れなかった。** 実体は「**直前の・同じ長さの窓**」で、
  先月全体でも先月の同じ日付範囲でもない（例: 9/17 時点なら 8/15〜9/1）。

### 決定

- **A. スタッフ別 × rating 別の感想件数を、件数のまま表示する**（日次タブ・選択期間に連動）。
- **B. 店舗の感想件数を単独の数字として出す**（「評価件数」から暗算させない）。
  **カードを2枚に分けた**（「感想（対象期間）」「評価スタンプ（対象期間）」・
  **2026-09-17 の本番確認後に修正**）。**合計は表示しない。**
  前期間比は**それぞれの数字どうし**で計算する（感想は感想と、スタンプはスタンプと）。
  **理由: 合算の前期間比では、感想とスタンプのどちらが増えたか読めないため。**
  （初版は「評価件数」のラベルを据え置き、下に内訳「感想 N ・ 評価スタンプ M」を
  添える形だった。合計値と合算の前期間比が残るため、2枚に分ける形に改めた。）
- **C. 店舗のティア別売上を表示する。** スタッフ別の ¥ は出さない（原則5 を維持）。
- **D. HR（月次）タブから売上カードを外す**（期間の意味の混在を解消）。
- **E. 前期間比に実際の日付範囲を添える**（例「前期間 8/15〜9/1」）。**計算は変えない。**

### ★件数のまま出し、スコア化しない理由（A の核心）★

rating 別の内訳は**スタッフを比べるための材料ではなく、店長が声の傾向を読むための材料**。
以下を**恒久的にやらない**と決める（`docs/00_philosophy.md` §4.1・4.4・4.5）。

1. **平均点・合計スコアを出さない。** 「平均 3.4」は1次元の点数で、
   **一度設計して廃止した順位ポイントの再来**になる（§4.1）。
2. **件数順に並べ替えない。** 並びは**在籍順**（`staff` を `created_at asc` で取得）のまま。
   順位が生まれた時点で、指名が回りにくい若手が構造的に不利になる（§4.4）。
3. **rating 1〜2 を強調色にしない。** 4段階すべて同じ `.stat-pill`。**赤は使わない**
   （`30_design.md` §2）。目立たせること自体が「絶対値で判定しない」（§4.4）と緊張する。
4. **「改善 0件」を良いことのように見せない。** 0件は減光の中立表示にとどめる。
5. **1..4 以外（null・想定外）はどの段階にも寄せない**（数えない）。

**測ると人はその数字に最適化する**（低評価を避ける＝声を集めなくなる）。
echo が可視化したいのは点数ではなく**届いた声そのもの**（§4.1「数字は飽きるが、生の声は飽きない」）。

### 設計上の要点

- **`ratings` と `reviewRatings` を名前で厳密に分けた。**
  `StaffAgg.ratings` は**有償の評価スタンプ**の件数、`StaffAgg.reviewRatings` は
  **感想の4段階**（`reviews.rating` 1..4）の件数。取り違えると数字がまるごと入れ替わる。
- **ティア別売上は `rating_purchases.amount` の実値合計**（**件数×単価にしない**）。
  価格は改定される（Wonderful ¥500→¥1,000・2026-06-18・`CLAUDE.md` §6）ため、
  現単価を掛けると**改定前を含む期間で過大になる**。台帳の実額が正。
  実値合計なので**内訳の総和は店舗合計¥と構造的に一致**する。
- **感想件数とスタンプ件数は集計層（`salonTotals`）が分けて返し、合算は返さない。**
  表示側で足し引きしない＝数字の出どころを1か所に保つ。当期・前期間の両方を持つので、
  前期間比も同じ種類どうしで比べられる。
- **前期間の範囲は表示専用のラベル**として渡す。`prevStartMs` / `prevEndMs` の計算には触らない。
  窓は `[start, end)` の半開区間なので終端はその日の 0:00 を指す。終端が「いま」なら「現在」と書く。
- **金額は主役にしない**（§4.6）。ティア別売上は件数ピルの下に小さく置き、カードを増やさない。
- **スタッフ別の ¥ は出さない。** `StaffAgg.revenue` は per-staff では 0 のままで、
  `buildAgg` のループには触っていない。`yen()` の呼び出しは**日次タブの店舗合計1か所だけ**になった。

### 採らなかった案と理由

| 案 | 採らなかった理由 |
|---|---|
| rating 別を積み上げ横棒で出す | 棒の長さがスタッフ間で並ぶと比較を誘う（§4.5） |
| 合算値（旧「評価件数」）をカードとして残す | 合計の前期間比は**どちらが増えたか読めない**。2枚に分けた時点で合計を出す理由が無くなった（2026-09-17 修正） |
| ラベルを「活動件数（感想＋評価スタンプ）」に変える | カードを分けたので合算のラベル自体が不要になった |
| ティア別売上を別カードにする | 金額のカードが2枚になり §4.6（金額を主役にしない）に反する |
| HR の売上を直近3ヶ月固定に揃える（D-1） | タブ内の意味は揃うが、**期間セレクタが HR で完全に無意味**になる。「先月の売上」も HR から消える |
| HR に注記だけ足す（D-3） | いちばん安いが**混在が残る**＝読み違えの余地も残る |
| 前期間の定義そのものを「先月同期間」に変える | 範囲外。定義を変えると過去の比較と連続しなくなる。**表示で誤解を消す**方を採った |

### 実装 commit

`af7fc86`（集計層: `rating` 取得・`reviewRatings`・`salonTotals` の内訳・ティア別売上・範囲ラベル）/
`dd00fa1`（A: スタッフ別の4段階ピル＋ガードレール）/
`01a681d`（B・C・E: 評価件数の内訳・ティア別売上・前期間の日付範囲）/
`674da40`（D: HR タブから売上カードを外す）

本番確認後の修正:
`2206bf0`（B: 評価件数カードを「感想」「評価スタンプ」の2枚に分割・合計を廃止・
`.metric-grid-4` で4枚時の折り返しを 1列/2×2/4列 に）

`npm run lint` の指摘は変更前と**同一**（9 errors / 3 warnings、いずれも既存分）。
`npm run build` は成功。**DB 変更なし**（`reviews.rating` は 0004 からある既存列で select に足しただけ）。

### 触っていないもの

`src/app/staff/**`・`src/lib/review-visibility.ts`・`src/lib/review-purchase.ts`・
`src/lib/review-server.ts`（購入条件と表示条件）・`src/app/api/checkout/**`・
`src/app/api/stripe/**`（決済経路）・`src/app/manager/inbox/**`（§16・§17）・
`src/lib/admin-salon-stats.ts`（`/admin` の別画面）。

### 本番実機確認（**2026-09-17 実施・一部**）

CARTA・今月（カード分割後の版）で確認した。

| 確認項目 | 結果 |
|---|---|
| B: 感想とスタンプが**別カード** | **合格** 「感想 40件」「評価スタンプ 7件」が独立して並ぶ。**合計は出ない** |
| B: 前期間比が**それぞれ独立** | **合格** 感想 +900% / スタンプ +133% / 売上 +114% と**3枚がばらばらの値**＝合算していないことが読み取れる |
| E: 前期間の日付範囲 | **合格** 「**前期間 8/15〜9/1**」が表示される。当期と同じ長さの窓になっている |
| C: 店舗合計¥ | **合格** ¥1,500（+114%） |
| A: スタッフ別の rating ピル | **合格**（目視）「感想」「スタンプ」の見出しつき・**在籍順**・**低評価も同色**。ガードレールどおり |

**未確認（この時点では見ていない）**:

- **SQL との件数突き合わせ**（感想 40 / スタンプ 7 が `reviews` / `rating_purchases` と一致するか）
- **C: ティア別売上の表示**（内訳の総和＝店舗合計¥ ¥1,500 になるか）
- **D: HR タブに売上カードが無いこと**

### 範囲外（今回やらない）

- **前期間の定義そのものの変更**（表示を足しただけ）
- **echo flow の定義の変更**／**要ケア判定のロジック**
- 平均点・スコア・ランキング並べ替えの追加（§4.1・§4.5 により**恒久的に範囲外**）
- スタッフ別の ¥ 表示（原則5）
- `/admin` 側の集計

### 未対応・確認事項

1. **本番実機確認は一部のみ**（上表）。**残り3点が未確認**:
   SQL との件数突き合わせ／ティア別売上の表示と総和の一致／HR タブに売上カードが無いこと。
   A・B・E は目視で合格しているため、残りは**数字の裏取りと D の見落とし確認**にあたる。
2. `reviews.rating` が `null` の行は4段階のどこにも数えない。
   RPC が 1..4 を強制しているため実データには無い想定（§14 未対応4 と同じ）。
   **あれば「感想 N件」と4段階の合計が食い違う**形で表に出る。
3. 合計（感想＋評価スタンプ）を見る手段が画面から無くなった。必要になったら再検討する
   （合算は「1つの感想にスタンプが付くと2件」になる延べ数なので、意味のある指標か自体が未確定）。
4. HR タブから ¥ が消えたことで、店長が金額を見るには日次タブへ戻る必要がある。

---

## 19. `manager_only` の感想にも評価スタンプを贈れる（2026-09-17 決定・**2026-09-20 実装／2026-09-22 本番確認済み**）

**上書き対象**: §13「追加決定（2026-09-15）」の購入条件（`:760-771`）／
§14 決定2（`:847-850`）／§14 決定3 のうち **「rating を出さない」部分だけ**（`:851-855`。
本文を出さない点は不変）。**いずれも本文は書き換えず、各所に「§19 で上書き」の注記を1行入れてある。**

### 背景

`isPurchasableReview` の条件5（`share_scope === 'everyone'`）は §13 で入れた。理由は
「`manager_only` の感想に有料スタンプが付くと**課金されたのにスタッフに届かない**が残るため」。
つまり §14 で `rating` を外した後も、**購入条件が「本文をスタッフ本人に見せる条件」に相乗りしたまま**だった。

2026-09-17 の調査で分かったこと:

- **`manager_only` の本文がスタッフ本人に漏れる経路は1つも無い。**
  `/staff` の3クエリ（Team voices `staff/page.tsx:212`・`myHigh` `:237`・`myLow` `:246`）は
  すべて `share_scope` で先に弾いており、`myLow` は **body を select すらしていない**。
  詳細（`staff/received/[reviewId]/page.tsx:107`）は body を取るが `staffViewMode` が
  `hidden` に倒して **404**（`:162-164`）。**スタッフ宛ての LINE 通知は存在しない**
  （push は顧客への来店リマインドと運営者へのアラートの2つだけ）。
- **body が `share_scope` 無しで読めるのは店長経路だけ**（`/manager/inbox:178`・
  `/dashboard` の `dashboard-data.ts:180`。後者は `:207` に「manager 専用ガード済＝全 share_scope 閲覧可」）。
- **`rating_purchases` から `reviews` を join して body を取る箇所は 0 件。**
- **DB 側に `share_scope='everyone'` を前提にした条件は無い。**
  CHECK（`0004:25-28`）と RPC の入力検証（`0046:58-59` ほか）は**書き込み時の値チェックのみ**、
  一意インデックス `0047:37-39` は `review_id` だけ、RLS はポリシー0件、webhook は
  `metadata.review_id` しか見ない。→ **migration 不要。**

つまり **「届かない」を表示側で解けば、購入条件から `share_scope` を外せる。**

### 決定

1. **購入条件から `share_scope` を外す**（`isPurchasableReview` の条件5 を削除）。
   条件は「**本人・当該サロン/スタッフ宛て（`staff_id` 非 null かつ一致）・未購入**」だけになる。
2. **購入があった感想は、`share_scope`・`rating` によらずスタッフ本人に
   「お客様の名前・ティア・rating」を届ける。**
   **本文とタグは、`everyone` かつ `rating>=3` 以外では届けない。**
3. **`review-visibility` は `manager_only` を rating 判定より前に打ち切り、`full` を返しえない形にする。**
   **★`full` は allow-list にする（2026-09-19 追記）★**
   判定は「`manager_only` を除外する」ではなく「**`share_scope === 'everyone'` のときだけ `full`**」と書く。
   それ以外（`manager_only` / `null` / `either` / 未知の値）は**すべて rating を見ずに**
   「**購入あり → `stamp_only` ／ 購入なし → `hidden`**」に落とす。
   ＝ **本文には未知の値を通さず、名前・ティア・rating には通す。**
   理由: 決定1 で購入条件から `share_scope` が消えるため、**`null` / `either` の感想も購入できる**
   （`either` は `10_domain.md` が廃止済みとするが RPC 0046 は今も受理・HANDOFF 食い違い#1）。
   ここを「`everyone` と `manager_only` **以外は hidden**」にすると、
   **購入されたのに届かないが別ルートで復活する**（§13 が条件5 を入れた元の問題）。
   未知の値を「危険な方向（本文）には通さず、安全な方向（名前・ティア・rating）には通す」
   ＝ `InboxList.tsx:41-46`「未知の値を既知の値に寄せない」と同じ向きに倒す。
4. **`myHighQuery`・Team voices の `share_scope='everyone'` 絞り込みは変えない。**
5. **`/staff` 一覧の stamp_only 行は従来どおり日付＋「評価スタンプが届きました」のみ。**
   名前・ティア・rating は**詳細画面だけ**（§13 決定2 を維持）。
6. **commit は4本に分け、購入条件（決定1）を必ず最後にする**（2026-09-19 追加・§6.4）。

   | # | commit | 変更ファイル | 実機で見るもの |
   |---|---|---|---|
   | 1 | 表示判定を allow-list 化（決定3） | `review-visibility.ts` | **回帰のみ。** 確認 7・8・10 と、`everyone`＋rating 1＋購入が従来どおり |
   | 2 | 詳細画面に rating を届ける（決定2 表示側） | `staff/received/[reviewId]/page.tsx` | **確認11。§14 の残置データ（CARTA `b64be72e`）で合格判定が取れる** |
   | 3 | 一覧の母集団を `full` の補集合に | `staff/page.tsx`（`myLowQuery` のみ） | **見た目が変わらないこと＝回帰。** 行数・並びが commit 2 時点と同一・重複行なし |
   | 4 | 購入条件から `share_scope` を外す（決定1） | `review-purchase.ts` ＋ `review-server.ts` | **確認 1〜6。** ここが唯一お客様から見える変化 |

   **★commit 4 は必ず最後★**
   先行させると、**表示側が未実装のまま `manager_only` の購入が成立し、
   課金されたのにスタッフに何も届かない**（§13 が条件5 を入れた元の理由）が**本番で発生する**。

   **commit 1〜3 は `manager_only` の購入がまだ存在しないため、本番の見た目を変えない**
   （＝安全な no-op）。どこで止めても本番は無傷で、1本ずつ戻せる。
   commit 1 と 3 は「`full` の定義」を共有しており**片方だけ間違えると一覧と詳細が食い違う**ので、
   分けたうえで commit 3 の回帰確認を「行数が変わっていないか」に絞れるようにする。

   **各 commit で `npm run lint` が変更前と同数（9 errors / 3 warnings）・`npm run build` 成功も見る。**

   **実装 commit（2026-09-20・決定どおり4本・購入条件が最後）**:
   `b6d8086`（1 表示判定の allow-list 化）/ `726ccbe`（2 詳細画面に rating）/
   `633357d`（3 `myLowQuery` を補集合に）/ `c371da0`（4 購入条件から `share_scope` を外す）。
   4本とも上の lint / build の条件を満たしている。
   commit 1〜3 の時点で確認8・12 を先に見た（下の本番実機確認）。

### 理由

- お客様が「店長のみ」で選んだのは **本文の届け先**であって、**応援を贈らないこと**ではない。
- **rating が届くことで、スタッフが自主的に店長へ助言を求められる。**
  低評価の言葉は店長が受け止める（`00_philosophy.md` §4.8）という設計と矛盾しない。
  むしろ「何かあったらしい」と本人が気づける方が、店長との会話が始まりやすい。
- **4段階の絵文字は「言葉」ではない。** §14 決定3 が rating を伏せたのは
  「低評価の**言葉**を本人に届けない」ためで、絵文字1つはその「言葉」に当たらない。
  → **`everyone` の低評価でも rating を届ける**ように揃える（案 B・非対称を作らない）。

### ★判定順の罠（実装時に最も注意すること）★

現行 `review-visibility.ts:57-62` は3段で、**`:60` を緩めるだけでは `:61` に落ちる**。

```
if (share_scope !== 'everyone') return "hidden";   // :60 ← ここを緩めると
if ((rating ?? 0) >= 3)         return "full";     // :61 ← ここに落ちて本文が出る
return hasPurchase ? "stamp_only" : "hidden";      // :62
```

**`manager_only` かつ `rating 4` の感想の本文がスタッフ本人に出る。**
`manager_only` は **rating を見る前に打ち切り**、`hasPurchase ? "stamp_only" : "hidden"` の
2択しか返さない形にすること。**`full` を返しうる経路に `manager_only` を通さない。**

### モードの整理（案 B・3値のまま）

| モード | 出すもの | 対象 |
|---|---|---|
| `full` | 名前・ティア・**rating**・**タグ**・**本文** | `everyone` かつ `rating>=3` |
| `stamp_only` | 名前・ティア・**rating**（タグと本文は出さない） | 購入あり かつ `full` 以外（`manager_only` 全 rating ／ `everyone` の `rating<=2`・null） |
| `hidden` | なし（404） | 購入なしで `full` 以外・他人宛て・`staff_id` null |

**モードは増やさない。** `stamp_only` の中身に rating を足すだけで足りる（案 B を採った理由の1つ）。

### 実装の範囲（**2026-09-20 実装済み**）

| ファイル | 変更 |
|---|---|
| `src/lib/review-purchase.ts` | 条件5（`:77`）と `PURCHASE_SHARE_SCOPE`（`:29`）を削除。**`PurchasableReviewRow` から `share_scope` 列も落とす**（`:36-42`。§14 で `rating` を落としたのと同じ作法＝この場に条件を書き戻す余地を消す）。JSDoc を §19 に |
| `src/lib/review-server.ts` | `loadReviewForPurchase` の select を5列→4列（`share_scope` を落とす） |
| `src/lib/review-visibility.ts` | 決定3。`manager_only` を rating 判定より前に打ち切る |
| `src/app/staff/received/[reviewId]/page.tsx` | `mood`（rating の絵文字）を `stamp_only` でも算出する（`:196-198`）。**`:256` は案A＝気分・タグ・本文を要素ごとに出し分け、タグと本文は `full` のみ。** 罫線の二重化に注意 |
| `src/app/staff/page.tsx` | `myLowQuery`（`:247-255`）を **`myHighQuery` の補集合**にする（**2026-09-19 修正。「`.eq(share_scope, everyone)` を外す」では不足**・下記★）。**body は select しない**まま（`select("id, created_at")` 不変）。`myHighQuery`・Team voices は**変更しない**（決定4） |
| — | **migration 不要。** `/manager/inbox`・`/dashboard`・`/mypage`・`/api/checkout`・`/rating`・`/review/complete`・webhook・LINE リマインドは**変更不要**（判定を関数に委ねているため追従する） |

**挙動が変わる副作用（意図どおり）**: `manager_only` を選んだ直後の `/review/complete` と
`/mypage` にも「評価スタンプを送る」が出る（§14 で低評価に出るようにしたのと同じ構図）。

#### ★`myLowQuery` は「`manager_only` を足す」ではなく「`full` 以外を全部」と書く（2026-09-19）★

**初版の「`.eq(share_scope, everyone)` を外す」では欠落が出る。**
現在の rating 条件は `.or("rating.lt.3, rating.is.null")` なので、`.eq` を外すだけだと
**`manager_only` ＋ `rating 4` ＋ 購入あり**が

- `myLowQuery` に入らない（`rating.lt.3` も `rating.is.null` も false）
- `myHighQuery` にも入らない（`.eq(share_scope,'everyone')` で弾かれる）
- **どちらの一覧にも出ないのに、詳細画面は `stamp_only` を返す**

＝ **「課金されたのに `/staff` に出ない」が残る**（§19 が潰すはずの当のバグ）うえ、
URL 直打ちでだけ見える行ができて `staff/page.tsx:364-371` の
「`canOpen` は detail の `canView` と**厳密に同条件**」という不変条件も壊れる。

**正しくは `myHighQuery` の述語の補集合にする。**

```
High = share_scope = 'everyone' AND rating >= 3
Low  = NOT(High)
     = share_scope <> 'everyone' OR share_scope IS NULL OR rating < 3 OR rating IS NULL
```

```ts
.or(
  [
    `share_scope.neq.${STAFF_VISIBLE_SHARE_SCOPE}`,
    "share_scope.is.null",
    `rating.lt.${STAFF_BODY_MIN_RATING}`,
    "rating.is.null",
  ].join(","),
)
```

**★`IS NULL` の2項を明示しないと、その行がどちらの一覧にも入らず消える★**
PostgREST の `neq` / `lt` は SQL の `<>` / `<` で、**3値論理により NULL に対して TRUE を返さない**
（`share_scope IS NULL` の行は `share_scope.neq.everyone` で拾えない。rating も同じ）。
PostgREST は TRUE の行しか返さないので、NULL 項を落とすと High からも Low からも漏れる。

**「`manager_only` を足す」ではなく「`full` 以外を全部」と書くことで、排他かつ網羅が
構造的に保証される**（`manager_only` が `.eq(everyone)` で弾かれるから**偶然**排他、ではなく、
**述語が互いに補集合だから**排他になる）。決定3 の allow-list と同じ考え方を一覧側にも適用する。

### 排他性の確認（実装時）

`myHighQuery`（`everyone` かつ `rating>=3`）と新しい `myLowQuery` が**同じ行を二重に拾わない**こと。
上の補集合の形にすれば**どの行もちょうど一方にだけ入る**（排他かつ網羅）が、実機で確認する。

**二重取得したときの見え方**（実機で気づくために）:
`staff/page.tsx:349-362` は High（`mode:"full"`）と Low（`mode:"stamp_only"`）を連結 →
`created_at` 降順 → `slice(0,5)`。同じ行が両方に入ると

- **同じ日付の行が2つ隣り合う**（`created_at` が同一なので必ず隣接）。
  一方は本文つき、もう一方は「評価スタンプが届きました」＋日付だけ。リンク先は同じ id。
- **5件枠を2つ消費する**ため、本来5番目の行が押し出される（**目では分からない静かな欠落**）。
- React の `key` は `v.id` なので**重複キー警告**が出るが、`/staff` は**サーバーコンポーネント**のため
  警告は**ブラウザの devtools ではなく `next dev` のターミナル／Vercel の Function ログ**に出る。

### 法務

**変更不要**（§14 決定4 と同じ論法）。購入画面の告知文
「お名前とスタンプの種類が、担当スタッフとサロンに表示されます。」は、`manager_only` を選んだ
お客様にも**過不足なく成立する**（名前とティアしか出ない。rating は「スタンプの種類」に
付随する評価の粒度で、本文＝お客様の言葉ではない）。プライバシーポリシー第5条4項も変更しない。

### 採らなかった案と理由

| 案 | 採らなかった理由 |
|---|---|
| `manager_only` だけ rating を出す（案 A） | **より強く隠した方が多く届く**逆転になる（`manager_only`＋rating1 は届き、`everyone`＋rating1 は届かない）。理由はどちらにも等しく当てはまる |
| `manager_only` も rating を出さない（案 C） | §14 と揃うが、決定2 の核心（スタッフが店長へ助言を求められる）が失われる |
| モードを4値に増やす（`stamp_rating` 新設） | 案 B なら3値のままで足りる。モードが増えるほど判定順の罠が増える |
| `share_scope` を画面に配って分岐する | 判定が `review-visibility` と画面の2か所に散る（§14 で単一ソース化した意味が消える） |
| 条件5 だけ外して表示側を触らない | **「課金されたのにスタッフに届かない」が再現する**（§13 が条件5 を入れた元の理由） |

### 本番確認手順（実装後）

| # | 条件 | 期待 |
|---|---|---|
| 1 | `manager_only`＋rating 4 で感想を送る | `/review/complete` に**「評価スタンプを送る」が出る**（従来は出なかった） |
| 2 | `/mypage` | 同じ感想に対して「評価スタンプを送る」が出る |
| 3 | 購入する | 決済が通り `rating_purchases` に1件 |
| 4 | スタッフ本人の `/staff` | 日付＋「評価スタンプが届きました」の行のみ（名前・ティア・rating は出ない・決定5） |
| 5 | その行から詳細へ | **名前＋ティア＋気分（最高）**。**本文・タグは出ない** |
| 6 | `manager_only`＋rating 1 で同じ手順 | 詳細で**気分（改善）まで出て、本文は出ない**（決定2 の核心） |
| 7 | `manager_only`＋購入なし | 一覧に出ない・詳細は **404** ⚠️ **確認3（購入）の前に差し込む**（2026-09-19 修正・下記「実行順」） |
| 8 | Team voices | `manager_only` は**出ない**（決定4・staff 経路も manager 経路も） |
| 9 | 店長の `/manager/inbox`・`/dashboard` | 従来どおり本文が読める（変化なし） ⚠️ **§20（2026-09-19）実装後は「変化なし」ではない**（ティアバッジ・詳細への導線・顧客名が増える）。**§19 の確認は §20 より先に行う**か、§20 実装後なら「本文の読み取りが変わらないこと」だけを見る |
| 10 | `everyone`＋rating 4 | 従来どおり本文まで（`full`・回帰） |
| 11 | `everyone`＋rating 1＋購入 | **rating（改善）が出る**。本文は出ない（§14 決定3 の rating 部分を上書きした結果） |
| **12** | **排他性**（未対応3） | 同じ日付の行が**2つ並んでいない**こと ＋ `next dev` / Vercel の Function ログに**重複キー警告が無い**こと。**サーバーコンポーネントなのでブラウザの devtools には出ない**（→ 上記「二重取得したときの見え方」） |
| **13** | **`share_scope` の分布**（食い違い#1） | **Niii で `select share_scope, count(*) from reviews where salon_id=… group by 1` を1回流す。** CARTA に `null` / `either` が無いことは §17 で確認済み。あれば決定3 の allow-list のとおり「購入あり → `stamp_only`」になることを1件見る |

#### 実行順（2026-09-19 確定）

**確認7 を確認3（購入）の前に差し込む。** 同じ1行が「**購入なし → 404**」「**購入あり → `stamp_only`**」と
変わるのを観察でき、**別の感想で試すより結論が強くなる**。**感想も1件節約できる**
（そうしないと `manager_only`＋購入なし用に3件目の感想＝3件目の来店が要る）。

**1・2 →(購入前に) 7 → 3 → 4 → 5**、`manager_only`＋rating 1 で **6**。
**8・9・10・11・12 は既存データだけで足りる**ので、commit 1・2 の直後に前倒しできる
（→ 決定6 の commit 表）。

**必要な新規データ**: 来店2・感想2・購入2（Thank you ¥100 ×2 ＝ **¥200**）。
CARTA（`manager_only`＋rating 4・歩夢宛て）と Niii（`manager_only`＋rating 1・藤原和音宛て）に割れば
**1日で通せる**（`unique(customer_id, salon_id, visited_on)` と 1来店1感想のため、
同一サロンで2件目は翌日以降になる）。
スタッフ本人の経路は §14 と同じ `staff.role='staff'` のアカウント（**CARTA 歩夢 / Niii 藤原和音**）を使う。
**SELNI は使わない**（staff アカウントが無く、運営者は店長を兼ねるため staff 経路に落ちない・§13 未対応2）。
実決済は §13・§14 と同じく**返金しない・削除しない**。Niii は **VIP 特典が再発動する可能性**がある。

`npm run lint` が変更前と同数・`npm run build` 成功も各ステップで確認する。

### 本番実機確認（**2026-09-20〜22 実施・本番確認済み**）

**Niii**・スタッフは**藤原和音**（`staff.role='staff'`）・お客様役は運営者本人。
実行順は上の「実行順」どおり（**1・2 →(購入前に) 7 → 3 → 4 → 5**）。

| # | 条件 | 結果 |
|---|---|---|
| 1 | `manager_only`＋rating 4 で感想を送る | **合格**。`/review/complete` に**「評価スタンプを送る」が出た**（**従来は出なかった**＝決定1 が効いている） |
| 2 | `/mypage` | **合格**。Niii のサロンカードにも同じ導線が出た |
| 7 | `manager_only`＋**購入なし** | **合格**。`/staff` の「あなたに届いた声」に**出ない**。**「あなたへ 今週」の件数は 4→5 に増えた**（件数は感想の数で、一覧は購入で絞る＝設計どおり）。Team voices にも出ない |
| 3 | 購入する | **合格**。**Thank you ¥100** で決済が通った |
| 4 | スタッフ本人の `/staff` | **合格**。一覧に**「評価スタンプが届きました 9/20」**が出た。**名前・ティア・rating は一覧に出ていない**（決定5）。評価スタンプの件数は 1→2 |
| 5 | その行から詳細へ | **★合格★**。「評価が届きました」「from 原 朋之様」「**Thank you**」「**その時の気分・最高**」が出て、**本文とタグは出ない**。**決定2・3 の核心を実機で証明した**（`manager_only` でも応援と気分は届き、言葉は届かない） |
| 6 | `manager_only`＋rating 1 | **実施しない**（2026-09-22 判断）。`staffViewMode` は `manager_only` のとき **rating を見ずに `stamp_only` を返す**ため、「`manager_only` かつ低評価」は**確認5**（`manager_only`＋rating 4 で気分まで出て本文は出ない・2026-09-20 合格）と**確認11**（rating 1 でも気分が出る・2026-09-22 合格）で**経路ごとに証明できている**。新規の来店・感想・¥100 の購入と残置データを増やしても、**証明できることがほぼ増えない** |
| 8 | Team voices | **合格**（commit 1〜3 の時点）。`manager_only` は出ない（決定4） |
| 9 | 店長の `/manager/inbox`・`/dashboard` | **合格**（2026-09-20・CARTA・運営者の **manager** アカウント）。`/manager/inbox` は**本文が従来どおり読め**、件数も出た（**全部 64 / 店長のみ 21 / お店のスタッフに 43**）。`/dashboard` も **VIP のお客様・ティア別の内訳・スタッフ別の評価がいずれも従来どおり**で、**金額の出し方も変わっていない**。**§19 は店長画面に影響しないことを実機で確認した**（→ §20 の着手条件が満たされた）。なお **21+43＝64 で全件**＝CARTA には未知の `share_scope` が無いまま（§17 の 17+36＝53 に続く2回目の確認・食い違い#1） |
| 10 | `everyone`＋rating 4 | **合格**。Niii `20ff7475…`（§14 の残置データ・購入あり）が**従来どおり本文・タグ・気分まで**出る（回帰） |
| 11 | `everyone`＋rating 1＋購入 | **合格**（2026-09-22・CARTA・**歩夢の staff アカウント**）。`b64be72e`（`everyone`・rating 1・購入 `f1b2d015`）の詳細で、**名前（from 原 朋之様）・ティア（Thank you）に加えて「その時の気分・改善」が新たに出た**。**本文とタグは出ない**。**§19 commit 2 の効果を既存データで確認した** |
| 12 | 排他性（未対応3） | **合格**（commit 1〜3 の時点）。「あなたに届いた声」に**同じ日付の行が2つ並ばない** |
| 13 | `share_scope` の分布（食い違い#1） | **合格**（2026-09-22）。Niii の `share_scope` は **`everyone` 63件・`manager_only` 11件のみ**で、**`null` / `either` は無い**。CARTA も §17 で2種類のみと確認済み。**食い違い#1（RPC が `either` を受理する）は、実在データには現れていない** |

**残置データ（返金しない・削除しない・§13/§14 と同じ方針）**:
Niii の **`manager_only`＋rating 4 の感想**と、その **Thank you ¥100 の購入1件**。

### 範囲外

- 店長経路（`/manager/inbox`・`/dashboard`）の表示（従来どおり全 `share_scope` の本文を読む）
- `/staff` 一覧に名前・ティアを出すこと（§13 決定2 を維持）
- タグを `full` 以外で出すこと（タグは本文相当の情報とみなす）
- 購入への期限追加（§15 決定4 のまま）

### 未対応・確認事項

1. ~~実装は未着手~~ → **2026-09-20 実装済み**（`b6d8086` / `726ccbe` / `633357d` / `c371da0`。
   決定6 どおり4本・購入条件が最後）。着手条件だった §15 の本番実機確認の項目5 は
   2026-09-20・CARTA で合格していた。
   ~~残るのは本番実機確認の3点（確認6・11・13）~~ → **2026-09-22 に本番確認を完了**（上表）。
   **確認11・13 は合格**（既存データのみで判定）、**確認6 は実施しない**（確認5・11 で経路ごとに
   証明済み・理由は上表）。
   **確認9（店長画面が変わらない）は 2026-09-20 に合格**＝**§20 の着手条件は満たされた。**
2. `review-visibility.ts` の判定順（上の「罠」）を実装時に必ず確認する。
   `manager_only`＋`rating 4`＋購入ありで**本文が出ないこと**を本番確認 6 の前に単体で確かめる。
3. `myHighQuery` と `myLowQuery` の排他性（同じ行を二重に拾わないか）。
   **補集合の形にすれば構造的に保証されるが、実機（確認12）でも見る。**
4. **`LOW_RATING_SCAN = 20`（`staff/page.tsx:87`）の母集団が `manager_only` 全件に広がる**
   （2026-09-19 追加）。これまでは「低評価の中から購入済みを探す走査幅」だったが、
   補集合化により **`full` 以外すべて**が母集団になる。
   **`manager_only` を多く受けているスタッフでは、20件の窓から古い購入済み行がこぼれうる。**
   **消えるのは一覧（「あなたに届いた声」）だけで、詳細画面は URL があれば従来どおり開ける。**
   **CARTA は全53件のため当面は問題にならない。** 定数名も実態と合わなくなる
   （「低評価の走査幅」ではない）。commit 3 の回帰確認で「行数が変わっていないか」を見る理由がこれ。
5. `docs/00_philosophy.md` §4.8 は §19 に合わせて一般化済み（「本人に見せない本文」）。
   実装時にコード内コメント（`review-visibility.ts` / `review-purchase.ts` /
   `staff/page.tsx` / `staff/received/[reviewId]/page.tsx`）の典拠も §19 に揃えること。
6. **`staff/page.tsx:440-442` のコメントが §19 以前の説明のまま**（2026-09-20 追加）。
   「絞り込みは Team voices の staff 経路と同一条件のため…（`manager_only` / `rating<=2` は
   出さない）」と書いてあるが、**§19 以後は `manager_only` でも購入があれば `stamp_only` 行として
   出るため同一ではない。** 挙動は §19 どおりで**コメントだけの食い違い**。
   → **§22 commit 1 で直す**（同じブロックを触るため）。

---

## 20. 店長画面で「誰がどのティアを送ったか」を見られるようにする（2026-09-19 決定・**2026-09-22〜23 実装・本番確認済み**＝commit 4本すべて完了）

**上書き対象**: §13 決定2（`:728-729`）の「一覧には顧客名・有料ティアを出さない」を
**スタッフ画面の一覧に限る**と限定／§16 範囲外（`:1250`）・§17 範囲外（`:1370`）の
「Inbox の行から詳細への導線」を解消／§19 本番確認手順 9（`:1649`）の「店長画面は変化なし」に影響。
**いずれも本文は書き換えず、各所に注記を1行ずつ入れてある。**

### 背景（2026-09-19 調査）

- **§13 決定2 が禁じたのはスタッフ画面の一覧。** 「一覧（あなたに届いた声）と Team voices」と
  書いてあるとおり文脈は `/staff` で、**店長画面の一覧を禁じた記述は docs のどこにも無い**。
- **店長に見せること自体は既に決まっている。** §13 追加決定（`:777-778`）が
  「名前＋ティアは…**同サロンの店長にも表示される**（既存の `canView` 経路）。
  §5『オーナーは全数字を見る』と整合するため許容」と明記している。
- **現状は2つの情報が別の画面に分かれている。**

  | 画面 | 顧客名 | ティア |
  |---|---|---|
  | `/manager/inbox` | **出る**（`page.tsx:179` で `customers(display_name)` を join 済・`InboxList.tsx:75`） | **出ない**（`rating_purchases` の参照が**0件**・`SalonNav.tsx:35`） |
  | `/dashboard` 最近の評価 | **出ない**（`dashboard-data.ts:327`「顧客名は含めない＝原則7」） | 出る（`dashboard-data.ts:328-337`） |
  | `/dashboard` VIP 一覧 | 出る（`dashboard-data.ts:404-408`） | 出ない（付随するのは `earned_stamps.count`＝**無償**スタンプ） |
  | `/staff/received/[reviewId]` | 出る（`:181`） | 出る（`:244-248`） |

  **両方が同時に出るのは最下段の1件詳細だけ。** しかも店長がそこへ到達する画面上の導線は
  `/staff` の Team voices 経由しか無く、それも ①`manager_only` は出ない（`staff/page.tsx:219`）
  ②`limit(5)`（`:275`）③manager のナビに `/staff` へのリンクが無い（`SalonNav.tsx:26-56`）
  の3重で、**実質「導線は無い」**。

### 決定

- **1. `/manager/inbox` の行にティアバッジを足す。**
  `rating_purchases` から `.select("review_id, tier").in("review_id", …)` で**1本引き**、
  `Map<review_id, tier>` を `InboxRow` に足して `ScopeBadge` の隣に描画する。
  **migration 不要・DB 変更なし**（§18 と同じ「既存列を select に足すだけ」の作法）。
- **2. `/manager/inbox` の行から `/staff/received/[reviewId]` へリンクする。**
  §16・§17 が範囲外としていた導線を、ここで解消する。
- **3. `/dashboard` の「最近の評価」に顧客名を足す。**
  行の構成は **時刻・顧客名・スタッフ名・ティア**。
  `dashboard-data.ts:327` の「顧客名は含めない＝原則7」のコメントは、
  **§20 で撤回した旨に書き換える**（§13 追加決定と整合させるため。
  コメントだけ残すと、次に読む人が「出してはいけない」と読んでしまう）。
- **4. 「最近の評価」に5件ずつ遡る「もっと見る」を足す。**
  §17 の Inbox と同じく `created_at, id` の**複合キー**で、**offset は使わない**
  （`created_at` は一意ではない＝ページを跨いだ境界で行が重複/欠落する。
  `fetch-all-rows.ts:20-21` と同じ規約）。

### ★ガードレール（§18 A「スタッフをスコア化しない」を守るため）★

**恒久的にやらない**（`00_philosophy.md` §4.1・4.5、`40_decisions.md` §18 `:1417-1432`）。

1. **スタッフ別のティア集計を Inbox に足さない。** Inbox は「声を1件ずつ受け止める画面」で、
   人を比べる画面ではない。集計が要るなら `/dashboard` にある。
2. **ティア順に並べ替えない。** 並びは `created_at desc` のまま
   （高ティアが上に来た瞬間に「金額順」になる）。
3. **高ティアを強調色にしない。** `InboxList.tsx:47-50` の
   「**両方とも同じ褪せグレー**（`.tag-quiet`）」をそのまま踏襲する。
   ミントは「ブランド＋好調/上昇」の差し色（`30_design.md` §2）で、
   **ティアはどれが good でもない**。赤も使わない。
4. **金額を出さない。** `inbox/page.tsx:302` の
   「金額はこの画面では扱いません（原則5）」を維持する。
   ティアと価格が1対1で公開されている点は §13 決定1（`:726-727`）が
   「**これを承知の上で**」出すと既に判断済みで、同じ論法が店長画面にも成立する。

### 注意（実装時に必ず織り込むこと）

- **過去の購入にはティアが出ない。** §13 決定6（`:747-751`）で
  **過去21件の `review_id` は全件 null のまま遡及しない**と決めているため、
  決定1 を実装しても古い行にはバッジが付かない。
  **「新しいものにしか出ない」ことを HANDOFF に残す**
  （残さないと店長が「壊れている」と誤解する）。
- **§19 実装後は `manager_only` の感想にもティアが付く。** これは**設計どおり**
  （§19 で購入条件から `share_scope` を外した）。Inbox は `manager_only` も表示するので、
  「店長のみ」バッジとティアバッジが同じ行に並ぶ。矛盾ではない。
- ~~**§19 の本番確認 9 と順序が衝突する**~~ → **解消（2026-09-20）**。
  §19 の確認9（店長画面は変化なし）は **2026-09-20・CARTA で合格**したので、
  **§20 はいつ実装してもよい**（§20 実装後の店長画面の変化は §19 の確認結果を汚さない）。

### 実装前の判断（2026-09-22）

1. **決定2 の補足（詳細画面の見出しと戻り先）。**（2026-09-22 改訂。当初の「閲覧者が店長のときだけ変える」を置き換え）
   - **見出しは「閲覧者が店長か」ではなく「その感想が誰宛てか」で決める。**
     - 宛先が閲覧者本人（`review.staff_id === ctx.staff_id`）→ **「あなたへの評価」**（従来どおり）
     - 宛先が他のスタッフ → **「○○さんへの評価」**（○○は担当スタッフ名。引けないときは「担当スタッフさんへの評価」）
     - 宛先がサロン全体（`staff_id` が null）→ **「お店への評価」**
   - **戻るボタンと「ホームへ」の行き先は「どこから来たか」で決める。** Inbox の各行のリンクに `?from=inbox` を付け、
     **閲覧者が manager かつ `from=inbox` のときだけ `/manager/inbox`** に戻す。それ以外は従来どおり `/staff`。
   - **`?from=` は戻る先の表示にだけ使い、権限判定（`staffViewMode`・`mode` の算出）には一切使わない。**
     値は `"inbox"` と完全一致したときだけ有効とし、それ以外は無視する。
   - **理由**: 店長がスタイリストも兼ねている場合（原・さがべぇ）、`/staff` の「あなたに届いた声」から自分宛ての感想を
     開いたときに、見出しが「○○さんへの評価」、戻る先が `/manager/inbox` になっていた（`ec197da` の挙動）。
     **スタッフとして読んでいるときはスタッフの画面のままにする。**
   - **「今週 N」（担当スタッフの今週の件数）は従来どおり残す**（店長は §5 で全数字を見る側のため）。
   - スタッフ本人（`role='staff'`）は `staffViewMode` により**自分宛てしか開けない**（`review-visibility.ts:73-74`）ため、
     見出しは常に「あなたへの評価」・戻り先は常に `/staff` のまま＝**スタッフ本人の表示は変わらない**。
2. **決定4 の take の上限は 100。** ダッシュボードの一覧としては十分で、`.in()` で引く顧客名の数も抑えるため。
   **take は URL（`?take=`）で持つ**。クライアント側で持つと、上限までの顧客名を最初にまとめて
   ブラウザへ送ることになり、**原則7 に反する**。（→ 未対応2 を解消）
3. **決定1 の取得は `.in()` を100件ずつに分けて引く。** 埋め込み（`rating_purchases(tier)`）は採らない。
   埋め込みは**本番に外部キーが張られていることが前提**になるが、**migration ファイルを本番スキーマの
   真実として信用しない**方針（`CLAUDE.md` の教訓・§1）のため、外部キーに依存しない `.in()` を選ぶ。
   （→ 未対応3 の URL 長の懸念はこれで回避する）
4. **「新しい購入にしかティアが出ない」（過去21件の `review_id` が null・§13 決定6）のは Inbox（決定1）だけ。**
   ダッシュボードの「最近の評価」は `rating_purchases` を直接読むので、**過去分にもティアと顧客名が出る**。
5. **commit は4本に分ける**（§6.4・1本ずつ実機で確認してから次へ）:
   **1** ティアバッジ／**2** リンクと店長向けの詳細画面／**3** 顧客名／**4** もっと見る。

### 実装 commit と本番実機確認

| # | commit | 内容 | 本番実機確認 |
|---|---|---|---|
| 1 | `57909bd` | 決定1: Inbox の行にティアバッジ（`.in()` を100件ずつ・`review_id, tier` のみ・`.tag-quiet`） | **合格**（2026-09-22・CARTA・運営者の **manager** アカウント）。`b64be72e` の行（**9/17・歩夢・原 朋之様**）に、公開範囲「お店のスタッフに」の隣へ **「Thank you」が同じグレー（`.tag-quiet`）**で出た。**購入の無い行は公開範囲のバッジだけ**で、**行の高さ・位置もずれない**。**並び順は日付の新しい順のまま**。件数は **67・21・46**（9/20 午後に届いた「お店のスタッフに」の感想3件の分だけ増えた＝正常。**21+46＝67 で全件**） |
| 2 | `ec197da` ＋ `fa85aef`（2b） | 決定2: Inbox の行から詳細へのリンク＋詳細画面の見出しと戻り先。**`ec197da` は「閲覧者が店長か」で分岐していたため、兼任店長が `/staff` から自分宛てを開くと店長向けの表示になった** → **2b（`fa85aef`）で「誰宛てか」「どこから来たか（`?from=inbox`）」に直した**（上の「実装前の判断」1） | **合格**（2026-09-22・CARTA・**原＝運営者本人・兼任の店長**）。①**自分宛てを `/staff` から**（`7c6064f8`）: 見出し「**あなたへの評価**」・URL に `?from=` なし・戻る先 `/staff` ②**他のスタッフ宛てを Inbox から**（`b64be72e`・歩夢宛て）: URL 末尾 `?from=inbox`・見出し「**歩夢さんへの評価**」・戻る先 `/manager/inbox`・**本文とタグも表示**（manager なので `full`）・「今週 0」 ③**サロン全体宛てを Inbox から**（`17d4a8a6`・おおたごし様）: 見出し「**お店への評価**」・戻る先 `/manager/inbox`・**「今週 N」の行は出ない**（担当スタッフがいないため） |
| 3 | `213931b` | 決定3: 「最近の評価」に顧客名（VIP 用の `customers` 取得にまとめて1回で引く・`amount` は持たせない） | **表示は合格・並び順は日付追加後に確認**（2026-09-22・CARTA・**manager** アカウント）。「**時刻・○○様・スタッフ名・ティア**」の順で並び、**ティアの書体（斜体）も変わっていない**。**顧客名は正しく出ている**。ただし**時刻だけで日付が無い**ため、行の順（新しい順）が正しいか実機で判断できなかった（**14:02 → 16:27 → 10:16 → 18:16 → 20:27** と上下しており、別の日が混ざっている）。→ 下の「追加決定」で日付を足す（commit 3b） |
| 3b | `68d57d9` | 追加決定: 「最近の評価」の時刻を「M/D HH:mm」に（書式は `src/lib/jst-format.ts` の `jstMonthDayTime` を Inbox と共有） | **並び順は合格**（2026-09-22・CARTA・**manager** アカウント・**スマホ**）。「最近の評価」が **9/19 14:02 → 9/18 16:27 → 9/17 10:16 → 9/15 18:16 → 9/12 20:27** と新しい順に並んだ。**9/17 10:16 の「原 朋之様・歩夢・Thank you」は `b64be72e` の購入（`f1b2d015`）と一致**（＝commit 3 の「並び順は日付追加後に確認」もこれで確認済み）。**ただしスマホの幅では、顧客名とスタッフ名が「桝 …」「ひ…」「原 …」「吉田…」と1〜2文字で切れて読めなかった**（日付を足したぶん横幅が足りず、名前の列が削られた。「原　朋之」のように**全角空白を含む名前は空白で切れて誰か分からない**）。→ 下の「追加決定」2 でスマホは2段にする（commit 3c） |
| 3c | `7594667` | 追加決定2: 「最近の評価」をスマホの幅（`max-width: 520px`・`.metric-grid`／SalonNav と同じ）では2段に。パソコンの幅は `.recent-who { display: contents }` で従来の1段 | **合格**（2026-09-22・CARTA・**manager** アカウント）。**スマホ**: 1段目「**日付・ティア**」、2段目「**○○様 → スタッフ名**」で、**全角空白を含む名前（原　朋之様・桝　和也様）も切れずに出た**。**5行とも同じ形で区切り線もずれない**。**パソコン**: 従来どおり1段（日付・顧客名・スタッフ名・ティア）で、**矢印は出ない** |
| 4 | `b37c7f4` | 決定4: 「最近の評価」に5件ずつの「もっと見る」（**take 上限 100・URL（`?take=`）で持つ**・深さと URL の組み立ては `src/app/dashboard/recent.ts`・「もっと見る」は `scroll={false}`） | **合格**（2026-09-23・CARTA・**manager** アカウント）。**素の `/dashboard` は5件・クエリなし**。「もっと見る」で **`?take=10` になり10件**、読み切ると「**すべて表示しました（9件）**」。**`?period=last-month` では先月分3件**で「すべて表示しました（3件）」。**期間を切り替えると take が落ちて5件に戻る**。**`?period=custom&from=2026-04&to=2026-09` でも5件＋「もっと見る」**が出る。**スマホの2段表示と並び順は不変** |

**§20 は完了（2026-09-23）。** 決定1〜4 と追加決定1・2 のすべてが実装・本番確認とも済み、**残りの実装は無い**。migration は最後まで不要だった（既存列のみ）。

### 追加決定（2026-09-22）

- **「最近の評価」の時刻を、Inbox と同じ「M/D HH:mm」（例: `9/20 14:02`・JST）にする。**
  理由: 時刻だけでは別の日の評価が混ざったときに**並び順が読み取れず**、決定4 の「もっと見る」でさかのぼると**何日の評価か分からなくなる**ため。
  （commit 3 の本番確認で、時刻が 14:02 → 16:27 → 10:16 → 18:16 → 20:27 と上下して並び順を判断できなかったことが発端）
- **2.「最近の評価」はスマホの幅では2段にする。** 1段目に「**日付・ティア**」、2段目に「**○○様 → スタッフ名**」。
  **パソコンの幅では今の1段（日付・顧客名・スタッフ名・ティア）のまま。**
  理由: 店長がレジ横でスマホで見る画面で、**顧客名が読めなければ決定3 の意味がない**ため。
  （commit 3b の本番確認で、スマホの幅では名前が1〜2文字で切れて読めなかったことが発端）

### 採らなかった案と理由

| 案 | 採らなかった理由 |
|---|---|
| 購入一覧を新設（`/manager/purchases`） | **購入だけを並べた画面は実質「売上ランキングの素材」**になる（§4.6 金額を主役にしない）。Inbox は既に顧客名を join 済みで、足りないのは tier 1列だけ |
| Inbox へのリンク追加（決定2）だけにする | 最小だが**一覧としては解決しない**（1件ずつ開く必要が残る） |
| `/dashboard` のティア別内訳に顧客名を足す | 内訳は**店舗全体の集計**（`tierBreakdown`）で、顧客名を持つ粒度ではない。持たせると集計と明細が1つの表に混ざる |
| 顧客名を出さずティアだけ Inbox に出す | 「誰から」が分からず、§20 の目的（どの顧客からどのティアか）を満たさない。名前は既に同じ行に出ている |

### 範囲外（今回やらない）

- スタッフ別のティア集計・平均・並べ替え（上のガードレール1・2 により**恒久的に範囲外**）
- Inbox の期間絞り込み／担当スタッフ・rating での絞り込み（§17 範囲外のまま）
- `/staff` 側の一覧に名前・ティアを出すこと（**§13 決定2 を維持**・§19 範囲外のまま）
- 金額の表示（原則5）
- `/dashboard` VIP 一覧へのティア追加（VIP は**無償**スタンプの世界で、有償ティアとは別軸）

### 未対応・確認事項

1. ~~実装は未着手~~ → **完了（2026-09-22 着手・2026-09-23 完了）**。commit 4本（1／2・2b／3・3b・3c／4）すべて実装・本番確認済み（上の「実装 commit と本番実機確認」）。**★着手条件は満たされた（2026-09-20）。★**
   着手順序は **§19 →（本番確認）→ §20** を想定していたが、**§19 は 2026-09-20 に実装済みで
   確認9（店長画面が変わらない）も合格**したので、**§20 は着手してよい**。
   §19 に残っていた確認6・11・13 も **2026-09-22 に決着**（11・13 合格・6 は実施しない）＝§19 の本番確認は完了。
2. 決定4 の「もっと見る」は `recent` を `dashboard-data.ts` 内で 5 件に切っている
   （`:328-337`）ため、**集計層の返り値の形から変える**必要がある。
   Inbox（`?take=`）と同じく URL に深さを持たせるか、この画面だけクライアント state にするかは未決。
   → **解消（2026-09-22）: URL（`?take=`）で持つ・上限 100**（上の「実装前の判断」2）。
3. 決定1 の `.in("review_id", …)` は Inbox の表示行ぶん（既定50件・最大1000件）で引く。
   **`take` を増やしたときに `.in()` の要素数が PostgREST の URL 長上限に当たらないか未確認**。
   → **2026-09-22: `.in()` を100件ずつに分けて引くことで回避する**（上限の実測はしていない・「実装前の判断」3）。
4. 過去21件（`review_id` null）の扱いを画面で説明するか、注記なしで済ませるかは未決。

---

## 21. `/owner` — オーナーが自社5店舗を見る（2026-09-19 決定・**2026-09-23 実装中＝実装順1・2・3a まで本番確認済み**・§8.1 の具体化）

**§8.1（`:372-479`）の画面設計を実装に落としたもの。** テーブル・可視範囲・招待フロー・
未決定3点（オーナーの認証方法／登録方法／既存データ移行）は **§8.1 が正のまま**。

### 目的

オーナーが「**各店舗にどんな感想が出ているか**」と
「**誰に・どの期間で・どのティアが何個か**」を、**評価の参考として**見る。

賞与の自動算出式ではない（原則6・`00_philosophy.md` §4.7）。

### 決定

- **1. `/owner` は店舗を切り替えて、既存の店長画面と同じものを見る形にする。**
  `/manager/inbox` と `/dashboard` のロジックを **`salonId` 引数版として流用**する
  （`getDashboardData` は既に `salonId` 引数・`dashboard-data.ts:144`）。
  **既存ファイルは壊さない**（§8.1 `:414-416`
  「`getStaffContext()` が単一 `salon_id` を持つ前提を崩すと、`/manager` 配下の
  10箇所以上のスコープ指定すべてに影響し、認可の事故リスクが高い」を維持）。
- **2. 5店舗を横に並べた全スタッフ一覧は作らない。**
  店舗間のランキングを生まないため（§18 A `:1424-1425`
  「順位が生まれた時点で、指名が回りにくい若手が構造的に不利になる」／
  `admin-salon-stats.ts:11-16`「**スタッフ別の内訳は絶対に出さない**…集計しなければ転用もできない」）。
  **スタッフ別は「1店舗ずつ」でのみ見る。**
- **3. 店舗単位の数字の比較は出してよい**（CARTA 47件・Niii 32件 など）。
  スタッフのスコア化には当たらない。`/admin/salons` が既に同じ形
  （`admin/salons/page.tsx:22-24`「スタッフ別の内訳は出さない」／
  `:26-31` 率が低いサロンを警告色で強調しない）。**同じ配色ルールを踏襲する。**
- **4. 閲覧記録（店長がいつ・どの感想を読んだか）は作らない。** 理由3点:
  1. **§4.1「スタッフをスコア化しない」と正面衝突する。** 既読率が数字になると
     「読まずに全部開く」行動を誘発する（§18 `:1431`
     「**測ると人はその数字に最適化する**」）。指標はその瞬間に意味を失い、しかし残る。
  2. **Next.js の prefetch・再描画で「読んでいないのに読んだ記録」が残り、数字が不正確になる。**
     サーバーコンポーネントの render 中に INSERT することになるため、構造的に避けられない。
  3. **従業員の行動記録は労務領域**で、山口先生の専門外（`70_legal.md` §4 範囲外
     「労働基準法…山口先生には確認できていない」）。
  - **代替**: オーナーが知りたい「店長が向き合っているか」は、
    **既にある感想率・購入率を店舗別に出すことで見る（結果で見る）**。
    算出は `admin-salon-stats.ts:130-134, 324-326`（`rate()`）に既にある。

### 設計上の要点

- **再利用できるのは `.in("salon_id", ids)` → `Map` の作法。** 既存4本:
  `getSalonRewardsMap`（`rewards.ts:81`）／`getConsumableRewardStatesMap`（`同:124`）／
  `getMypageActionsMap`（`review-server.ts:159`・「**クエリ本数はサロン数によらず4本で固定**」）／
  **`getAdminSalonStats`（`admin-salon-stats.ts:140`・「クエリ本数は固定7本」）**。
  最後のものが `/owner` の直接の下敷きになる。
- **`fetchAllRows`（`fetch-all-rows.ts`）・`computeVipProgress`（`vip.ts`）・
  `RATING_TIERS`/`getTier`（`rating-tiers.ts`）・`resolvePeriod`（`dashboard/period.ts`）は
  salon 非依存**なのでそのまま使える。
- **`DashboardClient` 以下の描画コンポーネントは無変更で再利用できる**（`data` を props で受けるだけ）。
- ⚠️ **`getDashboardData` を複数サロンに「広げて」はいけない。**
  `staffNames` を**名前文字列でキーにしている**（`dashboard-data.ts:216-226, 232-274`）ため、
  **別店舗に同名スタッフがいると集計が合流して壊れる**。決定1 のとおり
  「1サロンぶんを何度も呼ぶ」形にすれば、この罠を踏まない。
- **オーナーの認可は `staff.role` で判定しない。** `display-role.ts:19-33` の `owner` は
  **DB に存在しない表示専用の値**（「そのサロンで `created_at` 最古の manager」）で、権限差はゼロ。
  §8.1 未決定1 のとおり `organization_members` 由来で解決する。
  `admin-guard.ts` の **fail closed（未設定なら全閉）** の型を踏襲すること。
- **`salon_id` はセッション由来のまま。** `/owner` で「店舗を切り替える」ときも、
  切り替え先が**そのオーナーの `organization_id` 配下であることをサーバー側で検証**する。
  `50_security.md` §1.1「body / query の `salon_id` は絶対に信用しない」は `/owner` でも不変。

### 法務

**プライバシーポリシーに「運営会社が複数店舗を運営する場合、その運営会社が閲覧する」と
読める記述があるかを、2026-09-15 送付の最終チェックの回答が返った時点で確認する。
無ければその1文を足す。**（→ `70_legal.md` §4.1）

**この確認の対象には「顧客名も含む」**（3b で `/owner/[salonId]/dashboard` に
「最近の評価」と「VIP のお客様」の顧客名を出したため。`70_legal.md` §4.1 の
「感想（顧客名を含む）」と同じ範囲）。

**`/owner` の設計・実装は法務待ちにしない。** 確認の結果が「1文を足す」で収まる見込みのため。

**SELNI が将来独立して別会社になる場合**の扱いは、§8.1 `:447-449` の
`organization_id` 付け替えに加えて、**独立前の期間にオーナーが見ていたことの扱い**を別途決める。
**今は4年契約のため先送り**（§8.1 `:385-386`「SELNI は当面（4年程度の想定）carta と同一組織」）。

### 採らなかった案と理由

| 案 | 採らなかった理由 |
|---|---|
| `/manager/inbox`・`/dashboard` 自体を複数サロン対応に広げる | §8.1 `:414-416` の判断に正面から反する。`Inbox` は `take` ページングがサロン横断で成立せず、`getDashboardData` は同名スタッフで壊れる（上の要点） |
| 5店舗の全スタッフを1画面に並べる | 決定2。店舗間ランキングになる |
| 閲覧記録（B案） | 決定4。§4.1 と衝突・prefetch で不正確・労務領域 |
| オーナーにも `/manager/*` 全部を開放する | §8.1 `:412-413`「店舗の設定・スタッフ招待は各店長が自分で行っている。オーナーが全店舗の `/manager` に入る必要はない」 |

### 範囲外（今回やらない）

- 閲覧記録テーブル（決定4）
- 店舗をまたいだスタッフ別の集計・ランキング（決定2）
- `/owner` からの店舗設定・スタッフ招待・特典設定・店頭QR（§8.1 のまま各店長が `/manager` で行う）
- サロンの組織移動画面（§8.1 `:444-445`「機能として作らない」のまま）
- Stripe アカウント共有（§7.1 のまま保留。organizations 導入後に「同一 `organization_id` から継承」と書ける）

### 時期・依存

- **時期**: §8.1 のとおり **2026年10月末目標**（第三者サロンとの契約がその頃から動きうるため）。
- **依存**: **§20 のティア表示は `/owner/inbox` がそのまま継承する。§20 を先に実装する。**
  逆順にすると同じ変更を2画面に入れることになる。

### 2026-09-23 決定（組織の導入・migration 0048 適用済み）

**§8.1 の未決定3点のうち、1（オーナーの認証方法）と 3（既存データの移行）を決めた。**
2（オーナーの登録方法）は**招待の方式が未決のまま**（下記）。

#### 決めたこと

1. **組織の単位は「事業者（契約主体）」。**
   **数字を見る権限を、お金が入る主体に揃えるため。**

2. **オーナー権限は `organization_members` で持ち、`staff.role` とは別体系にする。**
   識別子は **`line_user_id`**。スタッフ判定（`resolveStaffByLineUserId`）・
   運営者判定（`admin-guard.ts`）と**同じ系統に揃えるため**。
   → §8.1 未決定1 の「env と DB の二重管理をどう扱うか」への回答は
   **「置き場は分けたままでよい。引く鍵（`line_user_id`）を揃えることで実害を消す」**。

3. **1人1組織**（`organization_members` の `unique(line_user_id)`）。
   **複数組織の切り替え UI は作らない。** 必要になったら制約を緩める migration を足す。

4. **「オーナー」表示は `organization_members` から導出する。**
   `display-role.ts` の現行判定（そのサロンで `created_at` 最古の manager）では**なくなる**。
   **carta の5店舗のオーナーは原のみ。**
   検証期間中であることは5店舗のスタッフが理解済み。

5. **DEMO サロンとテストサロンは echo Labs 組織に分け、`/owner` の数字に混ぜない。**
   → §8.1 未決定3 の回答。**NULL 許容にはしない**（`salons.org_id` は NOT NULL）。

6. **サロンは必ず「招待コードの発行者（運営者）が指定した組織」に所属する。**
   **組織の自動作成はしない。**
   **既存の `staff` 行を持つ人は、サロン作成の入口で弾く。**
   （現状は入口を通過してしまい、最後の `staff` INSERT が unique 違反で落ちて
   サロンごとロールバックされる＝`salon/new/route.ts:172-191` の既知衝突。→ コミット4 で入口に移す）

7. **`salons.org_id` と `salon_invites.org_id` は `ON DELETE RESTRICT`。**
   **組織の削除でサロンが連鎖削除されるのを DB で止める。**

#### 未決（0049 以降で決める）

**オーナー招待の方式。** 2案あり、**どちらも採っていない**:

| 案 | 内容 |
|---|---|
| **P** | 運営者が**先に組織を作る**（`/admin/organizations` で組織＋オーナーを登録 → §8.1 の記述どおり） |
| **Q** | **招待の消費で組織を作る**（オーナー招待コードを配り、消費時に組織とオーナーを作る） |

0048 にオーナー招待テーブルを入れなかったのはこのため（0048 冒頭に明記）。

#### 実装の順番

| # | 内容 | 状態 |
|---|---|---|
| 1 | **0048**（`organizations` / `organization_members` / `salons.org_id` / `salon_invites.org_id`） | **2026-09-23 適用済み**（`cd825c3`） |
| 2 | オーナー表示の差し替え（`display-role.ts` を `organization_members` 由来にする） | **2026-09-23 完了**（`aa4a6bd`。前準備として `a8cdbc1`） |
| 3 | `/owner` の閲覧 | **着手（3a `1a81c82` まで完了）**。内訳は下の「コミット3 の設計」 |
| 4 | サロン作成処理の**組織指定必須化**と**入口チェック**（既存 staff 行を持つ人を入口で弾く） | **2026-09-23 完了**（4a `0c0cc38` / 4b `f920527` / 4c `1611308`。**4c は本番未検証**・下記） |
| 5 | オーナー招待（方式 P / Q を決めてから） | 未着手 |

#### ~~★運用上の制約（コミット4 を本番に出すまで有効）★~~ → **解除（2026-09-23）**

> ~~**新しいサロン招待コードを発行しない。**
> `salons.org_id` が NOT NULL になったのに対し、`/api/manager/salon/new` の
> `salons` INSERT は `org_id` を渡していない（`route.ts:143-150`）。
> **このままサロンを作ると INSERT が落ちる**（`error=save` でロールバック）。
> 2026-09-23 時点で**未消費かつ期限内の招待コードは0件**なので、今すぐ壊れるものは無い。~~

**解除**: コミット4c（`1611308`）の push をもって解除。
`salons` の INSERT に `org_id`（招待コードの行から読んだ値）が入るようになり、
**組織が指定された正しいコードならサロンを作れる**状態になった。
組織未指定の招待は `invite_no_org` で弾かれる（`salons` の INSERT より前）。

⚠️ ただし **4c は本番未検証**（下記）。初めて実際に作られるのはコミット5 のとき。

#### 列名の記録（§8.1 / §21 本文との差）

§8.1 と §21 の本文は **`organization_id`** と書いているが、0048 で実際に作った列は
**`org_id`**（`salons.org_id` / `salon_invites.org_id` / `organization_members.org_id`）。
**本番の実列名 `org_id` が正。** 本文の `organization_id` は読み替えること。

#### 0048 の内容（適用済み・2026-09-23 13:06）

- `organizations`（`id` / `name` / `created_at`）— 初期2組織:
  **合同会社carta** / **echo Labs（デモ・テスト）**
- `organization_members`（`org_id` / `line_user_id` / `role` / `created_at`）
  `unique(line_user_id)` ＋ `check (role = 'owner')`。`org_id` は `ON DELETE CASCADE`
- `salons.org_id`（NOT NULL・`ON DELETE RESTRICT`）— 振り分けは**店名ではなく id** で指定:
  carta 5件（CARTA / Niii / nun Fukushima / SELNI / suco）／echo Labs 2件（DEMO / テストサロン）
- `salon_invites.org_id`（NULL 可・`ON DELETE RESTRICT`）— 既存3行は遡って埋めない
- carta のオーナーは **`staff` 行から `line_user_id` を select して登録**
  （PII を migration に直書きしない）
- 新テーブルは **RLS 有効・ポリシー0件**（`50_security.md` §1.2・0037 / 0043 と同じ作法）
- ガード3つ（`salons` 7件の一致／オーナー `staff` 行が1件かつ LINE 紐付けあり／
  NOT NULL 化の前に `org_id` の埋め残しなし）。**`raise exception` で中断**（§1.12）
- `scripts/demo-seed.sql` も `org_id` NOT NULL に追従済み

### 2026-09-23 決定（コミット3 `/owner` の設計）

上の「2026-09-23 決定」で組織の骨格を決めたのに続き、`/owner` の**認可の畳み方と URL 構成**を決めた。

#### 決めたこと

1. **オーナーでない人は `redirect("/staff")`。未ログインは LINE ログインへ（`returnTo` 付き）。**
   **404 にはしない。** 理由: **`/owner` はサロン世界の画面**であって echo Labs の運営画面ではない。
   `/admin/*` が 404 で存在を伏せるのは「運営画面がある」こと自体を隠すためで
   （`admin-guard.ts` の 403 オラクル回避）、`/owner` にその要件は無い。
   **リダイレクトだけでは組織名も店舗の存在も漏れない**（中身は1つも描画しない）。
   行き止まりを作らない方を採る＝`/dashboard` が非 manager を `redirect("/staff")` に
   畳んでいるのと同型。

2. **URL 構成**:
   - `/owner` — 店舗一覧（のちに店舗単位の比較・決定3）
   - `/owner/[salonId]/dashboard` ・ `/owner/[salonId]/inbox`

   **`?salon=` のクエリにはしない。** 理由2点:
   - **既定値の問題**。クエリが無いときにどの店舗を出すかを決める必要が生じ、
     「最初の店舗」を既定にすると並び順の変更が表示内容を変えてしまう。
   - **付け忘れで別の店舗を開く事故を避ける**。パスに入れておけば、
     店舗の指定が無い URL はそもそも存在しない（404 になる）。

   なお**パスに入れても `salonId` はクライアント入力**なので、
   「そのオーナーの組織配下か」の検証はサーバー側で必ず行う
   （上の「設計上の要点」・`50_security.md` §1.1 は不変）。

3. **書き込み（スタッフの店舗移動・権限変更）はコミット3 に含めない。**
   §8.1 は `/owner` の機能として挙げているが、実装順3 は「`/owner` の**閲覧**」。
   閲覧を先に本番で確かめてから、書き込みを別コミットで足す。

4. **店舗の並び順は暫定で `salons.created_at` 昇順。**
   **正式な並びはコミット3d（店舗単位の比較）で決める。**
   コード側にも暫定である旨をコメントしてある（`owner-guard.ts`）。

#### 実装の進捗（本番確認済み）

| # | 内容 | commit | 状態 |
|---|---|---|---|
| 1 | 0048（組織・オーナー所属・`salons.org_id`） | `cd825c3` | **適用済み・確認済み** |
| 2a | `/staff` の感想の可視範囲を `ctx.role` に移す | `a8cdbc1` | **本番確認済み** |
| 2 | オーナー表示を `organization_members` 由来に | `aa4a6bd` | **本番確認済み** |
| 3a | `/owner` の認可の骨格＋店舗一覧（数字なし） | `1a81c82` | **本番確認済み** |
| 3b | 店舗切り替え＋`/owner/[salonId]/dashboard`（`requireOwnerSalon` をここで作った） | `cdfae2a` `4e3d8a0` `f533778` | **本番確認済み** |
| 3c | `/owner/[salonId]/inbox` | `507ee5f` | **本番確認済み** |
| 3d-1 | 店舗一覧に今月の数字と前月比（店舗単位の比較） | `5eb99ad` | **本番確認済み** |
| 3d-2 | 業態でまとめる（店舗に「業態」を持たせ、オーナーが `/owner` で選ぶ） | — | **未着手・実装時期は未定** |
| 4a | 入口チェック（staff 行を持つ人を弾く） | `0c0cc38` | **本番確認済み** |
| 4b | 招待コードの発行で組織を指定 | `f920527` | **本番確認済み** |
| 4c | サロン作成で招待コードの組織を必須化 | `1611308` | **実装済み・★本番未検証★** |
| 4d | サロン作成のレート制限 | — | 未着手 |
| 5 | オーナー招待（方式 P / Q を決めてから） | — | 未着手 |

**対照群の確認**（本番でオーナーは原のみのため、他の人の実機で確認した）:
**Niii の東佑美（非オーナーの manager）** と **Niii のスタッフ1名** の実機で、
`/owner` が `/staff` に畳まれることを確認済み。

#### 3b の決定（顧客名と注記・2026-09-23）

- **`/owner/[salonId]/dashboard` は店長画面と同じ情報を出す。顧客名も出す。**
  対象は「最近の評価」（§20 決定3）と「VIP のお客様」の2箇所。
  理由: **オーナーが店長と同じ情報を持つため**。片方だけ伏せると、
  オーナーと店長が同じ画面を見ながら別の事実を見ている状態になる。
  原則7（個人情報は echo 一元管理・サロンは自店データのみ）には反しない
  ＝オーナーが見るのは**自社の店舗のデータだけ**で、`requireOwnerSalon` が
  組織配下であることをサーバー側で検証する。

- **顧客名の注記は画面ごとに出し分ける。**
  「この画面でのみ表示します」という旧文は、表示画面が2つになった時点で
  実態と合わなくなった。**どちらの文も「誰が見られるか」を書く**形に揃える
  （原則7 の説明であって、画面の数を数えるものではない）。

  | 画面 | 文言 |
  |---|---|
  | `/dashboard`（既定） | ※顧客名はレジでの判別補助のために表示しています。見られるのは、この店舗の店長とオーナーだけです（原則7）。 |
  | `/owner/[salonId]/dashboard` | ※顧客名は、この店舗の店長とオーナーだけが見られます（原則7）。 |

  オーナー側から「レジでの判別補助」を外したのは、**オーナーはレジに立たない**ため。
  実装は `DashboardClient` の `customerNameNote` prop（既定＝店長向け・
  `/dashboard/page.tsx` は無変更）。

#### 3b の実装と本番確認（2026-09-23）

| commit | 内容 |
|---|---|
| `cdfae2a` | `requireOwnerSalon` ／ `/owner/[salonId]/dashboard` ／ `/owner` の店舗名リンク化 |
| `4e3d8a0` | `/owner` の店舗一覧を1店舗1行に（`.stack-sm` は gap だけで flex にならない） |
| `f533778` | 顧客名の注記の出し分けと、実態と合わなくなったコード内コメントの修正 |

**本番確認済み（デプロイ後）**:

- **自組織の5店舗が開ける**（CARTA / Niii / nun Fukushima / SELNI / suco）
- **他組織の店舗 ID（DEMO・テストサロン）→ 404**
- **存在しない uuid → 404**
- **uuid として形式不正の値 → 404**

**4つが同じ 404 であること**が要点。403 にすると「そのサロンは存在するが見せない」と
分かってしまい、**他組織の店舗の存在確認に使える**（`admin-guard.ts` の 403 オラクル回避と同根）。

#### 3b で見つかった別の課題（扱いは未定・§21 の実装順には含めない）

**ダッシュボードのスタッフ別に、アーカイブ済みで LINE を一度も紐づけなかった行が並ぶ。**

- **観測（2026-09-23・Niii）**: 「加地杏奈」が**在籍1行＋アーカイブ済み3行**。
  アーカイブ済みの3行はいずれも `bound_at` が null で `invite_token` が残っている
  ＝**招待を発行したが本人が受諾しないまま再発行された行**（推測: 招待リンクの
  再発行のたびに新しい staff 行が増えたもの）。同じ形の行が「藤原和音」にも1行ある。
- **どこに出るか**: `/dashboard` と `/owner/[salonId]/dashboard` の**両方**。
  `dashboard-data.ts` は退職者を集計から外さずグレー表示にする方針（方針①）で、
  `line_user_id` / `bound_at` は見ていないため。
- **実害**: 数字は増えない（その行には感想も評価も紐づかないので全部0件）。
  **読みにくさの問題**。オーナー画面でも同じものが見える。
- **扱いは未定。** 案（**決定ではない**）: 集計対象から「一度も紐づかなかった行」を外す／
  そもそも招待の再発行で新しい行を作らない／既存の空行を片づける。
  **どれも既存の集計・招待フローに触るため、§21 とは別に判断する。**

#### 3c の決定（`/owner/[salonId]/inbox`・2026-09-23）

- **`/manager/inbox` と同じ一覧を出す。**
  顧客名・公開範囲バッジ・ティアバッジ・本文まで同じ（§21 決定1「店舗を切り替えて、
  既存の店長画面と同じものを見る」）。

- **一覧の行を詳細画面（`/staff/received/[reviewId]`）へのリンクにしない。
  オーナー用の詳細画面も作らない。**
  理由: 調査の結果、**詳細にしかない情報は「タグ」と「今週 N」の2つだけ**だった
  （本文は一覧が全文を出している。`.inbox-body` に文字数制限も `line-clamp` も無い）。
  そのために詳細画面をもう1つ作る価値が薄い。
  加えて既存の詳細画面は `getStaffContext()` で認可しており、オーナーが他店舗の感想を
  開くと `ctx.salon_id` が一致せず 404 になる。
  **`/owner` から `/staff/*` へのリンクを出さない**という条件もこれで満たす。

  ⚠️ **「タグを一覧に出すか」は未決**。出すなら `/manager/inbox` にも同じ判断が要る
  （店長画面の行の情報量が変わる）ので、**§21 とは切り離した別の判断**とする。
  **決めていない＝今はどちらの画面にもタグを出していない。**

- **オーナー用のナビ（`OwnerSalonNav`）でその店舗の「数字」と「感想」を行き来する。**
  `SalonNav` は使わない（`/manager/*`・`/staff/*` へリンクするため）。
  出すのは RoleBar ／「数字」「感想」のタブ ／「← 店舗一覧へ」の3つだけ。
  **`/owner/[salonId]/dashboard` のナビもこれに統一した**（3b で置いた自前のナビを置換）。

- **取得処理を `src/app/manager/inbox/inbox-data.ts` に切り出した。**
  `/manager/inbox` と `/owner/[salonId]/inbox` の単一ソース。
  処理・順序・クエリは逐語的に移しただけで変えていない。`salonId` は引数で受け、
  **複数店舗には広げない**。
  **全クエリに `salon_id` の絞り込みがあることを確認済み**（DB を引くのは3箇所＝
  `rating_purchases` の tier 取得・`reviews` の一覧・`reviews` の件数。
  3つとも `.eq("salon_id", salonId)` を通る）。
  渡す値は `/manager` がセッション由来の `ctx.salon_id`、`/owner` が
  `requireOwnerSalon` の照合を通った `salon.id`（**URL の文字列をそのまま渡さない**）。

#### 3c の実装と本番確認（2026-09-23）

| commit | 内容 |
|---|---|
| `507ee5f` | `/owner/[salonId]/inbox` ／ `inbox-data.ts` の切り出し ／ `OwnerSalonNav` ／ `InboxList` の `linkRows` |

**本番確認済み（デプロイ後）**:

- **CARTA と Niii の一覧が、それぞれの店舗の感想だけを出す**（混ざらない）
- **公開範囲のタブで URL が `/owner` 内に留まる**（`/manager/inbox` に飛ばない）
- **形式不正の店舗 ID が 404 になる**
- **既存の `/manager/inbox` は従来どおり、行から詳細が開く**

#### 3d の決定（店舗単位の比較・2026-09-23）

§21 決定3 は「店舗単位の数字の比較は出してよい」とだけ定めていた。**出し方**を決めた。

- **別の枠（比較専用の表やカード）は作らない。`/owner` の店舗一覧の各行に数字を足す。**
  1行＝**店舗名＋今月の数字**で、行全体を押すとその店舗の
  `/owner/[salonId]/dashboard` が開く。画面を増やさず、一覧がそのまま比較になる。

- **出す数字は、各店舗のダッシュボードの「今月」と同じもの。**
  感想件数 ／ 評価スタンプ件数 ／ 評価スタンプ売上（店舗合計）と、**それぞれの前月比**。
  スタッフ別の数字とスタッフ個人の ¥ は出さない（**店舗合計の売上だけ**・原則5）。

- **並び順は数字の多い順にしない。当面は登録順**（`salons.created_at` 昇順）。
  順位を作らないため（§4.5）。前月比の色も**上昇だけミント**で、
  **下降・低い数字に色を付けない**（`/admin/salons` と同じ作法・赤は使わない）。

#### 3d-2 の決定（業態でまとめる・実装時期は未定）

- **店舗に「業態」を持たせ、業態ごとにまとめて表示する方式に決定。**
  業態の例: ヘア / アイ / ネイル / マッサージ など。
- **業態はオーナーが `/owner` で選べるようにする。**
  → これが **`/owner` で初めての書き込み機能**になる（§8.1 が挙げる
  「スタッフの店舗移動・権限変更」とは別の、3つ目の書き込み）。
- **業態ごとの中の並びは登録順**（3d-1 と同じ。数字順にしない）。
- **実装時期は未定**（コミット4・5 の前か後かも決めていない）。
  migration が要るか、業態の値をどう持つか（列か別テーブルか・値の集合を固定するか）も
  **未決**。決めてから着手する。

#### 3d-1 の実装と本番確認（2026-09-23）

| commit | 内容 |
|---|---|
| `5eb99ad` | `/owner` の店舗一覧に今月の数字と前月比 ／ `getSalonHeadline` の切り出し ／ `DeltaPct` の共有化 |

**一覧とダッシュボードの数字を一致させた方法**:

- **`getDashboardData` は複数店舗に広げていない**（§21 設計上の要点。`staffNames` が
  名前文字列キーのため同名スタッフで集計が合流して壊れる）。
- **店舗合計の計算だけを `dashboard-data.ts` から切り出し、`/dashboard` と共有した**
  （`resolvePeriodWindows`＝前期間の定義 ／ `computeSalonTotals`＝数え方）。
  `getDashboardData` も同じ2つを呼ぶように直したので、**定義は構造的に1つしかない**。
- 一覧は `getSalonHeadline` を**店舗ごとに1回**呼ぶ。スタッフ別には一切触れないので、
  同名スタッフの問題が原理的に起きない。
- **1店舗あたり2クエリ**（`reviews` と `rating_purchases`）で、**どちらも `salon_id` で絞り込み済み**。
  クエリ本数は「**店舗数 × 2本**」＝スタッフ数・顧客数に依存しない（carta の5店舗で10本・並列）。
  `select` は `created_at`（と `amount`）のみ＝本文・顧客名・`staff_id` を取らない。
- 渡す `salonId` は `requireOwnerPage()` が返した **`ctx.salons` 由来の DB の値**
  （`/owner` は `params` も `searchParams` も受け取らない）。

**本番確認済み（デプロイ後）**:

- **CARTA の一覧の数字と前月比が、`/dashboard`（今月）と完全に一致**
- **並びが登録順**で、**赤がどこにも無い**
- **既存の `/dashboard` の前期間比の表示が変わっていない**（`DeltaPct` の切り出しによる退行なし）
- **他組織の店舗 ID は 404 のまま**（3b から変わらない）

#### コミット4 の決定（サロン作成の組織指定必須化＋入口チェック・2026-09-23）

- **スタッフ行を持つ人は、サロン作成の入口で弾く。**
  **manager もオーナーも含む。退職済み（`archived_at` あり）の行も含む。**
  退職済みを含めるのは、本番の **`uq_staff_line_user_id` が archived 条件を持たない**
  部分 unique index だから（`60_incidents.md` 2026-09-02 の★訂正★）。
  退職済みの行だけを持つ人も `staff` の INSERT で必ず unique 違反になるので、
  **在籍だけを見る `resolveStaffByLineUserId` では弾けない**。
  → 判定は `hasAnyStaffRow`（`archived_at` で絞らない・取得失敗は fail closed）。

  ★置く位置★ **フォームの読み取り・ロゴのアップロード・`salons` の INSERT より前**。
  以前は `staff` INSERT（`salons` INSERT の後）で落ちており、補償トランザクションが
  失敗すると**店長不在の孤児サロンが残りうる**経路だった。入口で弾けばその経路が消える。

- **文言**（どちらも echo 運営への問い合わせを案内し、内部事情は書かない）:

  | 場面 | 文言 |
  |---|---|
  | すでにスタッフ行がある | このアカウントはすでにスタッフとして登録されているため、新しいサロンを作れません。サロンを追加したい場合は、echo 運営（info@echo-thanks.jp）までご連絡ください。 |
  | 招待コードに組織が未指定 | この招待コードは使用できません。お手数ですが、echo 運営（info@echo-thanks.jp）までご連絡ください。 |

- **招待コードの発行で組織を必須にする**（`/admin/invites`）。
  選択肢は `organizations` を `created_at` 昇順。ラベルに**サロン数**を添える（§8.1）。
  API は **`organizations` に実在するかをサーバー側で確認**してから INSERT し、
  入れるのは DB から引き直した id（フォームの文字列をそのまま使わない）。
  **`salon_invites.org_id` は NOT NULL にしない**＝既存3行（`org_id` が null）の履歴を
  書き換えないため。**使えるかどうかはアプリ側（`checkInviteCode`）で判定する。**

- **サロンの `org_id` は招待コードの行から読んだ値だけを使う。**
  `checkInviteCode` が返した `orgId` を `salons.org_id` に入れる。
  **フォーム・クエリからは絶対に受け取らない。組織の自動作成もしない**（§21 決定6）。
  **招待の消費（`consumeInvite`）の WHERE にも同じ `org_id` を入れる**＝事前確認と消費の間に
  招待の組織が変わっていたら消費に失敗させ、既存の `invite_race` 経路でロールバックする
  （`salons` に入れた組織と招待の組織が食い違ったまま確定させない）。

- **レート制限（4d）は別コミット。** `/api/manager/salon/new` にだけ
  `login_attempts` の絞りが無い件（`50_security.md` §5-5）は、コミット4 に混ぜると
  「弾かれたのは入口チェックか制限か」が切り分けにくいため分けた。

#### コミット4 の実装と本番確認（2026-09-23）

| commit | 内容 | 確認 |
|---|---|---|
| `0c0cc38`（4a） | 入口チェック（`hasAnyStaffRow`） | **本番確認済み** |
| `f920527`（4b） | 招待コードの発行で組織を指定 | **本番確認済み** |
| `1611308`（4c） | サロン作成で招待コードの組織を必須化 | **★本番未検証★**（下記） |

**4a の本番確認**（原のアカウント）:
- `/manager/salon/new` で**フォームが出ず**、上の文言が表示される
- **API に直接送っても `error=already_staff` で止まる**（`salons` の INSERT に到達しない）

**4b の本番確認**（原のアカウント）:
- 組織の選択肢と**サロン数の表示**が出る
- **未選択では発行できない**
- **echo Labs 指定のコード（`ZVGG-0441-R45K`）を発行**でき、一覧に組織名が出る
- **存在しない組織 ID は `error=org` で弾かれ、コードが増えない**

#### ★4c は本番未検証（「実機確認してから push」の例外）★

**原のアカウントでは 4c の経路に到達できない。** 4a の入口チェックで先に止まるため
（原は CARTA の staff 行を持つ）。**staff 行を1つも持たない LINE アカウントでしか
通らない経路**で、それを本番で用意する手段が今は無い。

- **確認したこと**: コードレビュー。`org_id` の出所と経路
  （`salon_invites.org_id` → `checkInviteCode` の `select` → `invite.orgId` →
  `salons.org_id` の INSERT → `consumeInvite` の WHERE）と、
  route が `form.get("org_id")` を**読んでいない**こと。
- **確認していないこと**: 実際にサロンが作られること。作られた行の `org_id`。
- **いつ確認するか**: **コミット5 で新しいオーナーが初めてサロンを作るとき。**

§6.4「一度に一つの変更を実機で確認してから次へ進む」の**例外として記録する**。
例外にした理由は「本番で確認する手段が無い」ことであって、確認を省いたのではない。

##### コミット5 で初めてサロンが作られたときに確認すること

**A. 作成の直後に画面で**

1. 完了画面（`?created=`）に店名と店頭QRが出る
2. `/admin/invites` でそのコードが「登録済」になり、**組織**と**サロン名**が入る
3. `/admin/salons` に新しいサロンが1行増える

**B. SQL で `org_id` の一致を確かめる（本命）**

`/admin/invites` は招待側の `org_id` しか見せないので、`salons` 側との突き合わせは SQL で行う。

```sql
-- 招待コードの組織と、そのコードで作られたサロンの組織が一致しているか
select i.code,
       io.name  as 招待の組織,
       s.name   as サロン名,
       so.name  as サロンの組織,
       (i.org_id = s.org_id) as 一致,
       i.used_at
  from public.salon_invites i
  join public.salons s         on s.id = i.salon_id
  join public.organizations io on io.id = i.org_id
  join public.organizations so on so.id = s.org_id
 order by i.used_at desc nulls last;

-- 組織に属さないサロンが無いこと（NOT NULL なので 0 件が正）
select count(*) as org_id_null from public.salons where org_id is null;

-- 新しいサロンの中身（作成者の staff 行が1件・role=manager か）
select s.name, s.org_id, o.name as 組織,
       (select count(*) from public.staff st where st.salon_id = s.id) as staff数,
       (select count(*) from public.staff st where st.salon_id = s.id and st.role = 'manager') as manager数
  from public.salons s
  join public.organizations o on o.id = s.org_id
 order by s.created_at desc
 limit 3;
```

**「一致」が `true` であること**が確認点。`i.org_id` が null の古い行は join で落ちるので、
出てくるのは 4b 以降に発行したコードだけになる。

**C. 作成したオーナーの見え方**

4. そのオーナーの `/staff`・`/dashboard` に店長として入れる（`role=manager`）
5. **そのオーナーの `/owner`** — `organization_members` に行が無ければ `/staff` へ飛ぶ。
   **作成しただけではオーナーにならない。**
   ⚠️ ここは**コミット5 の設計判断そのもの**（方式 P / Q が未決）なので、
   **期待値を先に決めてから**確認すること。
6. 原の `/owner` — echo Labs 組織のサロンなら**出ない**。carta 指定で作った場合は一覧に増える。

**D. 失敗側（片付けが要らないので、余裕があれば）**

7. **組織未指定の古いコード**（期限切れの1件を「復旧」して使う）→ `error=invite_no_org`。
   **`salons` が増えない**（INSERT より前で弾いている）。

### 未対応・確認事項

1. ~~実装は未着手。前提は §8.1 未決定3点（オーナーの認証方法／登録方法／既存データ移行）の解決。~~
   → **2026-09-23 着手**。§8.1 未決定1（認証方法）・3（既存データ移行）は上の「2026-09-23 決定」で解決。
   **2（登録方法）は招待の方式 P / Q が未決のまま**。実装は順番1（0048）まで完了、**2〜5 が残り**。
2. ~~migration が要る（`organizations` / `salons.organization_id` / `organization_members` /
   `salon_invites.organization_id`）。番号は **0048** が次。~~
   → **2026-09-23 適用済み**（`0048_organizations.sql` / commit `cd825c3`）。**実列名は `org_id`**（上記）。
   RLS は**ポリシーを書かず完全 deny を migration に明記**する形を採った（0037 / 0043 と同じ作法）。
3. **`60_incidents.md` の2件**（`/admin/staff` が全サロンを対象にしている／
   `staff-invite.ts` の `line_taken` ガードが `archived_at` で絞っていない）は、
   どちらも「organizations 導入時に合わせて扱う」とされている。**§21 の実装で回収すること。**
   → **後者は 2026-09-23 の訂正で解消**（`uq_staff_line_user_id` が本番に実在するため
   「退職済み1行＋在籍1行」は DB で作れない＝素通りは起こり得ない。`60_incidents.md` の
   2026-09-02 の項の★訂正★を読むこと）。
   **代わりに「アーカイブ済み staff が `line_user_id` を占有し続ける」が残課題として立った**
   （退職者は別サロンに入れない。hard delete でのみ解放）。
   **これは §21 とは別の未着手課題で、扱う時期は未定。§21 の実装順には含めない。**
4. ~~carta の5店舗のうち nun Fukushima には共用端末の店長アカウント問題がある
   （`70_legal.md` §6・`60_incidents.md`）。`/owner` から見える店舗一覧に出るため、
   実装前に「nun」行の扱いを確定させておく。~~
   → **解決（2026-09-23）**。「nun」行を**アーカイブ**した。
   **顧客側のスタッフ選択（`/review`）から消えたことを実機で確認済み。**
   **完全削除にはしなかった**。理由:
   `reviews` 1件（2026-07-15・rating=1）と `stamp_adjustments` 5件（`created_by`/`updated_by`）が
   この staff 行を参照しており、FK が `on delete set null` のため
   **削除すると「誰の実績か」「誰が入れた補正か」が null になる**（attribution が失われる）。
   また `api/manager/staff/delete` は `reviews` が1件でもあれば **409** で拒否するため、
   そもそも画面から削除できない。**アーカイブは `unarchive` で戻せる**（§4.4）。
   詳細と、アーカイブ後も残るもの（`/dashboard` のグレー表示・Inbox に残るテスト感想・
   `line_user_id` の占有）は `60_incidents.md` の 2026-09-15 の項に記録した。

---

## 22. `/staff` の「あなたに届いた声」を読み切れるようにする（2026-09-20 決定・**同日実装・本番確認済み**）

**関係**: §14 決定3（2本のクエリに分けた理由）と §19（母集団の拡大）を前提にする。
**§19 未対応4（`LOW_RATING_SCAN` の窓）を決定1 で解消する。**
取得方式は §17（Inbox の「もっと見る」）をそのまま踏襲する。

### 背景（2026-09-20 調査）

- 「あなたに届いた声」は **`MY_VOICES_LIMIT = 5`**（`staff/page.tsx:80`）で、**ページングが無い**
  （`?take=` も「もっと見る」も無し。`/manager/inbox` の §17 とはここが違う）。
- その手前に **`LOW_RATING_SCAN = 20`**（`:87`）の走査窓がある。
  `myLowQuery` は「`full` 以外」を 20 件取り、**そのあと JS で購入のある行だけを残す**（`:350-365`）ので、
  **21 件目以降は購入判定にすら載らない。**
- **§19 で `myLowQuery` の母集団が「`full` 以外すべて」に広がった**（`manager_only` の全 rating ＋
  `null` / `either` / 未知の値 ＋ `everyone` の低評価）ため、**この窓に当たりやすくなった**（§19 未対応4）。
- 上限を超えた行は**一覧から消えるだけ**で、**詳細画面（`/staff/received/[reviewId]`）は URL が
  あれば開ける**し、上の件数マトリクス（`countRows`）にも**数えられたまま**。
  ＝「件数は増えたのに一覧に出ない」という静かなズレが起こりうる。

### 決定

1. **取得の順序を入れ替える。**
   現在: 「`full` 以外」を 20 件取る → JS で購入のある行だけ残す。
   変更後: **自分宛ての購入（`rating_purchases` を `staff_id` で引く）を先に取り、
   その `review_id` に対してだけ `myLowQuery` を `.in("id", ids)` で引く。**
   これで `myLowQuery` は**最初から「表示できる行」だけを返す**。
   ★**`LOW_RATING_SCAN` は撤廃する**★（走査幅と表示上限の二重管理が消え、**§19 未対応4 も解消**）。
2. **`myHighQuery` と `myLowQuery` を1本のクエリにまとめない。**
   まとめると **`body` を必ず select することになり**、「**本文を取ってこないことで見せない**」という
   §14・§19 の構造的担保が壊れる（`staff/page.tsx:230-233` の既存コメントのとおり）。**2本構成を維持する。**
3. **「もっと見る」は §17 の Inbox と同じ「成長する take」方式にする。**
   `?take=` を受け取り、**毎回先頭から `take` 件を取り直して全部描き直す**。
   **offset は使わない**（1件の挿入で境界が重複・欠落する）。並びは **`created_at desc, id desc`** の複合キー。
   `parseTake`（不正値は既定・ページサイズの倍数に切り上げ・MAX で丸める）と
   URL の組み立て（既定値のときはクエリを付けない）は **Inbox と同じ作法**（`manager/inbox/page.tsx:91-105`）。
   **ページングするのは `myHighQuery` だけでよい**（`myLowQuery` は決定1 により全件取れるため）。
   **`hasMore` = 描いた行数 < (`myHighQuery` の exact count ＋ low の件数) かつ `take` < MAX。**
4. **`/staff` が受け取る `searchParams` は `take` だけにする。**
   **`?salon=` は受け取らない**（サロンは `ctx.salon_id` 固定・§8 の不変条件）。
5. **Team voices には「もっと見る」を付けない。**
   Team voices は「**チームの声を眺める面**」であって**アーカイブではない**。
   全件を読み切る役割は **`/manager/inbox`（§17）**が持つ。
   深く遡れるようにするほど「**他人の評価を並べて眺める**」面に近づき、
   §18 A・§4.1/§4.5 の「**スタッフをスコア化しない**」と相性が悪い。
   ただし現状の `.limit(5)`（`:295`）は**直書き**で `MY_VOICES_LIMIT` と**偶然同じ値**なので、
   **片方だけ動かす事故を防ぐため定数化だけは行う。**

### 理由

- **「課金されたのに見られない」を潰したのが §19** なら、**「届いたのに読み切れない」を潰すのが §22**。
  自分宛ての受け取り箱は読み切れるべきで、他人の声を眺める面（Team voices）はそうではない（決定5）。
- **決定1 は「窓を広げる」ではなく「窓を要らなくする」。** 走査幅を 20→100 に広げても
  「`take` を増やしたとき何件走査すればよいか」が決まらず、`hasMore` も厳密には作れない。
  購入を先に引けば **low 側の件数はそのスタッフの購入数で上限が決まる**（現状は数件）ので、
  low はページングせず全件取ってよい＝ページングが要るのは high だけになる。
- **マージの正しさ**: low が完全で high は上位 `take` 件を取っているので、
  **真の上位 `take` 件が両者の和集合から漏れることはない。**

### 採らなかった案と理由

| 案 | 採らなかった理由 |
|---|---|
| `LOW_RATING_SCAN` を 20→100 等に拡大 | 症状が薄まるだけ。定数の意味（走査幅か表示上限か）が二重のまま残る |
| low 側をキーセットで「必要数集まるまで」ループ | 歩留まりが悪いと**クエリ本数が可変**（実質 N+1）。決定1 をやれば不要 |
| `reviews ⋈ rating_purchases` のビュー / RPC を作る | **migration が要る**（§6.1 の手動適用が乗る）。得られる結果は決定1 と同じでコストだけ増える |
| 1本のクエリにまとめる | 決定2。`body` を取らざるを得なくなり §14・§19 の担保が壊れる |
| `/staff` 全体をキーセット（カーソル）にする | §17 と同じく**将来の乗り換え先**。GET API ルートとクライアント状態が要る（現在の規模では過剰） |

### 実装の範囲（**2026-09-20 実装済み**）

**変更ファイルは `src/app/staff/page.tsx` の1本だけ。**
新規コンポーネント・新規 API ルート・lib の変更・migration は**いずれも不要**
（「もっと見る」の見た目は Inbox と同じ既存クラス `btn btn-quiet btn-block` で足りる）。

| 対象 | 変更 |
|---|---|
| `:87` `LOW_RATING_SCAN` | **削除**（`:82-87` の JSDoc ごと。決定1） |
| `:265-275` `myLowQuery` | `.in("id", <購入のある review_id>)` を足し、`.limit(LOW_RATING_SCAN)` を**外す**。`.or(…)` の4項（§19）と `select("id, created_at")` は**そのまま** |
| `:350-365` | 購入クエリを**マージ処理の中から `Promise.all`（`:288-298`）へ移す**。`.in("review_id", lowRows…)` ではなく **`.eq("staff_id", ctx.staff_id)`** で引く。`getTier` での絞り（`:362`＝詳細の 404 ガードと一致させるため）は**残す** |
| `:176` | `StaffHomePage({ searchParams })` にして `await`（`manager/inbox/page.tsx:120-122` と同型） |
| 定数 | `MY_VOICES_PAGE_SIZE`（現 `MY_VOICES_LIMIT` の 5）と `MY_VOICES_MAX_TAKE` を置く。Team voices の `.limit(5)`（`:295`）も定数化（決定5） |
| `:243` / `:382` | `myHighQuery.limit(take)` / `.slice(0, take)` |
| 追加 | `myHighQuery` と同条件の `count: "exact", head: true` を `Promise.all` に1本足し、`hasMore` を作る（決定3） |
| `:446-450` 付近 | 一覧の下に「もっと見る」（`<Link>`・クライアント JS を足さない）／読み切ったら「すべて表示しました（N件）」 |
| `:440-442` | **§19 以前のままのコメントを直す**（→ §19 未対応6） |

**1ページあたりのクエリ本数は 4 本**（購入1・low 1・high 1・high の exact count 1）で、
**すべて `Promise.all` で並列にできる＝N+1 にならない。**

### commit の順序

| # | commit | 実機で見るもの |
|---|---|---|
| 1 | 購入を先に引く形へ組み替え（決定1・`LOW_RATING_SCAN` 撤廃）。あわせて `:440-442` のコメントを直す | **見た目は変わらない＝回帰のみ。** 行数・並び・リンク先が変わっていないこと |
| 2 | `?take=` と「もっと見る」を足す（決定3・4）＋ Team voices の定数化（決定5） | 「もっと見る」で 6 件目以降が出る／読み切りで「すべて表示しました」 |

**2本に分ける理由**: **取得構造の組み替えと UI の追加を混ぜると回帰の原因切り分けができなくなる**（§6.4）。
§19 で commit を4本に割ったのと同じ考え方。

**実装 commit（2026-09-20・決定どおり2本）**:
`bec3b60`（1 購入を先に引く・`LOW_RATING_SCAN` 撤廃）/ `12fc436`（2 `?take=` と「もっと見る」）。
**各 commit で `npm run lint` は 9 errors / 3 warnings（変更前と同数）・`npm run build` 成功。**

### 本番実機確認（**2026-09-20 実施・完了**）

**CARTA**・staff は **「原」**（運営者本人の `staff` アカウント）。

| # | 確認項目 | 結果 |
|---|---|---|
| 1 | **commit 1 の回帰**（見た目が変わらないこと） | **合格**。「あなたに届いた声」が **5件・日付の新しい順**（9/20・9/19・9/18・9/15・9/13）で、**同じ日付の行が2つ並ばない**（§19 確認12 と同じ観点）。**Team voices も従来どおり5件**。例外は出ない |
| 2 | **素の `/staff`** | **合格**。**URL にクエリが付かない状態**で「もっと見る」が出る（決定3 の「既定値のときはクエリを付けない」） |
| 3 | **「もっと見る」を押す** | **合格**。URL が **`/staff?take=10`** になり、行が **5件→6件**に増えた（**8/22 の分が現れた**＝従来は読めなかった行） |
| 4 | **読み切り** | **合格**。**「すべて表示しました（6件）」**に変わり、「もっと見る」が消える（`hasMore` の判定どおり） |
| 5 | **Team voices** | **合格**。**5件のままで「もっと見る」は出ない**（決定5） |

**この確認で `stamp_only` 行（「評価スタンプが届きました」）は現れなかった。**
原の購入6件が**すべて `everyone` かつ `rating>=3` の感想に対するもの**で、**`full` 行として本文つきで出ている**ため。
`stamp_only` になるのは「**本文を見せない感想に購入がある**」ときだけなので、**設計どおり**
（`manager_only` ＋購入の組み合わせは §19 の本番確認で Niii の藤原和音宛てに確認済み）。

### migration

**不要。**
`reviews` の索引は **`salon_id` 単独（`idx_reviews_salon`・`0001:85`）と `customer_id`（`:86`）のみで
`staff_id` の索引は無い**（`rating_purchases` も `salon_id` / `customer_id` のみ・`:87-88`）。
それでも **CARTA はサロン全体で 53 件**（§17 本番確認）なので今は不要。
要るとしたら **`reviews(staff_id, created_at desc, id desc)` と `rating_purchases(staff_id)`** だが、
**§17 未対応2（1サロン 500 件超でのキーセット移行）と同じ migration にまとめる**
（`reviews` への索引追加を2回に分けない）。
番号は **§21 が 0048 を予約済み**のため **0049 以降**。

### 範囲外（今回やらない）

- Team voices の「もっと見る」（決定5。**定数化だけ行う**）
- `/staff` のキーセット移行（§17 未対応2 と同じ 500 件超が条件）
- 一覧に名前・ティア・rating を出すこと（**§13 決定2・§19 決定5 を維持**）
- 件数マトリクス（`countRows`）の仕様変更

### 未対応・確認事項

1. **`.in("id", ids)` の URL 長上限。** **§20 未対応3 と同じ懸念**で、
   **どちらかで観測したら両方に効く**。現在の購入は**システム全体で 21 件＋実機テスト分**
   （§13・§14・§19 の残置）なので当面は無関係。
2. ~~`take` の上限（`MY_VOICES_MAX_TAKE`）の値を決めていない~~ → **1000 に決めた**（`12fc436`）。
   Inbox の `INBOX_MAX_TAKE`（`manager/inbox/page.tsx:88`）と**同じ理由・同じ値**
   （**PostgREST の `max-rows` 既定 1000 が静かに切り捨てる**ため、その手前で止める）。
3. **一覧の下のお客様向け注記が §19 以後は不正確**（2026-09-20 追加）。
   「**※上の件数と一致しないことがあります。お客様が店長にだけ届けたいと選んだ声などは、
   ここには表示されません。**」（`staff/page.tsx:604-606`）と、**その上のコード内コメント**
   （「この一覧（everyone かつ rating>=3）」）は、**`manager_only` でも購入があれば
   `stamp_only` 行として出る**ため実態と合わない。
   **差し替え文言は未決。** `/mypage` の補助文字（§15 未対応6）と**同じ性格の課題**なので、
   文言を決めてから直す。**挙動は §19・§22 のとおりで、表示される行は正しい。**
