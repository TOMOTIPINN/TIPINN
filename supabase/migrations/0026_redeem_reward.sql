-- 本番適用済み（2026-07-16・SQL Editor手動）。ファイルは事後記録。

-- ⚠️ この番号の関数定義は現存しない。0028 で drop → create し直したため、
--    redeem_reward の現行定義は 0028_redeem_reward_ok_reason.sql（本番の実物）を参照。
--    このスタブは番号の履歴に穴を作らないために残す（再実行不要・貼り付け不要）。

-- ↓↓↓ ここに pg_get_functiondef で吸い出した実物を貼り付け ↓↓↓
